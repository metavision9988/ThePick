# Phase 2 Eval MVP — apps/web 환경변수.
# Cloudflare Pages 배포 환경에서 PUBLIC_API_BASE_URL 을 production Workers URL 로 설정한다.
# (Astro 의 import.meta.env.PUBLIC_* prefix 자동 인식.)
#
# dev 시 default = http://localhost:8787 (wrangler dev) — apps/web 코드 hardcode fallback.
# staging / production 시 본 값을 명시 주입.

PUBLIC_API_BASE_URL=https://thepick-api-production.metavision9988.workers.dev
