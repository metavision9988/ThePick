# 결정 카드 #18 — E0-2 판정방법 문구 개정 소급 승인 + 무결성 러너 정기 실행 경로

> 작성: 2026-06-12 / 상태: **결재 대기**
> 배경: 채택 규칙 "게이트 문구 개정 = L3급 결재(침식 차단)"(`docs/audit/EXPANSION_GATE_DESIGN_20260611-073814.md:29`,
> 놓친 리스크 1 "게이트 침식(goalpost drift)" `:73`)의 **첫 적용 사례**. 리뷰 P4-3 "E0-2 문구 개정 결재 부재 → 결재 #18 소급 상신"
> (`.claude/reviews/review-20260611-084743-s4-integrity-runner.md:14`) + MASTER_PLAN §6b #18 행(`MASTER_PLAN.md:281`).

## ① 문구 개정 diff 실체 (git 검증 — 커밋 `1509405` → `e3adbf1`)

| 구분     | 구판 (커밋 1509405)                               | 현판 (커밋 e3adbf1, 현행 `EXPANSION_GATE_DESIGN_20260611-073814.md:37`) |
| -------- | ------------------------------------------------- | ----------------------------------------------------------------------- |
| 판정방법 | 고아0·끊김0·순환0 **+ CONCEPT-023 기지양성 검출** | 고아0·끊김0·순환0·**stale참조0 (러너 gatePass)**                        |
| 현황     | ✗ (미실행)                                        | 러너 가동 ✓ / **판정 ❌ FAIL** (1차 실측 고아 24·유령 103 상세 병기)    |

- **개정 사유**: 1차 실측(2026-06-11, 794/1274)이 "CONCEPT-023 기지양성" 전제를 반증 — 유효 inbound 1(F-21 활성)의 약연결, 절대 고립 아님(E0-2 행 ※기지양성 문구 정정 기록).
- **침식 여부**: 판정 결과 불변(개정 전 미통과 → 개정 후에도 FAIL — 개정으로 통과된 게이트 아님) + 검사항목 stale참조0 **추가** = 강화 방향. 단 절차상 결재 전 개정이라 소급 상신(커밋 e3adbf1 메시지에 "#18 신설(E0-2 문구 개정 소급)" 자기 기록).

## ② 러너 실체·실행 이력 (실코드)

- `scripts/run-graph-integrity-production.ts` — read-only 감사(production 쓰기 0, `:10-11`) / 덤프 부재 시 가짜 PASS 차단 `fail()`(`:43-48`) / `exit gatePass ? 0 : 2`(`:165`). 입력 = `wrangler d1 execute --remote --json` 덤프 2건(헤더 `:14-21`).
- 실행 이력 `g-ws2-integrity/`: **6회 전부 수동** — KST 06-11 5회 · 06-12 1회 (파일명 타임스탬프 = UTC, `2026-06-10T23-*` 4건 = KST 06-11 오전). 최신(`integrity-2026-06-12T04-07-20.md`): 유령 0(#20 Track A-1 수리 반영 — 본체 11 복원·유령 103 전소, 커밋 `c426f2c`)·고아 24 잔존·끊김 0·순환 0 → **FAIL 유지** (잔여 = Track B 고아 24).
- CI cron 선례: `.github/workflows/d1-schema-drift.yml:31-33` 일일 cron + `:52-53` `secrets.CLOUDFLARE_API_TOKEN/ACCOUNT_ID` 주입. 단 시크릿 실설정 여부 [미조사 — gh 미인증으로 확인 불가] + 해당 토큰의 read-only 범위 여부 [미조사].

## 선택지 비교 — 러너 정기 실행 경로

| 기준          | (a) 수동 유지 (현행)                                      | (b) CI cron (GitHub Actions)                                             | (c) 보류           |
| ------------- | --------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------ |
| 추가 작업     | 0 (2일간 6회 실행 실적)                                   | workflow 신설 + **read-only 전용 토큰** 시크릿 등록(쓰기 토큰 상주 회피) | 0                  |
| 드리프트 감지 | 세션 없으면 공백 (지연 위험)                              | 무세션 기간도 검출 — 단 현 상태 FAIL 이라 매일 알람 = 노이즈             | 공백 지속          |
| 시크릿 위험   | 0 (로컬 wrangler 세션)                                    | Cloudflare 토큰 CI 상주 — 범위 검증 [미조사] 선결                        | 0                  |
| 시의성        | E0-2 수리 진행 중(고아 24)·적재 빈도 낮음 — 수동으로 충분 | 첫 PASS 후 "PASS 유지 감시"로 전환 시 가치 상승                          | #18 행 처분 미종결 |

## 권고 (결정은 진산)

**① 소급 승인 + ② (a) 수동 유지** — ①은 강화 방향·판정 불변·실측 반증 근거가 git 으로 추적 가능(반려 시 러너 헤더 `:6-8`·보고서 6건 연쇄 재정정 필요). ②는 현 FAIL 수리 중이라 cron 알람 가치가 낮고 토큰 시크릿 실태 [미조사] — **E0-2 첫 PASS 시점에 (b) 전환을 별도 카드로 재상신**(d1-schema-drift 선례 패턴 준용)이 순서상 안전.

> 진산 확인란 ① 문구 개정 소급: ☐ 승인 / ☐ 반려(구판 복원 + 러너·보고서 재정정)
> 진산 확인란 ② 러너 정기화: ☐ (a) 수동 유지(첫 PASS 후 (b) 재상신) / ☐ (b) CI cron 즉시 / ☐ (c) 보류
