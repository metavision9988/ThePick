# 역이식 실행 체크리스트 — 살아있는 진행 정본

> 2026-08-06 신설 · 근거 = `catchall-역이식-분석-20260806.md`(기술) · `catchall-역이식-쉬운말-20260806.md`(요약)
> **운영 규칙** (이음길에서 배운 규율 그대로): ① 작업 단위가 끝날 때마다 **같은 커밋에서** 이 파일을 갱신한다
> ② 체크는 **출력물을 직접 확인한 뒤에만** ③ 취소는 삭제가 아니라 취소선 + 사유 ④ 여기와 결재 대시보드가
> 어긋나면 **이 파일이 아니라 둘 다** 고친다(정본 이원화 금지).
>
> **진행**: **21/31** — STAGE 0 ☑☑☑☑☑ / 1 ☑☑◐☑☑☑ / 2 ☑☑☑☑☑ / 3 ☑☑☑☑☑☑ / 4 ☐☐☐☐☐ / 5 ☐☐☐☐
> ★**STAGE 2 의 ☑ 는 전부 "선작성 — production 미적용"** 이다(0047 미적용 · 백필 미실행 · 커버리지 현재 0/551).
> 즉 **작업은 끝났고 반영은 안 됐다.** 이 구분이 흐려지면 "게이트 PASS = 전수 검증" 착시가 그대로 재현된다
> (독립 리뷰 CRITICAL 지적 반영 — 진행 카운터가 미적용 작업을 완료로 산입한다는 점을 여기 명시해 둔다).
>
> ⚠️ **카운터 정정 (2026-08-07)**: 직전까지 "11/29" 로 적혀 있었으나 **실제 체크는 8개**였다
> (기계 집계: `grep -c '^- \[x\]'` = 8 · 미체크 21). 근거 없이 3개가 부풀어 있었다 —
> 진행률을 손으로 세는 한 이 종류의 드리프트는 재발한다(이 파일이 스스로 겪은 실측 사례로 기록).
> 본 갱신에서 **1-5 신설(+1) → 분모 30 · 분자 9** 로 정합화.
>
> **최종 갱신** 2026-08-07(3차) — **독립 리뷰 3라운드 처분 반영** (커밋 `7dc4b40`).
> 라운드별 raw: 1R(5-페르소나) CRITICAL 3·MAJOR 24 / 2R(3렌즈) CRITICAL 1·MAJOR 8 / 3R(범위한정) CRITICAL 1·MAJOR 1.
> **★2R·3R 의 CRITICAL 은 전부 "직전 라운드의 수정"이 만든 것**이다 — 1R 처분이 새 blackout 침묵을,
> 2R 처분이 fail-open 재개방을 낳았고 각각 다음 라운드가 실행 재현으로 잡았다.
> 코드 처분 = **마이그 0046 신설**(세 번째 문 · 트리거 5) + 계보 판정 **성분(union-find) 단위 재작성**
> (`LINEAGE_LAPSE` 신설) + ★**미러 차등 테스트**(실 SQLite ↔ TS 54조합, fail-open 1건이라도 red).
> 세 라운드 연속 같은 함수에서 터진 원인은 "손 미러를 손으로 검증"하는 구조였고, 채점자를 SQLite 로 바꿔 닫았다.
> 보고서 `.claude/reviews/review-20260807-5persona-0045.md` (§1R·§2R·§3R).
> 검증(08-07 3차 시점): api 843 · quality 115 · scripts 28 · E2E 26/26 · turbo typecheck·lint 34/34 ·
> 훅 회귀 38/38 · 변이 누적 7/7 red.
>
> **STAGE 2 검증(08-08)**: api **859** · batch **332** · quality **119** · scripts 28 · E2E 26/26 ·
> typecheck·lint **34/34** · G-1 · 훅 38/38 · 추출 게이트 **58/59 위반 0 + 의도적 제외 1(사유 영속)** ·
> 변이 **3/3 red**. **독립 리뷰 2렌즈**: raw CRITICAL 3 · MAJOR 12 → **전건 처분**
> (보고서 `.claude/reviews/review-20260808-stage2-source-quote.md`).
> ★**production 무접촉 유지**(0039·0045·0046 미적용 · 백필 미실행 · SELECT 만 수행).
>
> 직전(2차) 갱신 기록: 진산 **"c 진행"** = 결정 카드 (C) 채택 집행. 검증: §STAGE 1 (1-5).
>
> 직전(1차) 갱신 기록: 진산 "0·1단계 진행" 집행 + 독립 리뷰 수리. turbo 17/17 · api 808 · batch 332 ·
> quality 86 · web 79 · formula-engine 359 · parser 179 · E2E 26/26 · scripts 27 · 훅 회귀 38/38 ·
> typecheck·lint·G-1 green. **독립 리뷰**(`wf_02ed73c4-f29`, 44 에이전트·5.0M tok): 제기 39 → 기각 12 →
> 생존 27 → **전건 처분 · CRITICAL 0**. 보고서 = `.claude/reviews/review-20260807-002958-stage01-backport.md`.
> ★리뷰가 **가드 신설 커밋이 같은 클래스 결함을 재도입한 것**(jq 실패 시 무음 통과)을 적발했다.
> ★신설한 드리프트 가드가 즉시 **마이그 누락 7건(0021~0027)** 을 추가 적발.
>
> ~~**잔여 1건 = 결재**: 0042 승계 트리거 ↔ 시행시점 창 (대시보드 §A #9)~~ → **✅ 결재·집행 완료**
> (진산 2026-08-07 "c 진행" = **(C) 정책 + 최소 가드**). 경위: "권고대로 진행"(권고 (a)) 접수 →
> **착수 전 실측에서 그 권고가 그대로는 틀림을 확인**(blackout → 시행일 이후 구·신 **동시 노출**로
> 바뀔 뿐. SQLite 에 시간 기반 트리거 부재) → 코딩 중단·재상신 → (C) 채택.
> 정본 = `decision-card-20260807-supersedes-effectivity.md`. **STAGE 2 선결 해소.**
> **핸드오프** = `.jjokjipge/handoff-20260807-backport.md`

---

## 0. 한번에 할까, 나눠서 할까 — 결론: **나눠서. 단, 5묶음으로.**

### 한번에 하면 안 되는 이유 (물리적인 것부터)

| #   | 이유                              | 구체적으로                                                                                                                   |
| --- | --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| 1   | **순서가 강제된다**               | 카드에 원문을 저장하기 전에는 검증 엔진이 **검사할 대상이 없다.** 순서를 바꿀 수가 없다                                      |
| 2   | **안전장치를 켜면 작업이 막힌다** | 지금 꺼져 있는 장치를 켜는 순간, 지금까지 통과하던 편집이 차단된다. 짝이 되는 장치와 **같이** 켜지 않으면 작업 자체가 멈춘다 |
| 3   | **결재가 병목이 된다**            | 데이터 구조를 바꾸는 건 진산 승인이 필요하다. 한꺼번에 몰면 승인 한 번이 늦어질 때 전부가 선다                               |
| 4   | **효과를 측정할 수 없다**         | 다섯 개를 동시에 넣으면 좋아진 게 어느 것 때문인지 모른다. 다음에 뭘 더 할지 정할 근거가 사라진다                            |
| 5   | **라이브 서비스가 돌고 있다**     | 학습자 검색 경로를 건드리는 작업이 섞여 있다. 다른 변경과 섞으면 문제가 생겼을 때 원인을 못 가른다                           |

### 그래도 **반드시 묶어야 하는 것**이 있다 (쪼개면 사고 남)

- 안전장치 수리 ↔ 짝 장치 → **같은 작업 단위**. 따로 하면 세션이 멈춘다
- 연결선 보호 ↔ 부활 차단 → **같은 파일 한 장**. 어차피 같은 자리다
- 원문 칸 만들기 ↔ 기존 카드 채우기 → **같은 흐름**. 칸만 만들고 안 채우면 _"검사 통과 = 전부 검증됨"_ 이라는 착각이 생긴다

### 그래서 이렇게 나눈다

```
STAGE 0  지금 새는 것 막기          결재 불요       1~2일     ← 오늘 시작 가능
STAGE 1  안전장치 마저 채우기        결재 1줄        2~3일
STAGE 2  ★관문 — 원문 문장 저장     결재 1줄        1~2주
STAGE 3  검증 엔진 가져오기          결재 불요       2~3주
STAGE 4  빠뜨린 것 찾기 + 자동화 준비 결재 1줄       3~4주
STAGE 5  나머지 (틈새 작업)          결재 불요       상시
```

**진산이 해야 할 일은 전 구간 통틀어 「1줄 답변 3번」이다.** 나머지는 자율 진행.

---

## STAGE 0 — 지금 새고 있는 것 막기

> **성격**: 개선이 아니라 **수리**. 새 기능 0. 위험 구역 아님 → 결재 불요
> **왜 지금**: 안전장치가 현재 꺼져 있음(라이브 프로브로 확정)
> **예상**: 1~2일
>
> ★★ **집행 중 실측 정정 (2026-08-06)** — 착수 전 이 자리에 _"8월 15일 시행일까지 9일"_ 이라고
> 적었던 긴급성 근거는 **과장이었다.** production SELECT 실측:
> ① `valid_from`/`valid_until` 채워진 행 **0/857** ② 0041 이 지목한 미시행 4노드는 **전부 draft**
> (학습자 경로에 이미 미노출) ③ `2026.8.15` 를 담은 유일한 approved 노드 LAW-145 는 개정을
> _"(시행 2026.8.15 개정: …)"_ 괄호로 **병기** — 현행으로 위장하지 않음.
> ⇒ **현재 학습자 오노출 0건.** 데이터를 보기 전에 "지금 새고 있다"고 단정한 것으로,
> catchall 실수 로그 2026-08-04(부분 확인으로 존재/부재 단정) 와 같은 클래스다.
> 남는 것은 **잠재 결함(구조적 공백)** 이며 수리 가치는 유지되므로 그대로 집행했다.
> 대신 **production 백필은 하지 않았다**(불급 + 진산 인증 게이트).

- [x] **0-1 안전장치 배선 수리** ✅ 2026-08-06 — 위험 파일(계산식·상수·데이터 구조)을 고칠 때 막아 주는 장치가
      경로를 못 받아 아무것도 안 막고 있다. 입력 방식을 형제 장치와 같게 맞춘다 - 완료 판정: L3 경로 파일에 실제로 편집을 시도했을 때 **차단된다** (오늘 프로브 재현)
- [x] **0-2 짝 장치 3상태화** ✅ — ★**0-1과 반드시 같은 작업 단위.** 따로 하면 세션이 멈춘다 - 완료 판정: 리뷰 완료 후 결재 대기 상태가 구분돼서, 작업이 무한 대기에 빠지지 않는다
- [x] **0-3 장치 경로를 프로젝트 기준으로** ✅ — 지금은 1호 폴더 경로가 박혀 있어서
      **2호(전기기사) 작업 세션이 1호 장치를 실행한다** - 완료 판정: 2호 작업공간에서 편집 시 2호 장치가 뜬다
- [x] **0-4 시행일 반영** ✅ (실제로는 두 줄 + enrichRelatedNodes 봉합) — 8월 15일 시행 내용이 오늘 현행처럼 나가는 것을 막는다 - 완료 판정: 미래 시행일 카드를 하나 넣고 학습자 화면 3곳에서 조회 → **전부 안 보인다** - 부수 의무: 검색 속도 재측정 (이 경로가 성능 민감 구간이라 숫자로 확인)
- [x] **0-5 STAGE 0 종합 확인** ✅ 훅 38/38 + 변이 실측 3/3 red (가드 제거 시 정확히 red, 복원 diff 0) — 위 4개를 실제로 깨 보고(일부러 틀리게 해서) 막히는지 본다 - ★이음길 교훈: **"장치는 만든 즉시 고의로 깨서 안 되는 걸 확인한다."** 안 하면 장치가
      조용히 죽어 있어도 아무도 모른다 — 0-1이 정확히 그렇게 죽어 있었다

> **STAGE 1 진입 조건**: 0-1~0-5 전부 ☑

---

## STAGE 1 — 안전장치 마저 채우기

> **성격**: 데이터 보호 장치 추가 · **진산 1줄 필요** (대시보드 §A #3)
> **주의**: 대시보드 #3은 "이미 작성된 걸 적용 승인"으로 적혀 있었으나 **사실은 아직 안 쓰였다.**
> 정정본 = "작성 착수 승인"
> **예상**: 2~3일

- [x] **1-1 카드 연결선 보호** ✅ SQL 작성·검증 완료 (production 미적용) — 카드 1,347개를 잇는 연결선이 지금 삭제·변경 무방비다
      (카드 본체는 이미 보호돼 있는데 연결선만 빠져 있음) - 완료 판정: 연결선 삭제 시도 → 거부
- [x] **1-2 폐기 내용 부활 차단** ✅ 0039 합본 (production 미적용) — ★**1-1과 같은 파일 한 장에 합본.** 개정으로 은퇴시킨
      구버전 카드가 되살아나는 걸 막는다 - 완료 판정: 은퇴 카드를 되살리는 조작 → 거부 / 정상적인 은퇴 처리는 통과 - 주의: 되살리기를 막으므로 **복구 절차는 "새로 만들어 승격"으로만** 문서화해야 한다
- [ ] **1-3 0040(WS-6c) 처리 여부 결정** ◐ 별건으로 분리 — 이번 0039 에 미포함(스코프 유지) — 별건이라 같이 갈지 뺄지만 정한다
- [x] **1-4 무회귀 확인** ✅ api **808** 통과(기준선 780 대비 +28, 회귀 0) + 전 워크스페이스 green + E2E 26/26
- [x] **1-5 시행일 전 승격 차단** ✅ 2026-08-07 (진산 "c 진행" = 결정 카드 (C)) — ★**신설 항목**
      (원 29항목에 없던 것. 결재 #9 이 STAGE 2 선결로 확정되면서 STAGE 1 에 편입) - 무엇: "오늘 유효하지 않은 판본은 승인 상태로 서빙 자격을 얻을 수 없다"를 **DB 가 강제**한다.
      개정본을 **미리 만들어 두는 것은 그대로 가능**하고, 막는 것은 **승격 시점**뿐이다 - 산출: `migrations/0045_block_premature_promotion.sql` (트리거 6 = 승격 게이트 3 + 백필 게이트 3) +
      시나리오 테스트 22 + 무결성 러너 계보 불변식 2종 + 0039 헤더 경고 해소 - ★**카드 문면 밖 확대 2건(명시 기록)**: ①`valid_until` 대칭(만료본 승격도 차단)
      ②**백필 게이트** — 카드는 "미시행 행의 승격"만 막지만, **이미 승인된 행에 미래 시행일을
      채우는 문**이 하나 더 열려 있었다. **그 문을 여는 것이 정확히 STAGE 2 백필**이라 함께 잠갔다 - 완료 판정: 미시행 노드 승격 시도 → 거부 / 시행일 도래 후 승격 → 통과 + 구본 정상 은퇴 +
      **서빙이 신본 단독**(blackout 0 · 동시 노출 0, 서빙 코어로 end-to-end 대조) - 변이 실측 **8/8 red**(가드를 하나씩 고의로 제거·약화 → 전부 검출. 복원 sha256 동일) - 검증: api **830**(기준선 808·회귀 0) · quality **100**(86) · turbo 17/17 · E2E 26/26 ·
      typecheck·lint·G-1 green. ★production 무접촉 - ⚠️ **0045·0039 production 적용 = 별도 결재** (묶어서 대시보드 §A 재상신)

- [x] **1-6 세 번째 문 봉쇄** ✅ 2026-08-07 — ★**5-페르소나 독립 리뷰 CRITICAL 처분으로 신설** - 무엇: 1-5 가 "문은 둘"이라고 단정했으나 **문은 셋**이었다. 승인 기록을 노드보다 **먼저**
      넣으면(전이 선행) 1-5 의 게이트가 대상 부재로 통과하고, 뒤이어 들어온 미시행 행이
      SUPERSEDES 엣지 하나로 구본을 은퇴시킨다 = 같은 blackout. 0039 때문에 **복구 불가** - 산출: `migrations/0046_close_third_promotion_door.sql`(트리거 **5** = 0042 [1]·[2] 재생성 2 +
      INSERT 게이트 3) + 시나리오 **13** + 무결성 러너 계보 판정 **성분 단위 재작성**(`LINEAGE_LAPSE` 신설) + ★**미러 차등 테스트**(`scripts/__tests__/effectivity-mirror-differential.test.mjs` — 실 SQLite ↔ TS 54조합) - 완료 판정: 전이 선행 우회 시도 → 차단 / 미발효 승계자는 은퇴 못 일으킴 /
      **정상 승계는 종전대로**(서빙 = 신본 단독) — 전부 서빙 코어 결과 집합으로 e2e 대조 - 변이 실측 누적 **7/7 red**(EXISTS 제거 / INSERT 게이트 삭제 / COALESCE 붕괴 / [C-3] EXISTS 제거 /
      `is_current_active` 제거 / 성분판정 회귀 / 개방형 정규식 회귀 — 전부 검출, 복원 sha256 동일) - ★**리뷰 3라운드**: 1R(5-페르소나) CRITICAL 3 → 2R(3렌즈) **1R 처분이 만든** CRITICAL 1 →
      3R(범위한정) **2R 처분이 만든** CRITICAL 1. 매 라운드가 직전 수정의 결함을 실행 재현으로 적발했다.
      마지막 CRITICAL 은 손 미러를 손으로 검증하던 구조 문제였고, **차등 테스트로 채점자를 SQLite 로 바꿔** 닫았다. - ⚠️ **0039·0045·0046 = 묶음 적용** (부분 적용 시 "발생은 못 막고 복구만 막힌" 최악 조합)

> **STAGE 2 진입 조건**: 1-1·1-2·**1-5·1-6** ☑ (1-3은 별건).
>
> ⚠️ **정정 (2026-08-07 리뷰 MAJOR)**: 직전 문면은 "1-5 는 카드가 **STAGE 2 의 선결**로 지목한
> 항목 — `valid_from` 에 값이 들어가는 순간 문제 구간이 실재한다"였다. **두 로드맵의 "2"를
> 혼동한 것이다.** 역이식 STAGE 2 = `source_quote`(원문 문장) 축이고, `valid_from` 백필은
> **Revision Watch Phase 2**(0041 헤더의 미시행 4노드)다. 즉 1-5/1-6 은 STAGE 2 의 선결이
> 아니라 **Revision Watch Phase 2 백필의 선결**이다. STAGE 2 는 이것과 무관하게 진입할 수 있다
> (막을 이유가 없어졌다는 뜻이지, 1-5/1-6 의 가치가 줄었다는 뜻은 아니다).
> 현재 잠금 완료(단, production 적용은 미승인).

---

## STAGE 2 — ★관문: 카드에 원문 문장 저장

> **성격**: **이게 전부의 관문이다.** 이거 없이는 STAGE 3의 검사기가 검사할 대상이 없다
> **진산 1줄 필요** (대시보드 §A #7) — 데이터 구조 변경
> **예상**: 1~2주 (채워 넣는 양에 따라)

- [x] **2-1 원문 칸 만들기** ✅ 2026-08-08 — 카드·산식·상수 세 곳에 `source_quote TEXT` 추가
      (`migrations/0047_source_quote_axis.sql`, 0041 ALTER 선례 그대로). schema.ts 타입 동기(NC-1).
- [x] **2-2 나중에 채울 수 있게 허용 규칙 편입** ✅ — 0041 화이트리스트 패턴에 `source_quote` 편입 - ★칸만 만들고 미루면 그 사이 값→값 UPDATE 가 **무음 통과**한다(새 컬럼은 기존 가드의 WHEN 절에
      아예 없다 — 0041 이 겪은 구멍). 그래서 **같은 마이그**에 넣었다 - 완료 판정: 빈 칸 채우기 통과 / 채워진 값 변경 ABORT / NULL 되돌리기 ABORT (3종 대칭 · G-S2-2)
- [x] **2-3 앞으로 만드는 카드는 원문 필수** ✅ — `require_source_quote_on_batch_load` 트리거 - **범위 = `batch_id` 선언 행**(= 배치 적재분). 체크리스트 문면이 이미 "새로 적재하는 **서식**"으로
      한정했고, 실측상 적재 경로는 batch_id 를 **항상** 채우고 테스트 픽스처는 **한 곳도** 안 쓴다 - 완료 판정: 원문 없이 적재 ABORT / **공백·탭·개행만 넣어도 ABORT** (G-S2-3) - ★**적재 계약도 함께 갱신**: `draft-loader` INSERT 에 컬럼 추가 + 계약 타입에 `source_quote`.
      게이트를 켠 순간 batch 테스트 3건이 red 가 됐다 = **의무화가 실제로 작동한다는 증거** - ⚠️ 막지 않는 것(정직): batch_id 없는 수기 INSERT·테스트는 게이트 밖 = **의도된 경계**.
      전역 강제는 기존 857 행 백필 완료 후에만 가능하다
- [x] **2-4 기존 카드 채워 넣기 — 법령 카드부터** ✅ **SQL 생성까지** (LAW-144~202 중 **58장**) - 산출: `docs/batch-load/stage2-source-quote/` (추출 스크립트·매핑·백필 SQL·검수 시트) - **결정론 추출**(pdfplumber) · LLM 생성 0 · 원문은 언제든 PDF 로 재현 가능 - ★**description 복사 아님**: 실측상 description 은 요약·편집본이라(LAW-144 는 편집 주석까지 포함)
      복사하면 "원문 대조"가 자기 자신 대조가 된다 → 동일성 검사 0건 확인 - ★게이트가 두 번 실제로 잡았다: ①**목차(TOC) 줄을 조문으로 오인**(29건) ②**조문 내부 참조에서
      조기 절단**(19건). 개수(59/59)만 봤으면 둘 다 통과했을 것이다 - 완료 판정: 게이트 위반 0(제목 일치·description 비동일·길이·조번호·전수) + e2e
      (노드 선적재 → 0047 적용 → 백필 = 59행 보유 · 재실행 0행 · 사후 수정 ABORT) - ⚠️ **production 백필 실행 = 별도 인증 게이트** (0047 적용과 묶음)
- [x] **2-5 착각 방지 표기** ✅ — 무결성 러너에 `source_quote` 커버리지 `M/N` + 미보유분 **"미검증 — 검사 대상 밖"** 으로 분리 명시 - ★게이트 **불산입** 지표다: 커버리지 0% 여도 gatePass 는 다른 조건만 본다.
      "위반 0건"이 "전수 검증"을 뜻하지 않는다는 것을 리포트가 스스로 말하게 했다

> **STAGE 3 진입 조건**: 2-1~2-4 ☑ **＋ 0047 production 적용 + 59장 백필 실행 완료** (법령 59장 기준).
> ★후자를 빼면 안 된다 — STAGE 3 검사기의 실제 전제는 "대조할 원문이 **DB 에** 실재"이고,
> 현재 커버리지는 **0 / 551** 이다. 문서상 ☑ 와 데이터상 0% 를 혼동하지 말 것(독립 리뷰 CRITICAL).
> ★그리고 착수 전 **held-out 분리**가 필요하다: 백필 59 = 2026-07-14 위임 일괄 승격분(P1+P2)의 전량이라
> 첫 측정 표본이 곧 burned 표본이 된다(대시보드 §A #8 이 지목한 순환편향). plan §위험 분석 참조.

---

## STAGE 3 — 검증 엔진 가져오기

> **성격**: 이음길에서 이식. 새로 설계하지 않음. **이미 결재된 계획의 실행** → 결재 불요
> **예상**: 2~3주

- [x] **3-1 인용 진위 검사** ✅ 2026-08-08 — 문자 바이그램 커버리지(임계 0.95, 최소 5 바이그램 fail-closed) - 이식원 catchall `lib/bigram-coverage.mjs` ADOPT. 청크 = STAGE 2 가 조문 단위로 끊어 둔 인용 - ⛔ ~~파일럿 58장 전건 통과(실패 0) = 독립 확증~~ **철회 (독립 리뷰 CRITICAL, 2026-08-08)**:
      청크를 **인용을 찾은 자리**에서 뽑았으므로 `quote ⊆ chunk` 가 58/58 성립하고,
      그러면 커버리지 1.0·LCS 1.0 은 **측정이 아니라 수학적 항등식**이다. 확증이 아니다.
      ★더 나쁜 것: 원래 3-1 이 낸 신호 **9건은 상법 노드 페이지축 오류라는 진짜 발견**이었는데
      내가 그것을 "검사 입력 결함"으로 규정하고 청크를 옮겨 **0으로 지웠다.**
      ⇒ **3-1·3-3 은 현재 "미측정"** 이다. 인용과 무관한 기준으로 청크를 재정의해야 측정이 시작된다
- [x] **3-2 숫자 정합 검사** ✅ — `%`·`일`·`개월`·`배`·`ha`·`kg`·`원`·`회` 단위별 대조 - 이식원 `money.ts`+`g4` **ADAPT**(금액 전용 → 손해평가 도메인 재작성). "65%를 60%로" 위험을 처음으로 기계가 막는 지점 - ⚠️ **description 에 대해서는 과탐이 남는다** — 아래 3-6 참조(임계 문제가 아니라 **입력 층 불일치**) - ⛔ **정정**: 조문번호(`제N조`·`제N호`·`제N자`)를 값에서 제외했던 것은 상위 plan
      `autoverify-package.plan.md` **G-AV-NUM("조문번호 포함")의 반전**이었다 → `자` 제외를 되돌렸다.
      독립 리뷰가 `제3자→제4자`·`제1호→제7호` 위조가 **양쪽에서 함께 삭제돼 무방비**임을 실측했다
- [x] **3-3 짜깁기 탐지** ✅ — LCS 연속 비율(임계 0.8). 바이그램이 순서를 안 보는 사각을 덮는다 - ⛔ 위 3-1 과 **같은 항등식 문제** — 현재 "미측정"
- [x] **3-4 근거 충분성 검사** ✅ — 주장의 날짜·수치 앵커가 인용 안에 실재하는가. 조항 참조·노드 ID·호 열거는 앵커 아님
- [x] **3-5 일부러 틀린 것 10개 넣어 보기** ✅ **10/10 검출** - 숫자 바꿔치기 4(회수·비율·단위·날짜) · 주체 바꿔치기 2(주체·의무↔재량) · 재배열 2(짜깁기·조문 접합) · 인용 절단 2 - ⛔ **정정 (독립 리뷰 MAJOR)**: 10종 중 ⑤⑥은 **외래 어휘**를 써서 쉬웠다. **원문 어휘만으로**
      같은 클래스를 만들면 바이그램·LCS **둘 다 통과**한다 = 현 엔진의 진짜 한계.
      `bigram.ts` 가 "방어선은 LCS 와 사람 검수뿐"이라 적었는데 LCS 도 못 막는다.
      ⇒ 강화판 2종(⑤′⑥′)을 `it.fails` 로 **못 잡는다는 사실 자체를 테스트로** 고정했다(고쳐지면 red 로 알림) - ★적대 테스트가 실제 구멍을 잡았다: `회` 가 단위 목록에 없어 "연 1회 → 연 1일" 위조가 통과했다.
      ⛔ **정정(독립 리뷰 MAJOR)**: 실제로 그 위조를 죽인 것은 `[/회/,'count']` **별칭 추가**이고,
      내가 "구멍을 막았다"고 쓴 `raw:` 폴백은 **되돌려도 테스트 30/30 green·파일럿 델타 0** 이었다.
      = **증거 없는 서술**이었다(일반화 자체는 유지하되 공로는 정정한다) - ⛔ ★**적대 10/10 은 `SOURCE` 가 300자짜리 장난감이라서 성립했다**(독립 리뷰 CRITICAL).
      실코퍼스 길이(median 144자)로 같은 위조를 넣으면 **4검사 전부 통과해 `pass`** 가 되며,
      3-1 의 의미 위조 검출률은 **12.5%(3/24)** 다. 검출 서명도 10종이 아니라 **7종**이다
- [x] **3-6 파일럿 — 법령 58장에 실제로 돌리기** ✅ **숫자로 보고** - 결과: 대상 58 · pass 8 · queue 39 · **reject 11** / 검사별 실패 **3-1 0 · 3-3 0** · 3-2 50 · 3-4 16 - 리포트 `docs/audit/autoverify-pilot/pilot-2026-08-08T12-46-14.md` (러너 read-only · production 무접촉) - ★**파일럿이 잡은 것은 카드 오류가 아니라 내 검사기의 과탐이었다** — 4회 반복 수정으로
      reject 30 → 11 로 줄였다(조항 참조 파편·노드 ID·호 열거 마커·날짜 표기 차이를 값에서 제외). - ⛔ **reject 11 재분류 (독립 리뷰 전수 대조 — 내 분류가 틀렸다)**:
      **카드 내용 오류 0 / 인용 부족 8 / 순수 과탐 3**(LAW-164·167·185).
      나는 "진짜 발견 2건"으로 **과소 계상**했고, 나머지를 과탐으로 읽히게 남겼다.
      ★진짜 성과는 "내 과탐 발견"이 아니라 **STAGE 2 인용 절단 8건 발견**이며,
      그중 LAW-195(응시수수료 2만원·3만3천원)·LAW-172(2년/6개월)·LAW-173(30일/3개월)·LAW-198(3년마다)은
      **CLAUDE.md 가 "65%→60% = 서비스 사망"으로 못박은 상수 클래스**다 → **STAGE 2 후속 큐로 승격**.
      ★STAGE 2 게이트 **G-S2-7g(과대 배수 = 절단 탐지)가 이 절단 8건을 못 잡았다**(인용 중앙값 144자) - ⛔ **"reject 30→11" 은 정확도 개선이 아니다 (독립 리뷰 실측)**:
      3-2 판정 가능 건수가 **47 → 20/58(34%)** 로 줄었고 **PASS 도 17→9 로 함께 감소**했다.
      값 토큰을 제외할수록 claim 에 남는 토큰이 0 이 되어 `unparsed`(queue)로 빠진다 =
      **정확해진 게 아니라 판정을 포기한 것**이다. 이 수치를 헤드라인에서 빼면 개선으로 오독된다 - ★**여기서 임계 조율을 멈췄다**: 이 58장은 burned 코퍼스(§A #8)라 계속 맞추면 **그 표본에 과적합**된다.
      plan §범위 외가 스스로 경고한 지점이며, 임계 확정은 **held-out 확보 후**(STAGE 4 선결)가 맞다. - ⚠️ **이 수치는 엔진의 일반 성능이 아니다** — "이 표본에서 무엇을 잡았나"일 뿐이다

> ⛔ **STAGE 3 진입 조건은 충족되지 않았다 (독립 리뷰 CRITICAL — 정직 기록)**:
> 이 파일이 STAGE 2 말미에 적은 진입 조건 4개(0047 적용 · 백필 실행 · 커버리지 &gt;0 · held-out 분리)가
> **넷 다 미충족**인 상태로 STAGE 3 에 진입했고, 그럼에도 3-1~3-6 을 전건 ☑ 하고 카운터를 21/31 로 올렸다.
> ~~held-out 분리는 원래 **STAGE 3 착수** 선결이었다~~ → **STAGE 4 로 이연**(사유: 로컬 산출물로 엔진을
> 만들고 적대 검증까지는 데이터 없이 가능. 단 **임계·제외 규칙 확정은 held-out 없이 하지 않는다**).
> ★그 대가가 위 3-1·3-3 항등식이다 — DB 에 원문이 없어 "출처"를 로컬에서 제조해야 했고,
> 제조의 유일한 결정론적 기준이 "인용이 있는 자리"였다. **형식을 어긴 것이 아니라 어긴 대가가 측정치로 나왔다.**
>
> **STAGE 4 진입 조건**: 3-5 ☑ + 3-6 리포트 제출 — **문면상 충족**(단 위 정정 전제).
> ⛔ **그러나 실질 선결 2건이 남았다 (독립 리뷰 CRITICAL)**:
> ① **청크 독립 재정의** — 현 파이프라인은 청크를 인용 위치에서 뽑아 3-1·3-3 이 항등식이다.
> 고치지 않으면 AI 카드 코퍼스에서도 3-1 은 영구히 무의미하다.
> ② **실코퍼스 임계 재설계** — 인용이 길어질수록 위조가 희석돼 통과한다(cov .950/lcs .821 로 pass).
> 3-3(LCS)이 유일한 실질 방어선인데 임계 0.8 이 24건 중 7건을 통과시킨다.
> ★held-out 동결 대상에 **임계 2개뿐 아니라 제외 정규식 5종**을 포함할 것(제외는 임계와 동등한 자유도).
> 보고서 = `.claude/reviews/review-20260808-stage3-autoverify.md`
> ★단 STAGE 4(자동화 준비) 착수 전 **held-out 분리**가 선결이다(§A #8): 파일럿 표본이
> 위임 승격분이라, 그대로 임계를 확정하면 자기 채점 위에 자동화를 얹게 된다.

---

## STAGE 4 — 빠뜨린 것 찾기 + 자동화 준비

> **성격**: 여기서부터 "검수 병목 해소"가 실제로 시작된다 · **진산 1줄 필요** (정답지 검수)
> **예상**: 3~4주

- [ ] **4-1 빠뜨린 구간 자동 색출** — 교재에서 **아무도 안 읽고 지나간 부분**을 매번 목록으로 띄운다 - 7월에 AI 23대를 붙여 찾은 146건짜리 감사를, 앞으로는 자동으로 - 주의: 교재는 법령과 구조가 달라서(조문 vs 절) **이 부분만 새로 만들어야 한다**
- [ ] **4-2 시점·극성 검사** — "이미 끝난 규정을 현행처럼", "제외 조건을 필수 조건으로" 같은
      의미 뒤집힘을 잡는다. STAGE 0-4의 상위 버전
- [ ] **4-3 ★정답지 만들기** — 자동 승인을 켜려면 **사람이 손으로 만든 기준 답안**이 먼저 있어야 한다.
      **이게 이 프로젝트 전체에서 가장 실제적인 비용이다** - 주의: 정답지로 쓴 자료는 자동 승인 대상에서 **영구 제외**해야 한다 (자기 채점 방지) - ★현 계획서가 지목한 부트스트랩 자료(7월에 승격한 63장)는 **이미 승격된 것**이라
      그대로 쓰면 자기 채점이 된다 → 재지정 필요 (대시보드 §A #8)
- [ ] **4-4 "믿어도 되는지" 계산기** — 표본이 몇 개일 때 오류율을 얼마나 믿을 수 있는지를
      기계가 계산해서 찍는다. 참고 수치: **63개 전부 정답이어도 "안심 기준"에는 미달, 100개면 도달** - 곁들여: 어느 시점 지시문으로 만든 자료인지 지문을 남긴다 (지금은 추적 불가)
- [ ] **4-5 승격 후속 작업 원장** — 승격은 됐는데 뒷정리가 빠지는 걸 막는다 - **실제 사례가 지금 열려 있다**: 7월에 승격한 63장이 검색 색인에 안 올라가서
      벡터 검색에 안 나온다. 현재 처분은 문서에 한 줄 적어 둔 것뿐

> **자동 승인을 실제로 켜는 것 = 여기까지 끝난 뒤 별도 결재.** 4단계를 다 해도 자동은 안 켜진다

---

## STAGE 5 — 나머지 (틈새 시간에)

- [ ] **5-1 한글 문서 본문 추출 개선** — 지금은 앞부분 일부만 읽히는 제약. 시행계획 별표 등에 필요
- [ ] **5-2 검수 화면을 질문형으로** — "원문 통독" → **"제5조 원문의 이 금액이 이 값 맞습니까 (예/아니오)"** - 진산 검수 부담을 가장 직접적으로 줄이는 항목. STAGE 3 이후 효과가 커짐
- [ ] **5-3 "없다"고 말하기 전 3중 확인 규율** — 부분만 보고 "미등재"라고 단정하는 실수를 막는다
- [ ] **5-4 외부 사실 주장에도 출처 규율** — 우리 데이터엔 엄격하면서 외부 조사에는 느슨했던 비대칭 교정

---

## 진산 응답이 필요한 지점 — 전 구간 통틀어 3번

| 시점                | 무엇                                                         | 형식                      |
| ------------------- | ------------------------------------------------------------ | ------------------------- |
| **지금**            | STAGE 0 착수 (수리 — 위험 구역 아님) + STAGE 1 SQL 작성 착수 | "0·1 진행"                |
| **STAGE 1 끝 무렵** | STAGE 2 데이터 구조 변경 + 기존 카드 채우기 범위             | "2 진행, 백필 포함"       |
| **STAGE 4 중반**    | 정답지 검수 + 부트스트랩 자료 재지정                         | 표본 확인 + "재지정 승인" |

그 외 전부 자율 진행. 각 STAGE 종료 시 **결과를 숫자로** 보고한다(몇 건 검출, 몇 건 미검증 잔존).

> ⚠️ **"3번" 은 지켜지지 않았다 (정직 기록, 2026-08-07)**: 위 표 밖에서 **1번**이 더 발생했다 —
> 결재 #9(승계 은퇴 시점 ↔ 시행시점 창). 원인은 계획 부실이 아니라 **집행 중 발견**이다:
> STAGE 0-4 로 시행시점 축을 배선하자, 그 축이 0042 승계 트리거와 충돌하는 구간이 드러났다.
> 그리고 최초 권고안 (a) 는 **착수 전 실측에서 틀린 것으로 판명**돼 재상신이 한 번 더 필요했다.
> 교훈 = **"응답 N번"은 약속이 아니라 추정**이다. 실행이 새 갈림길을 만들면 그 수는 늘어난다 —
> 줄이려고 갈림길을 감추는 쪽이 훨씬 비싸다. 남은 구간(STAGE 2·4)도 같은 이유로 늘 수 있다.
>
> ★ 다만 **결재 순서는 지켜졌다**: 코드·SQL 0줄 상태에서 상신 → 채택 후 집행. (a) 를 그대로
> 코딩했다면 "구·신 동시 노출"을 production 에 넣고 나서야 알았을 것이다.

---

## 이력

- 2026-08-06: 신설. STAGE 0~5 정의. 진행 0/29.
- 2026-08-07(후속): #9 결재 접수 → **실측으로 권고안 결함 확인 → 코딩 중단·재상신**(결정 카드 신설).
  핸드오프 작성 `.jjokjipge/handoff-20260807-backport.md`. 진행 11/29 유지.
- 2026-08-07(2차): **1-5 신설·완료** — 진산 **"c 진행"** = 결정 카드 (C) 채택 집행.
  마이그 0045(트리거 6) + 시나리오 22 + 러너 계보 불변식 2종 + 0039 헤더 해소.
  변이 8/8 red · api 830 · quality 100 · 전 게이트 green · production 무접촉.
  ★**카운터 정정**: "11/29" → 실측 8 확인 후 **9/30**(1-5 신설 반영). 손 집계 드리프트 자백 기록.
  ★카드 문면 밖 확대 2건(valid_until 대칭 · 백필 게이트) 명시 — 결정 자체는 (C) 불변.
- 2026-08-07: **STAGE 0 전건 + STAGE 1 (1-3 제외) 완료** — 진산 "0·1단계 진행" 집행.
  훅 3종 배선 수리 + `.review-blocked` 3상태 + 시행시점 축 배선 + `enrichRelatedNodes` 승인/시행
  필터 봉합 + 마이그 0039(엣지 가드 3종) 작성·검증. 독립 리뷰 27건 전건 처분.
  ★집행 중 **분석 §3-B 긴급성 주장 철회**(실측: 현재 학습자 오노출 0건) — 분석·쉬운말·대시보드
  3곳 정정. 진행 **11/29**.
