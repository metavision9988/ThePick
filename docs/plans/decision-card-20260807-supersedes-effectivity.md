# 결정 카드 — 승계(SUPERSEDES) 은퇴 시점 ↔ 시행시점 창 정합

> 2026-08-07 · **상태 = ✅ (C) 채택 결재 완료 (진산 2026-08-07 "c 진행") → 집행 완료** · L3 (DB 트리거)
> 발단 = 결재 대시보드 §A #9 "권고대로 진행"(진산 2026-08-07) → **착수 전 실측에서 권고안의 결함 발견**
> RULE #5 준수: 아래는 사실 + 선택지 + 권고. 채택은 진산.
> **집행 산출** = `migrations/0045_block_premature_promotion.sql` + 시나리오 테스트 +
> 무결성 러너 계보 불변식 2종. 상세 = §7(집행 기록). production 적용은 **여전히 별도 게이트**.

---

## 0. 왜 이 카드가 생겼나 — 승인된 권고가 그대로는 틀렸다

대시보드 §A #9 의 권고는 **(a) 0042 WHEN 절에 "승계자가 발효된 경우에만 은퇴" 추가**였다.
착수 전 변형본을 만들어 실측했더니 **blackout 을 이중 노출로 바꿀 뿐**이었다.

### 실측 (전 마이그 적용 in-memory SQLite · 재현 스크립트 §6)

시나리오: `OLD`(현행, approved, valid_from 없음) ← `NEW`(개정본, approved, **valid_from = 미래**)
가 SUPERSEDES 로 승계.

| 트리거            | 오늘 (시행 전)             | 시행일 이후                                  |
| ----------------- | -------------------------- | -------------------------------------------- |
| **현행 0042**     | **(없음)** ← 서빙 blackout | `NEW` ✅                                     |
| **권고 (a) 적용** | `OLD` ✅                   | **`OLD`, `NEW` 동시** ← 구·신 규정 동시 노출 |

### 원인 (구조적)

0042 의 두 트리거는 **이벤트 구동**이다 — `AFTER INSERT ON knowledge_edges` / `ON status_transitions`.
(a) 처럼 "승계자가 발효됐을 때만 은퇴" 조건을 걸면, 승인 시점엔 미발효라 발화하지 않는다.
그리고 **시행일이 도래해도 아무 행도 바뀌지 않는다** — SQLite 에는 시간 기반 트리거가 없다.
즉 은퇴를 실행할 **이벤트가 영원히 오지 않는다.**

> **판단**: 학습 서비스에서 "근거가 사라짐"(blackout)과 "구·신 규정이 동시에 보임"(이중 노출) 중
> 후자가 더 나쁠 수 있다. 학습자는 둘 중 무엇이 현행인지 알 방법이 없고, 시스템은 그 상태를
> 정상으로 여긴다(무음). ⇒ **(a) 단독 채택은 권고 철회.**

---

## 1. 문제의 정확한 형태

**하나의 시점 개념을 두 장치가 각각 다르게 관리하고 있다.**

| 장치                                                   | "현행"의 정의           | 갱신 시점                  |
| ------------------------------------------------------ | ----------------------- | -------------------------- |
| `is_current_active` (ADR-013 Materialized Active View) | 승계당하지 않은 것      | **승인 이벤트**            |
| `valid_from`/`valid_until` (0041 + 2026-08-06 배선)    | 오늘이 유효기간 안인 것 | **시각 경과**(이벤트 없음) |

둘이 어긋나는 구간이 정확히 **"승인됨 + 아직 미시행"** 이다. 지금까지 이 구간이 존재하지 않았던 이유는
**미시행 개정을 승인하지 않는 관행**(현 데이터: 개정 노드 4건 전부 draft) 때문이지, 장치가 막아서가 아니다.

---

## 2. 선택지

### (A) 발효 스윕 — (a) + 시간 이벤트 공급

(a) 조건을 넣고, **시행일이 지난 승계자의 구본을 은퇴시키는 주기 작업**을 추가한다.

- 구현: 기존 cron 인프라(`ops.yml` / scheduled worker) + 스윕 SQL + 무결성 불변식
  ("승계자가 발효됐는데 구본이 아직 활성 = 위반")
- 장점: `is_current_active` 의미 유지. 서빙 경로 무변경(성능 무영향).
- 단점: **새 실패 지점**(스윕이 안 돌면 이중 노출이 조용히 지속) → 불변식 + 알림 필수.
  스윕은 `knowledge_nodes` UPDATE = Hard Limit 화이트리스트 확인 필요.
- 규모: 마이그 1 + 스윕 1 + 불변식 1 + 테스트 + 리뷰 = **M~L**

### (B) 서빙에서 계보 해석 — 플래그 대신 시점으로 판정

`is_current_active` 를 시점 판정의 권위에서 내리고, 서빙 CORE 가
"이 계보에서 오늘 유효한 판본"을 직접 고른다.

- 장점: 시간 이벤트 불필요(구조적으로 항상 정확). 스윕 실패 같은 새 실패 지점 없음.
- 단점: **단일 진실원 CORE 에 계보 JOIN 추가** → hot path 비용 증가(현 2.35ms 에서 더). ADR-013
  전제를 건드리므로 **ADR 개정 사안**. 4~5 소비자 전부 영향.
- 규모: **L~XL**

### (C) 정책 — 시행일 전 승격 금지 (코드 0줄) ★

**미시행 개정은 승인하지 않는다.** 시행일에 승격하면 그 시점이 곧 이벤트라 0042 가 정상 발화하고,
blackout 도 이중 노출도 발생하지 않는다.

- 장점: **비용 0**. 현 관행과 이미 일치(개정 4노드 전부 draft). 오늘 당장 위험이 사라진다.
  0041/시행시점 창은 **2차 방어선**으로 남아 제 역할을 한다.
- 단점: **사람 규율에 의존** — 잊으면 다시 열린다. 그래서 기계 가드 1개는 붙여야 한다:
  **"미시행 노드의 approved 전이 차단"** 트리거(= 승격 시점에 `valid_from > today` 면 ABORT).
  이건 (A)(B) 보다 훨씬 작다(트리거 1개).
- 규모: 마이그 1(트리거 1개) + 테스트 + 문서 = **S**
- ★주의: 개정 준비 작업(미리 만들어 두기)은 draft 로 계속 가능하다 — 막는 것은 **승격**뿐이다.

### (D) 아무것도 하지 않음 — 현행 유지

- 오늘 위험 없음(도달 조건 = 미시행 승격, 현재 0건). 다만 **다음에 누가 미시행 승격을 하면
  blackout 이 무음으로 발생**하고, 그때는 원인 추적이 어렵다.
- 채택 시 최소 의무: 무결성 러너에 "계보 공백" 불변식만이라도 추가(관측).

---

## 3. 권고

> **(C) + 최소 기계 가드** 를 권고한다. 이유:
>
> 1. **현 관행이 이미 (C)** 다 — 데이터가 그 증거(개정 4노드 전부 draft). 새 규율을 만드는 게 아니라
>    이미 하고 있는 것을 기계가 지키게 하는 것이다.
> 2. (A) 는 "이중 노출을 막기 위해 주기 작업을 도입하고, 그 주기 작업이 실패하면 다시 이중 노출"이라
>    **실패 모드를 옮길 뿐** 없애지 못한다. 알림·불변식까지 딸려 온다.
> 3. (B) 가 구조적으로는 가장 옳지만 ADR-013 개정 + hot path 비용 + 5 소비자 영향이라
>    **STAGE 2(원문 저장) 앞에 끼워 넣을 크기가 아니다.**
> 4. (C) 를 택해도 (B) 로 가는 길이 막히지 않는다 — 나중에 시점 기반 서빙이 필요해지면
>    그때 ADR 개정과 함께 하면 된다. **되돌릴 수 없는 선택이 아니다.**
>
> 즉 **지금은 가장 싼 가드로 위험을 닫고, 구조 개편은 필요해질 때 별도 결재**로.

### (C) 채택 시 할 일 (다음 세션)

1. 마이그 `0045_block_premature_promotion.sql` — `status_transitions` BEFORE INSERT 트리거:
   `to_status='approved'` 이고 대상 노드의 `valid_from > today` 면 ABORT (메시지에 "시행일 이후 승격" 안내)
2. 0039 헤더의 blackout 경고를 "정책 (C) 로 해소됨"으로 갱신
3. 게이트: 미시행 노드 승격 시도 → ABORT / 시행일 도래 후 승격 → 통과 + 0042 정상 발화 →
   서빙이 `NEW` 단독 (blackout·이중 노출 둘 다 0)
4. 무결성 러너 불변식 1개: "활성 계보에서 구·신 동시 활성 0" (사후 관측)

---

## 4. 이 결정이 막고 있는 것

- **0039 production 적용** — blackout 경로가 열린 채로 부활 차단까지 걸면 복구 수단이 줄어든다.
  본 카드 결재 후 함께 적용하는 것이 안전하다.
- **STAGE 2 백필** — `valid_from` 에 값이 들어가기 시작하는 순간 이 구간이 실재하게 된다.
  즉 **본 카드는 STAGE 2 의 선결**이다.

## 5. 결재란

```
[x] 채택안: (C) 정책+가드 (권고)            ← 진산 2026-08-07 "c 진행"
[x] (C) 채택 시 — 마이그 0045 작성·검증 착수 승인 (production 적용은 별도)
[x] 0039 production 적용을 본 결정 이후로 묶는 것에 동의
```

> 결재 근거 기록: 진산 발화 "c 진행 하고 .. 실행 체크리스트도 현행화 체크해줘" (2026-08-07).
> 승인 범위 = **SQL 작성 + 로컬 검증까지**. `wrangler --remote` production 적용은 0039 와 함께
> 별도 게이트(TR-0/0038 선례) — 결재 대시보드 §A 로 재상신.

## 6. 재현 방법 (주장 검증용)

```
node <scratch>/probe9b.mjs     # 본 세션 사용분 — 아래 요지로 재작성 가능
  1. migrations/ 전량을 in-memory SQLite 에 적용
  2. OLD(approved) ← NEW(approved, valid_from=미래) SUPERSEDES 삽입
  3. 서빙 CORE 를 (오늘 / 시행일 이후) 두 시점으로 실행
  4. 0042 원본 vs (a) 변형본 각각 비교
```

핵심 관찰: (a) 변형본은 **시행일 이후 `OLD,NEW` 를 동시 반환**한다 — 은퇴를 발화시킬 이벤트가 없기 때문.

---

## 7. 집행 기록 (2026-08-07, (C) 채택 후)

### 무엇을 만들었나

| 산출물                                                              | 내용                                                                                                                                    |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `migrations/0045_block_premature_promotion.sql`                     | 트리거 **6** = 승격 게이트 3(node/formula/constant) + 백필 게이트 3                                                                     |
| `apps/api/.../scenarios/migration-0045-premature-promotion.test.ts` | 시나리오 **22** (G-0045-1~8, 서빙 코어와 end-to-end 대조 포함)                                                                          |
| `packages/quality/src/production-audit.ts`                          | 계보 불변식 2 = `LINEAGE_DUAL_ACTIVE` · `LINEAGE_GAP` (+ 러너 리포트 배선) ⚠️리뷰 후 **3종**(`LINEAGE_LAPSE` 신설)·판정단위 성분화 = §8 |
| `migrations/0039_...sql`                                            | ★★ blackout 경고 → "(C)+0045 로 해소" 로 갱신 (**SQL 본문 불변**)                                                                       |

### 카드 §3 대비 — 확대 2건 (Silent Pivot 방지 명시)

카드 §3 문면은 "`valid_from > today` 면 ABORT" 였다. 실제 구현은 **둘을 더 잠갔다**:

1. **`valid_until` 대칭** — 이미 만료된 판본의 승격도 차단. 승인 즉시 창 밖 = 무음 미노출이라
   같은 불변식의 반대쪽이다.
2. **백필 게이트 [B]** — 카드는 "미시행 행을 승격하는 문"만 막는다. 그런데 "**이미 승인된 행에
   미래 시행일을 채우는 문**"이 하나 더 열려 있었고(0041 이 NULL→값 1회 백필을 허용), 카드 §4 가
   "이 결정은 STAGE 2 의 선결"이라고 지목한 이유가 바로 **STAGE 2 가 그 백필을 시작하기 때문**이다.

   > ⚠️ **정정 (2026-08-07, 5-페르소나 독립 리뷰 MAJOR — 3곳에 전파된 사실 오류)**
   >
   > 위 문장은 **틀렸다.** 역이식 **STAGE 2 = `source_quote`(원문 문장) 축**이고(체크리스트
   > STAGE 2 = 2-1~2-5 전부 원문 관련), `valid_from` 백필은 **Revision Watch Phase 2**
   > (0041 헤더: 미시행 4노드 LAW-022/023/053/INV-087)다. 서로 다른 두 로드맵의 "2"를 혼동했다.
   >
   > 귀결: [B]는 `UPDATE OF valid_from, valid_until` 에서만 발화하므로 **역이식 STAGE 2 를
   > 게이팅하지 않는다.** 그리고 진짜 백필 대상 4노드는 전부 draft 라 [B]를 그대로 통과한다
   > (테스트 G-0045-6c 가 이를 고정).
   >
   > **[B] 자체의 근거는 유효하다** — 다만 정확한 근거는 "STAGE 2 가 그 문을 연다"가 아니라
   > **"임의 시점의 `valid_*` 백필로부터 서빙 중인 행을 보호한다"** 이다. 이 카드가 진산에게
   > 제시한 선결 관계(“#9 는 STAGE 2 의 선결”)도 같은 혼동의 산물이며, 실제로는
   > **Revision Watch Phase 2 백필의 선결**이다. 체크리스트·대시보드에도 같은 정정을 반영했다.
   > [A] 만 넣으면 카드가 경고한 그 문이 열린 채 남는다.

⇒ 결정 자체((C) 정책 + 최소 가드)는 불변이며, **가드의 적용 범위가 카드 문면보다 넓다.**
축소가 아니라 확대이고 되돌릴 수 있으므로 집행했으나, 진산이 원하면 [B] 만 떼어낼 수 있다
(트리거 3개 DROP — 카드 §3 문면 그대로의 최소본으로 축소 가능).

### 검증

- 시나리오 22 PASS · **변이 실측 8/8 red**(승격게이트 삭제 / COALESCE 3값붕괴 제거 / KST 보정 제거 /
  백필게이트 삭제 / `date()` 정규화 제거 / `valid_until` 절 제거 / active 조건 제거 / status 조건 무력화 —
  전부 검출. 복원 sha256 동일)
- api **830**(기준선 808·회귀 0) · quality **100**(86) · turbo 17/17 · E2E 26/26 · 훅 38/38 ·
  typecheck·lint·G-1 green
- ★**production 무접촉** — 0045·0039 미적용, 쓰기 쿼리 0

### 집행 중 발견 (정직 기록)

1. **기존 테스트 2건이 red 로 전환** — `study/routes.test.ts` 의 시행시점 회귀 테스트가
   _"approved 노드에 미래 valid_from 백필"_ 로 픽스처를 만들고 있었다. 그게 바로 [B] 가 막는 상태다.
   → 테스트 의도(2차 방어선 = 서빙이 독립적으로 감추는가)는 유효하므로, 해당 테스트 안에서만
   [B] 트리거를 내리고(존재 확인 후 drop) 검증하도록 고쳤다. **가드가 즉시 실제 위반을 잡은 사례.**
2. **잔여 위험의 방어선이 자동이 아니다** — `LINEAGE_GAP` 을 계산하는 무결성 러너는
   **어느 CI 워크플로에도 배선돼 있지 않다**(전수 확인). "관측한다"는 서술이 "감시한다"로 읽히지
   않도록 마이그 헤더·plan 에 명시했다. 러너 cron 배선은 별건.
3. **체크리스트 카운터가 틀려 있었다** — "11/29" 로 적혀 있었으나 실제 체크는 8. 손 집계 드리프트.
   1-5 신설 반영해 **9/30** 으로 정정(체크리스트 헤더에 자백 기록).

### 잔여 (진산)

- **0039 + 0045 + 0046 production 적용** = 대시보드 §A **#10** (묶음 적용, `wrangler --remote` 인증 필요)

---

## 8. 독립 리뷰 처분 (2026-08-07, 5-페르소나 병렬)

> 보고서 정본 = `.claude/reviews/review-20260807-5persona-0045.md`
> 위 §7 "집행 완료" 는 **독립 리뷰 전** 문면이었다 — 리뷰가 CRITICAL 을 냈으므로 아래가 최종이다.

### 무엇이 나왔나

5 렌즈(정합성 / 아키텍처 / 학습자노출·보안 / 요구사항대조 / 기술부채)를 서로 모르는 채 병렬 실행.
raw CRITICAL 3 · MAJOR **24** · MINOR 30 (렌즈별 합산 실측 — 초판 "25" 는 손 집계 오류, 2R 지적으로 정정). **서로 다른 3개 렌즈가 독립적으로 같은 구멍에 수렴**했고,
메인 세션이 전 마이그 하네스에서 직접 재현해 확정했다.

| 진앙                                                                                                     | 수렴     | 처분                                                                                                         |
| :------------------------------------------------------------------------------------------------------- | :------- | :----------------------------------------------------------------------------------------------------------- |
| **세 번째 문** — 전이 선행 우회 → 0042[1] 이 도출 status 만 보고 구본 은퇴 → blackout(0039 로 복구 불가) | 렌즈 ①②③ | **마이그 0046 신설** (0042[1] 재생성 + INSERT 게이트 3) · 시나리오 10 · 변이 3/3 red                         |
| **`LINEAGE_GAP` 판정축** — 엣지 없는 만료를 **영영 못 봄**(과소) + 다중홉 **오탐**(과대)                 | 렌즈 ①②③ | 판정축을 `is_current_active`→**서빙 여부**로 교정 + 터미널 승계자 규칙 + **`LINEAGE_LAPSE`**(노드 전수) 신설 |
| **미러 핀이 shape 정규식뿐** — `valid_until` 방향 drift 침묵                                             | 렌즈 ②⑤  | `G-0045-2b` 에 서빙 대조 추가 + **경고를 의존 상류**(`approved-nodes-sql.ts`)로 이동                         |
| **`migrations-v2` 원장 부재** — 2호가 결함만 상속                                                        | 렌즈 ②⑤  | README 에 **미추출 사유 원장** 등재(보류 사유 = 종목 정책 미결, RULE #5)                                     |
| **문서가 리뷰 전에 "완료"를 선언**                                                                       | 렌즈 ④   | 본 절로 하향·정정. plan 게이트는 리뷰 종료 후 같은 커밋에서 닫음                                             |
| **STAGE 2 ↔ Revision Watch Phase 2 혼동** — [B] 근거가 사실이 아님                                       | 렌즈 ④   | §7 확대 근거에 정정 블록 삽입(위) + 체크리스트·대시보드 동시 정정                                            |

### 카드 §3 대비 — 확대는 이제 **3건**

앞서 기록한 2건(`valid_until` 대칭 · 백필 게이트 [B])에 **[C] 세 번째 문 봉쇄(마이그 0046)** 가 더해졌다.
결재 문면("트리거 1개·규모 S")과의 차이를 정직하게 적으면: **트리거 1 → 10** 이다.

- **왜 멈추지 않고 진행했나**: 확대분이 (a)새 정책이 아니라 **이미 결재된 (C) 불변식의 구멍 메우기**이고
  (b)production 미적용·되돌릴 수 있으며 (c)남겨두면 **CRITICAL 이 열린 채 커밋**되기 때문이다.
  사용자 지시("CRITICAL/MAJOR 수정하고 커밋")의 범위 안으로 판단했다.
- **RULE #5 정합**: 그러나 "얼마나 넓게 잠글 것인가"는 정책 선택이므로 **사후 거부권 대상**으로
  대시보드 §A #10 에 명시했다. [B]만 떼는 축소(트리거 3 DROP)는 그대로 열려 있다. ★**0046 롤백은 DROP 이 아니다** — [C-2] 3개는 DROP 하되 [C-1]·[C-3]은 **0042 원본 블록을 재실행**해야 한다(그냥 DROP 하면 승계 은퇴가 통째로 사라져 구·신 영구 동시 노출). 2R 리뷰가 초판의 "DROP 4" 표기를 오류로 지적했다.

### 처분하지 않고 원장에 남긴 것 (비차단 · 별건)

| #   | 무엇                                                                                           | 왜 이번에 안 했나                                                                                                |
| :-- | :--------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------- |
| L-1 | **formulas/constants 은퇴 경로**(0014 무조건 deactivate) — 0045/0046 사정거리 밖               | 은퇴 semantics 변경 = 정책 결정. 0042 헤더가 예약한 **0043 슬롯**. 그때까지 `superseded_by` 사용 금지(정책 불변) |
| L-2 | **엣지 `is_active` 0→1 플립** → 구·신 동시 노출 (기계 가드 0)                                  | 0039 가 허용하는 조작. AFTER UPDATE 트리거 신설 = 별건 슬롯. 관측은 `LINEAGE_DUAL_ACTIVE` 가 함                  |
| L-3 | **승격 파일 부분 적용 / `INSERT OR IGNORE` 멱등성 파괴**                                       | 트리거로 막을 종류가 아님 — 런북 pre-flight SELECT 의무화(별건)                                                  |
| L-4 | **러너 CI 미배선** (`LINEAGE_LAPSE` 가 "돌리면 보인다" 수준)                                   | 선결 = 게이트 신호 분리(선재 고아 2로 상시 FAIL). 배선만 하면 1개월 내 무시되는 잡이 됨                          |
| L-5 | **러너 `parseDump` 필수컬럼 하드의존** — 0041 미적용 DB(2호 초기·staging·백업)에서 게이트 불가 | 도구 범위 축소가 실재. `--no-lineage-axis "사유"` opt-out 설계 = 별건                                            |
| L-6 | 미러 사본 8벌 → SQL VIEW 로 접기 / KST 상수 3중 구현 통합                                      | 구조 개선(비용 M). 창 규약 개정이 실제로 필요해질 때 함께                                                        |
