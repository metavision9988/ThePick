# migrations-v2 — exam-generic 스키마 정본 (프레임워크 T5)

> **근거**: 2026-07-04 멀티트랙 R5 결재 "프레임워크 정본 = packages/(코드) + docs/playbooks/\_template/(프로세스) + **migrations-v2/(스키마)**" + Revision Watch plan §9 Q7 위임 결재(2026-07-07).
> **역할**: 종목(exam) 무관 **재사용 스키마 템플릿**의 단일 보관처. 신규 종목(2호 전기기사 등) D1 초기화 시 여기서 인스턴스화한다.
> **관계**: `migrations/`(루트) = **1호 손해평가사 인스턴스 적용 이력**(0001~00NN, production 순차 적용 — 불변 원장). migrations-v2 는 그 대체가 아니라 **exam-generic 추출본**이다. 1호에 적용할 SQL 은 여전히 `migrations/` 슬롯으로 작성(선례: 0041/0042).

## 규약

1. **디렉토리 = 모듈 단위** (`revision-watch/`, `core/`(예약), …). 모듈 내 파일은 `0001_*.sql` 순번.
2. **exam-generic 조건**: 종목 하드코딩 금지(Hard Rule 16/17 — exam_id 는 컬럼/파라미터로만). 테이블·트리거 구조는 전 종목 동일 스키마 전제(종목별 D1 분리 배포 모델).
3. **인스턴스화**: 신규 종목 D1 에는 모듈 순서대로 그대로 적용. 1호(기존 production)에는 루트 `migrations/` 슬롯으로 **동치 SQL** 작성 후 적용 — 양쪽 드리프트는 동치 주석(원본 참조)으로 추적.
4. 적용 = 인간 인증 게이트(wrangler --remote). 이 디렉토리는 **정본 텍스트**이지 적용 이력이 아니다.

## 모듈

| 모듈              | 내용                                                             | 1호 인스턴스                        |
| :---------------- | :--------------------------------------------------------------- | :---------------------------------- |
| `revision-watch/` | 시행시점(effective_date) 축 + SUPERSEDES draft 게이트 2단 트리거 | `migrations/0041`·`0042` (선작성본) |

### ⚠️ 미추출 원장 — 승격 게이트 (2026-08-07)

`migrations/0045`(승격·백필 게이트 6) + `migrations/0046`(세 번째 문 봉쇄 4)는 **아직 v2 로 추출하지
않았다.** 누락이 아니라 **보류이며, 그 사유를 여기 남긴다** — 5-페르소나 독립 리뷰가 "기록이 아무 데도
없는 상태"를 MAJOR 로 지적했고(2개 렌즈 수렴), 처분이 이 항목이다.

- **왜 보류하나**: 0045/0046 이 강제하는 것은 스키마가 아니라 **정책**("시행일 전에는 승격하지 않는다")
  이다. 1호(손해평가사)는 관보 시행일 이후에만 현행이 되는 도메인이라 이 정책이 자명하지만,
  2호(전기기사) 등은 **개정 예고분을 미리 노출하는 학습 문화**가 실재할 수 있어 같은 정책이
  자동으로 옳다고 단정할 수 없다. 종목 도메인 확정 전에 v2 로 올리면 1호 정책을 프레임워크
  정본으로 승격시키는 셈이 된다(G-1 RULE #5 — 종목 정책은 인간 결정).
- **SQL 자체는 exam-generic 이다**: `exam_id` 리터럴 0, 테이블 3종은 전 종목 공통 스키마 전제.
  즉 채택이 결정되면 **거의 그대로 복사**할 수 있다(KST `+9 hours` 고정은 국내 자격시험 전제).
- **2호 착수 시 반드시 결정할 것**: 채택하면 `revision-watch/0003_*`·`0004_*` 로 추출,
  불채택하면 그 사유를 이 표에 남긴다. **아무 결정 없이 2호 D1 을 0001·0002 만으로 인스턴스화하면
  1호가 방금 막은 blackout 을 신품으로 물려받는다** — KEC 는 연 1~3회 개정이라 도달 확률이 1호보다 높다.
