# Step 19 Pass 3+4 — Advocate + Contract

## 메타

- 리뷰 방식: 독립 에이전트 (code-reviewer, 메인 컨텍스트 무관 단독 판단)
- 리뷰 범위: 변경 파일 11개 + 연관 파일 8개 (총 19개)
  - 변경: `migrations/0017_engine_telemetry.sql`, `apps/api/src/db/schema.ts`, `apps/api/src/index.ts`, `apps/api/src/telemetry/{types,admin-token,write-helper,routes}.ts`, `apps/api/src/telemetry/__tests__/routes.test.ts`, `apps/admin-web/src/{pages/telemetry/index.astro, components/TelemetryDashboard.tsx, types/telemetry.ts, env.d.ts}`, `apps/batch/src/{cost-meter,pipeline,recover}.ts`, `scripts/verify-engine-contracts.ts`, `docs/observability/master-dashboard.md`, `docs/quality/master-test-checklist.md`, `docs/plans/engine-hardening/step19-observability.plan.md`
  - 연관: `apps/api/src/middleware/cache-policy.ts`, `packages/shared/src/constants/exam-ids.ts`, `packages/shared/src/exam-adapter.ts`, `packages/shared/src/logger.ts`, `apps/admin-web/astro.config.mjs`, `docs/plans/engine-hardening/ROADMAP.md` §8, `migrations/0014_phase05_critical_hardening.sql` (트리거 패턴 비교), `apps/batch/__tests__/cost-meter.test.ts`
- 작성일: 2026-05-01 15:44 KST

---

## Pass 3 (Advocate) — Cross-Cutting UX + 보안

### 결과 요약

✅ 14건 PASS / 🔴 0건 / 🟠 1건 / N/A 1건

### 확인 (증거 기반 — 14건 PASS)

1. **TelemetryDashboard.tsx 4-상태 처리 완전성 (UX)** — `apps/admin-web/src/components/TelemetryDashboard.tsx:24-29` `FetchState.status: 'idle' | 'loading' | 'success' | 'unauthorized' | 'error'` 5상태 (요청보다 1개 더 — `idle`까지) 모두 분기 처리. 401 → `clearToken()` + `setToken(null)` + `unauthorized` (line 236-246), 비-OK → `error` 메시지 노출 (line 247-255), 에러 메시지 UI (line 365-378), 로딩 중 버튼 disable (line 331). `no_data` 상태는 GaugeCard 측에서 `'데이터 없음'` 한국어 안내 (line 152). 4상태 모두 PASS.

2. **localStorage 토큰 보안 모델 (security)** — `TelemetryDashboard.tsx:20` `TOKEN_STORAGE_KEY='admin_api_token'` + same-origin 정책으로 admin-web 외부 도메인은 접근 불가. XSS 공격 벡터로 노출 가능하나 admin-web 측에 위험 DOM 직접 할당 0건 (`grep` 검증 0 hits) + JSON.stringify content는 `<pre>` 텍스트로만 렌더 (line 143). React 자동 escape PASS. Phase 1 임시 방어선이라는 사실은 master-dashboard.md §4 Phase 2 전환 단계에서 명시.

3. **timing-safe equal 구현 + length-leak 방어 (security)** — `apps/api/src/telemetry/admin-token.ts:28-35` length 다르면 즉시 false 반환은 token length 정보 누출 가능하나 line 26-27 주석에서 명시 인지. `MIN_TOKEN_LENGTH = 16` (line 21) + line 46 `expected.length < 16` 시 401 강제로 length leak 상한 ≥16자로 한정. 실제 비교는 XOR loop (line 31-34)로 timing-safe. ADR 수준 trade-off 명시 정합 (Phase 2 Cloudflare Access 도입 시 본 미들웨어 제거).

4. **ADMIN_API_TOKEN 환경변수 misconfig 401 마스크 (security)** — `admin-token.ts:46-48` 환경변수 부재/짧음 시 401 응답 + `routes.test.ts:106-121` 두 케이스 (env 부재 + length<16) 모두 401 검증. information-leak 차단 PASS. routes.test 명시 검증.

5. **Zod 입력 검증 (security)** — `apps/api/src/telemetry/types.ts:45-57` `telemetryEventPayloadSchema` 4 검증 (examId refine + gaugeName enum + finite metricValue + min1 metric_value/json refine) 모두 422 응답. `routes.test.ts:124-165` 5건 검증 (빈 body / 미지 examId / 미지 gauge / 둘 다 부재 / NaN). PASS.

6. **assertValidExamId 다층 방어 (security + Hard Rule 17)** — `apps/api/src/telemetry/write-helper.ts:47` `assertValidExamId(payload.examId)` Zod 통과 후 brand type 진입 게이트로 두 번째 차단. `routes.test.ts:131-138` `examId='unknown-exam'` 시 422 검증. ALL_EXAM_ID_VALUES allowlist (`packages/shared/src/constants/exam-ids.ts:23`) 단일 출처 PASS.

7. **SQL injection 차단 (security)** — `apps/api/src/telemetry/{write-helper.ts:71-79, routes.ts:159-167, 187-205}` 모든 D1 쿼리 `prepare(...).bind(...)` prepared statement 패턴. 사용자 입력 직접 문자열 결합 0건. `routes.ts:147-149` `gauge` 파라미터도 `ENGINE_TELEMETRY_GAUGES.includes()` allowlist 검증 후 prepare bind. PASS.

8. **engine_telemetry append-only 트리거 (security)** — `migrations/0017_engine_telemetry.sql:101-113` `prevent_engine_telemetry_update` + `prevent_engine_telemetry_delete` 트리거 + `routes.test.ts:298-326` UPDATE/DELETE 시도 → `RAISE(ABORT)` 검증. 데이터 위조 방어 + Phase 2 GC 정책 명시 (line 105, 112). PASS.

9. **CHECK 제약 + Zod refine 동기 (data integrity)** — `0017_engine_telemetry.sql:66` `CHECK (metric_value IS NOT NULL OR metric_json IS NOT NULL)` + `types.ts:54-57` Zod refine 동일 메시지로 422 응답. `routes.test.ts:149-155` 검증. 빈 telemetry 이벤트 0건 PASS.

10. **metricJson 직렬화 + 64KB 한도 (security + perf)** — `write-helper.ts:51-64` JSON.stringify 실패 시 throw + 64KB 초과 시 throw. silent drop 차단 + Workers CPU 시간 + D1 row size 방어. CRITICAL 빈 catch 0건. PASS.

11. **Cache-Control 보안 floor (security)** — `apps/api/src/middleware/cache-policy.ts:67` 모든 매칭 실패 경로 (`/api/telemetry/*` 포함) `Cache-Control: no-store` 강제. `apps/api/src/index.ts:81` `app.use('*', cachePolicyMiddleware())` 첫 미들웨어 등록으로 어떤 early-return 에서도 헤더 적용 보장. shared cache 오염 0건 PASS.

12. **Phase 1 임시 인증 사용자 안내 (UX)** — `TelemetryDashboard.tsx:175-178` TokenForm 안내 "ADMIN_API_TOKEN 환경변수와 일치해야 합니다 (Phase 1 임시 인증). Phase 2 Cloudflare Access 도입 후 제거됩니다." 명시. master-dashboard.md §4 운영 가이드 1~4단계 명시. PASS.

13. **데스크톱 키보드 접근성 (a11y, single-user 진산님)** — `TelemetryDashboard.tsx:194-211` button + input 표준 HTML 요소 (tabindex 자동 주입), Logout/Refresh button 요소 사용. 진산님 admin-web 데스크톱 사용 가정 (mobile 80% 정책은 사용자용 web PWA 한정) — N/A 처리 일부 인정. 색상 대비: 본문 `#0f172a` (slate-900) on `#f1f5f9` (slate-100) 대비비 ~14.5:1 (WCAG AAA). gauge dot/badge: `#16a34a` (green-600) on white = ~4.5:1 (AA). PASS.

14. **404 vs 200 vs 422 vs 503 응답 코드 의미론 (UX)** — `routes.ts:148-149` 미지 게이지 → 404 / `:127` Zod 실패 → 422 / `:135-140, :223-225` D1 5xx → 503 + `Retry-After: 5` 헤더. HTTP 표준 정합 + UX 측면 클라이언트가 적절한 재시도 가능 PASS.

### 🟠 MAJOR (1건)

#### MAJOR-AD-1 — `/api/telemetry/*` CORS 미설정으로 admin-web ↔ api 크로스-오리진 호출 차단 (런타임 fetch 실패)

**증거:**

- `apps/api/src/index.ts:75-76`: CORS 미들웨어가 `/api/auth/*` + `/api/progress/*` 두 prefix에만 적용. `/api/telemetry/*`는 CORS 미들웨어 미등록.
- `apps/api/src/index.ts:21-26` `CORS_ALLOWED_ORIGINS`: `http://localhost:4321`, `http://127.0.0.1:4321`, `https://thepick-staging.pages.dev`, `https://thepick.app` — admin-web 포트 `4322` 부재.
- `apps/admin-web/astro.config.mjs:7` `server: { port: 4322 }` (admin-web 포트) ≠ 4321 (web PWA 포트).
- `TelemetryDashboard.tsx:22` `DEFAULT_API_BASE = 'http://localhost:8787'` — admin-web (`localhost:4322`) 에서 api (`localhost:8787`) 로 cross-origin fetch.
- `TelemetryDashboard.tsx:233-235` `fetch(...{headers: { 'X-Admin-Token': token }})` — `X-Admin-Token` 은 simple header 가 아니므로 브라우저가 OPTIONS preflight 의무 발생. CORS 미설정 시 브라우저는 `Access-Control-Allow-Headers: X-Admin-Token` 부재로 실제 요청 차단.

**영향:**

- 런타임 환경에서 admin-web `/telemetry` 페이지 fetch 100% 실패 (브라우저 콘솔에 `CORS preflight failed` 에러). routes.test.ts 는 `app.request()` 직접 호출이라 CORS 우회 — 통과해도 브라우저 작동 안 함.
- `TelemetryDashboard.tsx:248-254` 비-OK 응답을 `error` 상태로 표시하나 CORS 차단은 fetch 자체가 throw → catch (line 263-269) 진입 → "TypeError: Failed to fetch" 같은 일반 에러 노출. UX 진단 어려움.

**판정 근거:**

- Plan §0.A.1 "admin-web 인증 — Cloudflare Access 환경 구성 불가능 → admin token 임시 게이트" 는 인증 부분만 다루고 CORS 는 미언급.
- Plan §5.3 "401 응답 시 토큰 무효화" 는 auth 흐름만 — CORS 차단은 401 도 아니고 fetch reject.
- 본 step "테이블/인터페이스/UI 셸" 활성 + "실제 wire-up 은 BATCH-1 진입 직전" (master-dashboard §6) 라 "당장 페이지 동작" 우선순위 낮으나, master-dashboard.md §4 운영 가이드 "1. 진산님이 ADMIN_API_TOKEN 등록 → 2. admin-web 첫 진입 시 토큰 입력" 시나리오에서 첫 진입에서 막힘 = 운영 가이드 거짓.

**수정안 (즉시 흡수 권고):**

```typescript
// apps/api/src/index.ts (CORS allowlist 4322 추가 + telemetry prefix CORS 등록)
const CORS_ALLOWED_ORIGINS: readonly string[] = [
  'http://localhost:4321',
  'http://localhost:4322', // admin-web (Step 19)
  'http://127.0.0.1:4321',
  'http://127.0.0.1:4322', // admin-web (Step 19)
  'https://thepick-staging.pages.dev',
  'https://thepick-admin.pages.dev', // admin-web 별도 도메인 (Phase 2 후보)
  'https://thepick.app',
];

function buildTelemetryCorsOptions() {
  const base = buildCorsOptions();
  return {
    ...base,
    allowHeaders: ['Content-Type', 'X-Admin-Token'], // X-Admin-Token preflight 허용
  };
}
app.use('/api/telemetry/*', cors(buildTelemetryCorsOptions()));
```

**우회 가능성 (진산님 자율 판단 영역):** master-dashboard.md §4 운영 가이드의 "Workers Secret 등록 → admin-web 토큰 입력" 흐름을 BATCH-1 진입 시점까지 미사용으로 명시 deferred 한다면 본 MAJOR 는 "다음 step 흡수"로 이월 가능. 단 본 step "Phase 1 종료 게이트" 의무가 있다면 즉시 수정 권고.

### 반론 — "이게 깨질 수 있는 시나리오"

**시나리오 R-3-A:** admin_api_token 길이가 16자 ≤ 입력값 < 실제 token 길이 인 경우, `timingSafeEqual` 의 `if (a.length !== b.length) return false` 조기 종료로 length leak. 공격자는 길이만 학습 — 16~∞ 범위는 narrow down 가능. **방어:** `MIN_TOKEN_LENGTH=16` 강제 + 공격자가 알아낼 수 있는 정보는 ≥16 이라는 사실 + 길이가 16이라면 어차피 brute-force 어려운 키스페이스. Phase 2 Cloudflare Access 도입 시 자체 제거. 인지 + 방어 설계 PASS.

**시나리오 R-3-B:** TelemetryDashboard 가 30초 마다 `setInterval` 폴링 (`TelemetryDashboard.tsx:276`). 진산님이 Tab 백그라운드 두면 브라우저 timer throttle (1분/회 정도) 로 변동. `clearInterval` cleanup (line 277) 정상이나, 연속 401 발생 시 `clearToken()` + `setToken(null)` 후 useEffect cleanup → 재등록 시 token null 분기 (line 274) 로 fetch 중단. 토큰 무효화 후 폼 재표시까지 **다음 폴링까지 30초 무음 가능** — UX 큰 문제 아님. PASS.

**시나리오 R-3-C:** Workers Free 티어 CPU 50ms 한도 + dashboard 라우트 `routes.ts:185-215` 에서 8 게이지 × 2 쿼리 = 16 sequential D1 round-trip. 각 D1 prepared 호출 ~3-5ms 가정 시 48~80ms — **CPU 한도 임계 근접**. 주석 `routes.ts:184` "round-trip 1회" 는 misleading (실제 16 round-trip). 진산님 paid plan 사용 시 30s CPU = 충분, free tier 사용 시 timeout 가능. **방어:** Pass 2 영역이라 본 Pass 에서는 인지만, 후속 PR `UNION ALL 단일 쿼리` 권고. 본 step 영향도 낮음 (admin-web 사용자 1명).

### N/A (1건)

- **모바일 80% 접근성 (CLAUDE.md i18n + a11y)** — admin-web 은 진산님 데스크톱 단일 사용자 가정 (master-dashboard.md §4 정합). 웹 PWA 사용자용 a11y 정책 (44px 터치 타겟 등) 본 step 미적용. Plan §5.2 "admin-web 사용자 = 진산님 단일" 명시. N/A 인정.

---

## Pass 4 (Contract) — 기획 대조 / Silent Pivot 탐지

### 결과 요약

✅ 16건 PASS / 🔴 0건 / 🟠 1건 / N/A 1건

### 확인 (증거 기반 — 16건 PASS)

1. **engine_telemetry 8 게이지 enum 일치 (Hard Rules + 데이터 모델)** — `migrations/0017_engine_telemetry.sql:56-65` CHECK 제약 8개 / `apps/api/src/telemetry/types.ts:14-23` `ENGINE_TELEMETRY_GAUGES` 8개 / `apps/admin-web/src/types/telemetry.ts:9-18` mirror 8개 / `apps/api/src/db/schema.ts:587-596` Drizzle enum 8개 / `routes.test.ts:170-178` ALL_GAUGES 8개. 5 위치 모두 동일 순서 + 동일 값. plan §3.1 line 107-110 정합. PASS.

2. **마이그레이션 카운트 16 → 17 갱신 (MAJOR-A1 흡수)** — `scripts/verify-engine-contracts.ts:347, 351` `// Step 19 engine_telemetry 도입 = 17` + `const required = 17` / `docs/quality/master-test-checklist.md:193` `D1 마이그레이션 파일 카운트 17 / 17 / ✅`. 2 파일 동시 갱신 의무 (verify-engine-contracts.ts:348 명시) PASS. 실제 디렉토리 `migrations/` 0001~0017 17 파일 (`ls migrations/` 확인) PASS.

3. **모노레포 합계 909 → 937 갱신** — `master-test-checklist.md:57-59` `@thepick/api 199 → 227 (+28 telemetry)` + `모노레포 합계 909 → 937`. plan §10.3 "@thepick/api 199 → 199+신규 / 909 합계 → 909+신규" 정합. PASS (신규 28 testcase은 routes.test.ts 본문 분석으로 8 gauge × describe 블록 + admin-token 4 + Zod 5 + dashboard 3 + append-only 2 ≈ 28 일치).

4. **Hard Rule 15 (분기 금지) 정합** — `0017_engine_telemetry.sql` 본문 시험 분기 0건 (단일 fact table 구조). `apps/api/src/telemetry/*.ts` 4 파일 모두 `if (examId === ...)` 0건 (`grep -n "if.*examId" apps/api/src/telemetry/` empty). plan §9 정합. PASS.

5. **Hard Rule 16 (시그니처 examId) 정합** — `apps/api/src/telemetry/types.ts:47` `examId: z.string().refine(isValidExamId)` payload 첫 키. `write-helper.ts:42-47` `writeTelemetryEvent(payload, deps)` payload 진입 시 `assertValidExamId(payload.examId)` 첫 게이트. INSERT prepared statement (`write-helper.ts:73-78`) `exam_id` NOT NULL 컬럼 첫 인자 (id 다음). plan §9 line 252 + master-dashboard.md §1 NOT NULL 정합. PASS.

6. **Hard Rule 17 (EXAM_IDS 리터럴 단일 선언) 정합** — `apps/api/src/telemetry/*.ts` + `apps/admin-web/src/{components,types,pages}/**/*` 전체 파일에서 `'son-hae-pyeong-ga-sa'` literal 0건 (`grep -rn "'son-hae-pyeong-ga-sa'" apps/api/src/telemetry apps/admin-web/src` empty). 사용처는 `routes.test.ts:17, 142+` `EXAM_IDS.SON_HAE_PYEONG_GA_SA` 경유 (test fixture 예외 인정 — Rule 17 본문 명시). `verify-engine-contracts.ts:274-285` Hard Rule 17 검증 PASS. PASS.

7. **append-only 트리거 패턴 0014 정합** — `0017_engine_telemetry.sql:101-113` `prevent_engine_telemetry_update` + `_delete` 트리거 패턴이 0014 `prevent_*_update` 패턴 (knowledge_nodes/formulas/constants 등) 동일 RAISE(ABORT) 패턴 정합. master-dashboard.md §0 "append-only 보증" + plan §0.A.2 정합. PASS.

8. **Phase 1 = 7 게이지 / Phase 2 = 8 게이지 단계화 (Reality Anchor 정합)** — `apps/api/src/telemetry/types.ts:28-38` `PHASE_1_GAUGES` 7개 + `PHASE_2_GAUGES` 1개 (`learning_slo`). `routes.ts:109-114` `determinePhase` 함수 + `routes.test.ts:271-280` `learning_slo phase=2 + 나머지 phase=1` 검증. `TelemetryDashboard.tsx:62` `isPlaceholder = phase === 2` + line 152 "Phase 2 활성 예정" 표시. master-dashboard.md §1 + plan §0.A.3 + §3.2 정합. PASS.

9. **POST/GET 응답 코드 + 인증 정책 plan 정합** — plan §4.1-4.3: POST 응답 `{id, recordedAt}` + Phase 1 admin token / `routes.ts:122-142` POST `201 + TelemetryEvent` (id/recordedAt 포함 더 상세 응답 — superset OK) / `:145-177` GET gauge timeline / `:180-227` GET dashboard. 모두 `requireAdminToken` 미들웨어 (line 119) 적용. plan §5.3 "401 응답 시 토큰 무효화" → TelemetryDashboard.tsx:236-246 정합. PASS.

10. **MINOR-3A 흡수 — cost-meter logger** — `apps/batch/src/cost-meter.ts:37` `costMeterLog = createLogger(...).child(...)` + line 424 `costMeterLog.warn` (SOFT_WARN), line 437 `costMeterLog.warn` (HARD_THROTTLE), line 450 `costMeterLog.error` (KILL_SWITCH) — 3건 console.\* → logger 교체. `verify-engine-contracts.ts:289-305` `cost-meter.ts` paths 추가. plan §7.1 + master-test-checklist.md:208 "4 파일" 정합. PASS.

11. **MINOR-A1 흡수 — pipeline/recover logger.child 패턴** — `pipeline.ts:451` `const log = pipelineLog.child({ batchRunId, examId })` + `recover.ts:164` 동일 패턴. plan §7.2 "module 진입점에서 1회 생성 후 재사용" 정합. master-test-checklist.md:82 Logger 인터페이스 계약 PASS 정합.

12. **CHECK 제약 (metric_value OR metric_json 최소 1개) 정합** — `0017_engine_telemetry.sql:66` `CHECK (metric_value IS NOT NULL OR metric_json IS NOT NULL)` ↔ `types.ts:54-57` Zod refine 동일 메시지 ↔ `routes.test.ts:149-155` 422 검증. 3 위치 동기 PASS.

13. **인덱스 3종 + WHERE batch_run_id IS NOT NULL 부분 인덱스 정합** — `0017_engine_telemetry.sql:74-84` `idx_engine_telemetry_gauge_recorded` (gauge_name, recorded_at DESC) + `idx_engine_telemetry_exam_gauge` (exam_id, gauge_name, recorded_at DESC) + `idx_engine_telemetry_batch_run` (batch_run_id WHERE NOT NULL). plan §3.1 line 114-122 그대로 정합. Drizzle `schema.ts:614-625` 동일 인덱스 3개 PASS.

14. **메모리 정합 — `project_engine_observability` 8 게이지 + Cloudflare 단일 벤더** — master-dashboard.md §0 "Cost = $0/m 추가 (D1 + Pages + Workers 무료)" + "외부 SaaS 0건" 정합. 8 게이지 enum 모두 메모리 명시 게이지 (BATCH 진척/Cost/D1 SLO/Graph 무결성/품질 게이트/Formula 정확도/Reviewer 큐/학습 SLO) 일치. PASS.

15. **메모리 정합 — `feedback_review_filename_pattern` review-\* prefix** — 본 파일 경로 `.claude/reviews/step19-pass34-20260501-154451.md` 는 `step19-` prefix 사용 (review-gate.sh hook 인식 정합 위해 통합 인덱스에서 `review-*-step19.md` 별도 작성 의무 plan §1 line 27 명시). 본 Pass 단독 파일은 plan §"4-Pass 영속화" 대상이라 prefix 정합. 통합 인덱스 별도 의무는 commit 7 단계. PASS (인지).

16. **routes.test.ts 검증 8 게이지 라운드트립** — `routes.test.ts:169-178` `ALL_GAUGES` 8개 + `it.each(ALL_GAUGES)` 패턴으로 `gauge=batch_progress`...`gauge=learning_slo` 8 케이스 INSERT/응답 검증. plan §10.2 "8 게이지 INSERT → SELECT 라운드트립 (1~7 + Phase 2 placeholder)" 정합. PASS.

### 🟠 MAJOR (1건)

#### MAJOR-CT-1 — MINOR-4A "logger.ts excludeExactPaths 추가" plan 진술 미이행 (Silent Pivot 후보)

**증거:**

- plan §12 line 317 (handoff-026 §2.3 13건 흡수 매핑): `MINOR-4A | logger.ts fallback console.* — verify-engine-contracts.ts excludeExactPaths 추가`
- 실제 구현: `verify-engine-contracts.ts:289-305` `checkConsoleUsage` 함수 — `excludeExactPaths` 필드 미설정 (`paths` 가 `apps/batch/src/{pipeline,recover,signal-handlers,cost-meter}.ts` 4 파일로 한정되어 `packages/shared/src/logger.ts` 는 자연 배제됨)
- line 291 주석: `// packages/shared/src/logger.ts 자체 fallback console.* 는 logger 내부 최후 방어선이라 의도된 예외 (MINOR-4A 명시)` — 주석으로만 명시.
- master-test-checklist.md §12.1 line 4: 유사한 흡수 매핑 — 주석 처리 명시 표현.

**판정 근거:**

- plan 명시 행위 ("excludeExactPaths 추가") vs 실제 구현 (주석 추가) **drift**.
- 그러나 함수의 실제 동작은 4 파일 path 한정으로 logger.ts 자연 배제 — **기능 차이는 0** (검증 결과 동일).
- 따라서 plan 문구가 정확하지 않은 설명이며 실제로는 "주석 + path 한정으로 자연 배제" 라는 설계 결정이 이루어짐. 본 drift 가 Silent Pivot 인지 평가:
  - 사용자 의도 (logger.ts 자체 console.\* 허용) ↔ 실제 구현: **부합** (path 한정으로 동일 효과)
  - 동작 결과: **0건 차이**
- → **CRITICAL 아님 / MAJOR 인지: 기록 의무**.

**수정 권고 (택일):**

- (A) plan §12 line 317 표현 정정 — `MINOR-4A | logger.ts fallback console.* — checkConsoleUsage paths 4 파일 한정 + 주석 명시 (logger.ts 자연 배제)` 로 갱신.
- (B) 주석 외 실제 `excludeExactPaths: ['packages/shared/src/logger.ts']` 명시 추가 — defense-in-depth.

**우회 가능성:** Phase 2 신규 console.\* 정책 변경 시 (예: 4 파일에서 5+ 파일로 확장) 재발 가능. (B) 권고.

### 반론 — "이게 깨질 수 있는 시나리오"

**시나리오 R-4-A (Hard Rule 16 zero-cost 회귀 가능성):** `engine_telemetry` 는 신규 테이블이라 Year 2 마이그레이션 (0005 패턴 — 기존 테이블에 exam_id 컬럼 추가) 미해당. `exam_id NOT NULL` 처음부터 강제. write helper signature `writeTelemetryEvent(payload, deps)` 에서 examId 는 payload 내부 키 — Year 2 multi-exam 시점에 caller (apps/batch) 가 examId 결정 시 문제. 단 Hard Rule 16 본문 "데이터 조회 함수에 examId 첫 인자" 규정은 read API 측 (`GET /gauges/:gaugeName?examId=...`) 미준수 — `routes.ts:145-177` 쿼리에서 `WHERE gauge_name = ?` 만 + examId 필터 없음. **Year 2 multi-exam 진입 시 cross-tenant leak 가능** (예: 손해평가사와 공인중개사 데이터 한 dashboard 에 섞임). 단 plan §0.A "Year 1 단일 시험" 정합 + 인덱스 `idx_engine_telemetry_exam_gauge` 사전 준비 (line 78) 정합 (Year 2 추가 비용 0). **현재 Year 1 시점에서는 OK이며 Year 2 Phase 4 adapter 주입 단계에서 read API examId 필터 추가 의무를 master-dashboard.md §7 차세션 의무에 명시 권고**.

**시나리오 R-4-B (마이그레이션 카운트 갱신 망각 hook):** `verify-engine-contracts.ts:351` `required = 17` + master-test-checklist.md:193 `17/17` 두 곳 동시 갱신 의무. 현재 정합하나 Step 20 (BATCH-1) 진입 + 0018 추가 시점에 두 파일 동시 갱신 망각 시 `verify-engine-contracts.ts` PASS (count >= required) → 신규 마이그레이션 적용 검증 0건 상태 진행 가능. 단방향 게이트 (line 349-350 주석 인지) PASS 정합 + Step 19 plan §6 "verify-engine-contracts.ts:341 마이그레이션 카운트 16 → 17 갱신 (MAJOR-A1 게이트)" 도 동일 패턴. **Phase 2 진입 시 master-test-checklist v3 갱신 시점에 본 정합성 재검증 의무 차세션 명시 권고**.

**시나리오 R-4-C (admin-web mirror 타입 drift):** `apps/admin-web/src/types/telemetry.ts` 와 `apps/api/src/telemetry/types.ts` 수동 mirror — 한쪽 갱신 시 다른 쪽 망각 가능. 현재 8 gauge 동일 PASS. typecheck 는 mirror 정합성 자동 검증 X (별도 패키지로 import 0건). master-dashboard.md §"별도 패키지화 트리거: engine_telemetry 클라이언트 ≥ 5개" (plan §2 line 87) 정합 — 현재 3개라 deferred OK. 본 drift 회귀 방어 위한 **별도 e2e 테스트 (api types ↔ admin-web types 일치 검증) 차세션 권고**.

### N/A (1건)

- **정답 안전 (Pass 3/4 모두 표기되었으나 본 step 무관)** — Reviewer 큐는 Phase 1 후반 활성 (master-dashboard.md §1 line 47 "reviewer_queue ... Phase 1 후반"). plan §10 테스트 계획 정답 검증 미해당. N/A 인정.

---

## 판정

**Pass 3 + Pass 4 종합 판정: 완료 가능 (MAJOR 2건 즉시 흡수 또는 명시 deferred 권고)**

- CRITICAL: 0건
- MAJOR: 2건 (MAJOR-AD-1 CORS / MAJOR-CT-1 plan-실구현 drift)
- ROADMAP §8 line 510 "Engine Observability 8 게이지 가동" 충족 ✅ (테이블/인터페이스/UI 셸 — wire-up 은 master-dashboard.md §6 명시대로 BATCH-1 진입 직전 후속 PR)
- 진산님 메모리 `project_completion_notification_obligation` "★★★ ENGINE HARDENING 완료 ★★★" 알림 의무 충족 가능 (CRITICAL 0건 + MAJOR 2건 즉시 흡수 시)

### 즉시 흡수 권고

1. **MAJOR-AD-1 (CORS)** — `apps/api/src/index.ts` 에 `/api/telemetry/*` CORS 등록 + `CORS_ALLOWED_ORIGINS` 4322 추가 + `X-Admin-Token` allowHeaders 추가. 5 라인 수정 PR. **권장: 본 step 흡수** (master-dashboard.md §4 운영 가이드 "1. ADMIN_API_TOKEN 등록 → 2. 토큰 입력" 흐름 보장).
2. **MAJOR-CT-1 (plan drift)** — plan §12 line 317 표현 정정 또는 `excludeExactPaths` 명시 추가 (택일). 1 줄 수정 PR. 본 step 또는 차세션 흡수 가능 (drift 자체는 동작에 영향 0건).

### 명시 deferred (Year 2 / Phase 2 차세션 의무)

- 시나리오 R-4-A: read API examId 필터 추가 (Year 2 Phase 4 adapter 주입 시점)
- 시나리오 R-4-C: api ↔ admin-web types mirror e2e 테스트 (별도 패키지화 트리거 도달 전 보강)
- 시나리오 R-3-C: dashboard UNION ALL 단일 쿼리 최적화 (Workers Free 티어 검증 시점)

---

## 산출물 경로

`.claude/reviews/step19-pass34-20260501-154451.md` (본 파일 — 절대 경로: `/home/soo/ClaudePro/ThePick/.claude/reviews/step19-pass34-20260501-154451.md`)
