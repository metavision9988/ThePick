리뷰 방식: 독립 에이전트 2개 병렬
리뷰 범위: 변경 파일 3개 + 연관 파일 4개

# 4-PASS REVIEW — Step 0-9 M07 Claude API 배치 프로세서

## 에이전트 구성

1. silent-failure-hunter → Pass 1 (Surgeon) + Pass 2 (Architect)
2. code-reviewer → Pass 3 (Advocate) + Pass 4 (Contract)

## Pass 1 (Surgeon): 🔴 2건 / 🟠 2건 — 수정 완료

확인:

- batch-processor.ts:245-250 — parseContractJson 빈 배열 fallback이 데이터 무음 삭제
  → 누락 필드 경고 로그 + 전부 빈 경우 throw 추가
- batch-processor.ts:284-288 — response.usage null 체크 없음
  → 옵셔널 체이닝 + 기본값 추가
- batch-processor.ts:296 — response.content null/빈 문자열 체크 없음
  → 빈 content throw + stop_reason max_tokens 경고 추가
- batch-processor.ts:310-312 — catch에서 스택 트레이스 소실
  → errorObj.stack 로깅 추가

반론: Claude API가 rate limit 에러(429)를 반환하면 retry가 실행되지만,
401(인증 실패)도 동일하게 retry함. 현재는 모든 에러를 retryable로 취급.
→ 실제 프로덕션에서 SDK 래퍼가 non-retryable 에러를 구분해야 함.

## Pass 2 (Architect): 🔴 0건 / 🟠 1건 — 수정 완료

확인:

- import 경로: section-splitter, table-extractor, schema-validator 정상
- Workers 제약: 빌드 파이프라인 전용이므로 미적용 (정상)
- validateKnowledgeContract 반환값 구조 일치 확인
- buildUserPrompt 섹션 3000자 절삭 시 경고 로그 없음 → 경고 추가

반론: console.log/error 사용 — Phase 0에서는 허용하되,
프로덕션 전 구조화 로깅으로 교체 필요.

## Pass 3 (Advocate): 🔴 0건 / 🟠 1건 — Defer

확인:

- API 키 하드코딩 없음 (ClaudeClient 인터페이스 DI)
- 토큰 비용 로깅 구현됨 (model, input_tokens, output_tokens, retries)
- 에러 메시지에 batchId + 에러 유형 포함
- 느린 테스트(실제 sleep 3초+) — vi.useFakeTimers 미사용 → Defer (기능 영향 없음)

반론: 시스템 프롬프트가 JSON만 반환하라고 지시하지만,
Claude가 설명 텍스트를 추가할 가능성 있음. 이 경우 parseContractJson의
regex가 코드블록을 추출하므로 대응 가능.

## Pass 4 (Contract): 🔴 0건 / 🟠 1건 — Defer (의도적)

확인:

- 타임아웃 30초: DEFAULT_CONFIG.timeoutMs = 30_000 ✓
- 재시도 3회: DEFAULT_CONFIG.maxRetries = 3 ✓
- 지수 백오프: Math.min(1000 \* 2^attempt, 16000) ✓
- 토큰 비용 로깅: console.log에 model/in/out/retries ✓
- 파일 위치: packages/parser/src/batch-processor.ts ✓
- 재프롬프트 미구현 → TODO에서 "수동 트리거" 명시, Silent Pivot 아님
- constants 추가 필드(numeric_value, unit, confusion_level) → Step 0-10에서 처리

반론: Knowledge Contract의 constants 타입에 numeric_value/unit이 없으면
Step 0-10(Constants 추출기)에서 타입 확장이 필요함. 미리 타입에 optional로
추가해두는 것이 더 나을 수 있으나, YAGNI 원칙에 따라 필요 시 추가.

## 수정 요약

| #   | 이슈                               | 등급     | 조치                         |
| --- | ---------------------------------- | -------- | ---------------------------- |
| 1   | parseContractJson 빈 배열 fallback | CRITICAL | 경고 로그 + 전부 빈 시 throw |
| 2   | response.usage null 체크           | CRITICAL | 옵셔널 체이닝 + 기본값       |
| 3   | response.content 빈 값             | HIGH     | throw + stop_reason 경고     |
| 4   | catch 스택 트레이스 소실           | HIGH     | stack 로깅 추가              |
| 5   | 섹션 절삭 무로깅                   | MEDIUM   | 경고 추가                    |
| 6   | BatchResult JSDoc                  | HIGH     | 성공 판정 조건 문서화        |
| D1  | 재프롬프트                         | —        | Defer (수동 트리거 명시)     |
| D2  | constants 추가 필드                | —        | Defer (Step 0-10)            |
| D3  | vi.useFakeTimers                   | —        | Defer (기능 무관)            |

## 검증 결과

- pnpm typecheck: 13/13 통과
- vitest run: 77/77 통과 (54 schema-validator + 23 batch-processor)

## 판정: 완료 가능 (Critical 0건)
