# 4-Pass 독립 리뷰 — Phase 0 감사 수정 9건

리뷰 방식: 독립 에이전트 (code-reviewer + silent-failure-hunter + code-reviewer)
리뷰 일시: 2026-04-13
리뷰 범위: 변경 파일 5개 + 연관 파일 3개

## 변경 파일

1. `migrations/0003_temporal_guard_not_null.sql` (NEW)
2. `packages/formula-engine/src/sandbox.ts` (MODIFIED)
3. `apps/api/src/index.ts` (MODIFIED)
4. `apps/web/src/lib/db.ts` (MODIFIED)
5. `apps/web/src/stores/progress.ts` (MODIFIED)

## 리뷰 라운드

### 라운드 1: silent-failure-hunter + code-reviewer (병렬)

**발견 CRITICAL 3건:**

1. FK PRAGMA 실패 시 요청 진행 (500 반환 없음) -> **수정 완료**
2. IUserProgress.createdAt 누락 -> **수정 완료**
3. Dexie version jump without v1 definition -> **수정 완료**

**발견 MAJOR 4건:**

1. DB binding 없을 때 무음 진행 -> 수정 완료 (500 반환)
2. 기존 NULL 행 검증 쿼리 없음 -> Phase 0 데이터 없으므로 허용
3. nullable 필드 불일치 -> 라운드 2에서 재발견, 수정 완료
4. superseded_by 컬럼 vs SUPERSEDES 엣지 이중성 -> 설계 결정 사항 (문서화)

### 라운드 2: code-reviewer (독립 4-Pass)

── 4-PASS REVIEW ──────────────────

Pass 1 (Surgeon): 7건 확인 / 0건 CRITICAL / 1건 MAJOR / 1건 MINOR
확인:

- sandbox.ts:264 -- evaluate 결과 typeof+isFinite 체크 정상
- index.ts:17 -- await + catch + 500 반환 정상
- progress.ts:44-48 -- console.error + UI 에러 상태 전파 정상
  반론: scope value NaN 시 에러 메시지 부정확 (차단은 됨)

[MAJOR] progress.ts:35-36 -- reduce에서 undefined 필드 NaN 전파
-> **수정 완료** (?? 0 방어 추가)

Pass 2 (Architect): 8건 확인 / 1건 CRITICAL / 1건 MAJOR / 0건 MINOR
확인:

- SQL 트리거 INSERT 허용/UPDATE 차단 정상
- Dexie v1->v2 기존 데이터 보존 정상
- sandbox scope 검증과 AST 검증 일관성 정상
  반론: Dexie version(3) 확장 시 기존 store 재선언 필요

[CRITICAL] Dexie 인터페이스 nullable 불일치 (examScope, confusionLevel 등)
-> **수정 완료** (해당 필드 `| null` 추가)

[MAJOR] IKnowledgeNode.examScope non-null vs Drizzle nullable
-> **수정 완료** (위와 동일)

Pass 3 (Advocate): 5건 확인 / 0건 CRITICAL / 1건 MAJOR / 0건 MINOR
확인:

- FK 실패 시 500 반환 -- 기술 내부정보 미노출
- scope 키 검증 프로토타입 오염 차단 정상
- **proto** 패턴으로 차단 확인
  반론: ALLOWED_FUNCTIONS 이름으로 scope 변수 섀도잉 가능 (낮은 위험)

[MAJOR] BLOCKED_SYMBOL_NAMES에 **proto** 등 미포함 (방어 심층 부재)
-> **수정 완료** (**proto**, **defineGetter** 등 5개 추가)

Pass 4 (Contract): 6건 확인 / 0건 CRITICAL / 0건 MAJOR / 0건 MINOR
확인:

- Hard Limit UPDATE 금지 -- DB 트리거 강제 PASS
- Hard Limit 동적 코드 실행 금지 -- sandbox throwing stub PASS
- Hard Limit .env 커밋 금지 -- 변경 없음 PASS
- Constants DB 쿼리로만 -- ConstantsProvider 인터페이스 PASS
- AI 데이터 draft 상태 -- DEFAULT 'draft' PASS
- Ontology Lock -- 변경 없음 N/A
  반론: 마이그레이션 러너의 FK 설정 미확인 (현재 문제 없음)

판정: **완료 가능** (CRITICAL 0건, MAJOR 0건 -- 모두 수정 완료)
────────────────────────────────────

## 수정 이력

| 라운드 | 발견                           | 등급     | 수정                     |
| ------ | ------------------------------ | -------- | ------------------------ |
| 1      | FK PRAGMA 요청 진행            | CRITICAL | 500 반환으로 변경        |
| 1      | IUserProgress.createdAt 누락   | CRITICAL | 필드 추가                |
| 1      | Dexie version jump             | CRITICAL | v1 스키마 유지 + v2 증분 |
| 2      | Dexie nullable 불일치          | CRITICAL | 해당 필드 `\| null` 추가 |
| 2      | progress.ts reduce NaN         | MAJOR    | `?? 0` 방어 추가         |
| 2      | BLOCKED_SYMBOL_NAMES 방어 심층 | MAJOR    | 5개 이름 추가            |

## 검증

- typecheck: 13/13 통과
- tests: 217/217 통과
- lint: 수정 패키지 전체 통과
