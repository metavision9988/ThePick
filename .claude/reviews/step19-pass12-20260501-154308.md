# Step 19 Pass 1+2 — Surgeon + Architect

## 메타

- 리뷰 방식: 독립 에이전트 (silent-failure-hunter / 코드 정합성 + 연계 검증)
- 리뷰 범위: 변경 11종 + 연관 7종
  - 변경: `migrations/0017_engine_telemetry.sql`, `apps/api/src/db/schema.ts`,
    `apps/api/src/index.ts`, `apps/api/src/telemetry/{types,admin-token,write-helper,routes}.ts`,
    `apps/api/src/telemetry/__tests__/routes.test.ts`,
    `apps/admin-web/src/components/TelemetryDashboard.tsx`,
    `apps/admin-web/src/types/telemetry.ts`, `apps/admin-web/src/env.d.ts`,
    `apps/admin-web/src/pages/telemetry/index.astro`,
    `apps/batch/src/cost-meter.ts`, `apps/batch/src/pipeline.ts`,
    `apps/batch/src/recover.ts`, `apps/batch/__tests__/cost-meter.test.ts`,
    `scripts/verify-engine-contracts.ts`,
    `docs/observability/master-dashboard.md`
  - 연관: `migrations/0014_phase05_critical_hardening.sql`,
    `migrations/0015_batch_runs.sql`, `apps/api/src/middleware/cache-policy.ts`,
    `apps/api/src/__tests__/helpers/d1-from-sqlite.ts`,
    `packages/shared/src/exam-adapter.ts`, `packages/shared/src/logger.ts`,
    기존 `apps/batch/src/{checkpoint,signal-handlers}.ts`
- 작성일: 2026-05-01 KST 15:43

---

## Pass 1 (Surgeon) — Bottom-Up 코드 정합성

### 결과 요약

✅ 18건 PASS / 🔴 0건 / 🟠 2건 (MAJOR) / N/A 4건

### 확인 항목 (PASS 증거)

1. **migrations/0017 append-only 트리거**
   `0017_engine_telemetry.sql:101-113` — `prevent_engine_telemetry_update` +
   `prevent_engine_telemetry_delete` 둘 다 `BEFORE UPDATE/DELETE` + `RAISE(ABORT, ...)` 패턴.
   `__tests__/routes.test.ts:299-326` 에서 `backend.raw.prepare('UPDATE ...').run(...)` /
   `'DELETE FROM ...'` 가 `/forbidden|append-only/i` 정규식으로 throw 검증.
   회피 경로(`INSERT OR REPLACE`, `INSERT ... ON CONFLICT DO UPDATE`) 도 SQLite 가
   해당 row 의 UPDATE 로 처리하므로 BEFORE UPDATE 트리거가 차단.
2. **CHECK 제약 두 줄 — gauge_name enum + metric_value/metric_json 동시 NULL 차단**
   `0017_engine_telemetry.sql:56-66` 8 gauge 화이트리스트 + `(metric_value IS NOT NULL OR metric_json IS NOT NULL)`.
   `types.ts:54-57` Zod refine `data.metricValue !== undefined || data.metricJson !== undefined`
   가 DB CHECK 와 1:1 동기. `routes.test.ts:149-154` 에서 둘 다 부재 → 422 검증.
3. **assertValidExamId 다층 방어 (Hard Rule 17)**
   `types.ts:47` Zod refine `isValidExamId` (allowlist) + `write-helper.ts:47`
   `assertValidExamId(payload.examId)` (brand type) + `routes.test.ts:131-138`
   `'unknown-exam'` → 422. Zod 통과 후 cast 우회 시 두 번째 게이트가 차단.
4. **metric_json 직렬화 + 64KB 차단**
   `write-helper.ts:51-63` — `JSON.stringify` try/catch + `metricJsonText.length > 64*1024` 시
   `TelemetryWriteError` throw. silent drop 0건 — 호출자가 503 + Retry-After 응답.
5. **timing-safe equal 길이 비교**
   `admin-token.ts:28-35` — 길이 다르면 즉시 false (intentional information leak +
   MIN_TOKEN_LENGTH=16 강제로 짧은 토큰 차단), 길이 같으면 `charCodeAt XOR | 누적` 후
   `result === 0` 비교. early-return 없이 전체 루프 — Node.js V8 JIT 가 길이 동일 분기에서
   정확한 시간 동등성 보장 (Web Crypto subtle 미사용 사유 주석 충실).
6. **ADMIN_API_TOKEN 부재/짧음 → 401 마스크**
   `admin-token.ts:46-49` — `length < 16` 또는 `typeof !== 'string'` 시 401.
   `routes.test.ts:106-121` 에서 부재/짧음 둘 다 401 검증.
7. **TelemetryDashboard SSR safety**
   `TelemetryDashboard.tsx:32-34` `readToken` 에 `typeof window === 'undefined'` 가드.
   `index.astro:38` `client:only="react"` 로 SSR 자체 불가하므로 writeToken/clearToken
   서버 실행 경로 0. polling 도 useEffect 내부.
8. **30s 폴링 leak 차단**
   `TelemetryDashboard.tsx:273-278` `useEffect(() => { ... return () => clearInterval(id) }, [token, fetchDashboard])`.
   token 변경 / unmount 시 cleanup 정상.
9. **401 → token clear → 폼 복귀**
   `TelemetryDashboard.tsx:236-246` — 401 응답 시 `clearToken()` + `setToken(null)` →
   다음 렌더에서 `<TokenForm />` 표시.
10. **UUID v4 — Web Crypto 호환**
    `write-helper.ts:49` `crypto.randomUUID()` — Cloudflare Workers + Node 22+ 양쪽 native.
    테스트 결정성 위해 `generateId` 옵션 주입 가능.
11. **D1 INSERT RETURNING null 방어**
    `write-helper.ts:81-83` `if (!result) throw new TelemetryWriteError('INSERT returned no row')`.
    silent drop 0건. `result.recorded_at` 필드도 D1 default 트리거 — null 불가.
12. **rowToEvent 손상 데이터 마스크**
    `routes.ts:75-98` — `JSON.parse` 실패 / 비-object / Array 모두 `metricJson=null` 으로
    마스크 + 주석 명시. 손상 row 가 dashboard render 깨짐 유발 0건.
13. **dashboard limit clamp**
    `routes.ts:152-156` — `Number.parseInt` 후 `!Number.isFinite || n < 1` 시 100 fallback,
    `Math.min(n, 500)` 상한. NaN/음수/Infinity 모두 안전.
14. **CostMeter logger 도입 (MINOR-3A)**
    `cost-meter.ts:37-39` 모듈 스코프 child logger + 423/437/450 에서 SOFT/HARD/KILL
    각각 `costMeterLog.warn` / `.error` 호출. batchRunId/spendUsd/budgetUsd/ratio/stage
    구조화 컨텍스트 누락 0건.
15. **NaN/Infinity/non-integer 토큰 거부**
    `cost-meter.ts:254-266` — `Number.isFinite + Number.isInteger + ≥0` 체크. 부동소수점
    오차 차단 (`Math.round(costUsd * MICRO_USD_PER_USD)` 누적).
16. **recover.ts log.child() 9 호출 모두 적용**
    `recover.ts:164` `const log = recoverLog.child(...)` + 171/188/215/239/262/282/293/314/338
    9건 모두 `log.info|warn|error` (직접 `recoverLog.*` 잔존 0). `grep -c "log\.\(info\|warn\|error\)"` = 9.
17. **fixed dashboard ordering**
    `TelemetryDashboard.tsx:292-303` — `state.data ? sort(...) : ENGINE_TELEMETRY_GAUGES.map(...)`.
    8 gauge 항상 표시, learning_slo 만 phase=2.
18. **enum 순서 동기 (4 파일)**
    `migrations/0017:56-65` ↔ `apps/api/src/db/schema.ts:587-596` ↔ `apps/api/src/telemetry/types.ts:14-23`
    ↔ `apps/admin-web/src/types/telemetry.ts:9-18` 모두 동일 순서 + 동일 8개. drift 0.

### 반론 (Devil's Advocate)

- **시나리오 A — `pipelineLog` 단발 잔존 (line 899-903) 가 스택 트레이스/로그 검색 누락 유발 가능**
  MINOR-A1 의 의도가 "child() 1회 + inline context 제거" 인데 `runPipeline()` 내부
  `stageBatchStructurize` 호출이 `pipelineLog.warn(..., { batchRunId, examId, stage })` 로
  남아있다 (아래 MAJOR-S1 참조). 운영자가 `examId=...` 또는 `batchRunId=...` 로
  로그 검색 시 child logger 가 추가하는 module/component 메타데이터 (있다면) 와
  비대칭 발생. structured logger 의 hashed/normalized 컨텍스트 누락 가능.
- **시나리오 B — JSON deep object 직렬화 후 brand type 보존 실패**
  `write-helper.ts:90` `examId` 가 `assertValidExamId` 통과 후 `TelemetryEvent` 의
  `examId: ExamId` 로 저장되나, `routes.ts:90` `row.exam_id as TelemetryEvent['examId']`
  cast 는 검증 0. D1 row 가 어떤 이유로든 손상되어 (예: 직접 SQL 삽입) 비-allowlist
  exam_id 가 들어있으면 GET /dashboard 가 그대로 noreturn. defense-in-depth 라면
  read 측에도 `isValidExamId(row.exam_id)` 가드 권고 (MINOR).
- **시나리오 C — dashboard 8 게이지 N+1 쿼리**
  `routes.ts:182-215` for-loop 안에서 게이지마다 2 prepared statements
  (`SELECT ... LIMIT 1` + `COUNT(*)`) → 16 round-trips. D1 prepared statement 자체는
  in-process (CF Workers + D1 동일 region) 이지만, count24h 쿼리는 인덱스
  `idx_engine_telemetry_gauge_recorded` 의 prefix range scan 으로 cover. 데이터 1M row
  도달 시 latency p95 가 50ms 한도 압박 가능 — Phase 2 전 GROUP BY single-pass 로
  최적화 권고 (MINOR).

### 🟠 MAJOR-S1 — pipeline.ts:899 `pipelineLog` 잔존 (MINOR-A1 부분 흡수)

**파일/라인:** `apps/batch/src/pipeline.ts:898-904`

**증거:**

```typescript
} else {
  pipelineLog.warn('processBatch returned null usage — CostMeter skip for this call', {
    batchRunId: ctx.batchRunId,
    examId: ctx.examId,
    stage: 'batch_structurize',
  });
}
```

이는 `runPipeline()` 내부 `stageBatchStructurize` 함수에서 직접 `pipelineLog` 를
사용 + inline `batchRunId/examId` 컨텍스트를 다시 명시. MINOR-A1 의 명시 의도
("pipelineLog.child() 1회 생성 + 매 호출 inline context 제거 14건") 과 부분
모순. line 451 의 `log = pipelineLog.child(...)` 와 같은 함수 흐름 안인데
다른 stage helper (`stageBatchStructurize`) 가 `runPipeline` 의 `log` 를 받지
않아 모듈 스코프 logger 로 회귀.

**영향:**

- silent failure 직접 유발 X (warn 자체는 발화) — 로그 정합성 문제.
- 구조화 로깅 운영자가 log search "batchRunId=..." 와 "examId=..." 두 개로
  검색 시 child logger 가 누적하는 메타 (module/component 등) 와 inline 추가
  컨텍스트가 중복 또는 비대칭. JsonLogger 가 child + 호출 컨텍스트 merge 하면
  중복 단일 키 (`batchRunId` × 2) 가 발생 가능 (`packages/shared/src/logger.ts`
  의 child 구현 검증 필요).

**권고 fix:**

옵션 A — `stageBatchStructurize` 시그니처에 `log: Logger` 추가하여 `runPipeline`
의 child 를 전파:

```typescript
async function stageBatchStructurize(
  ctx: PipelineContext,
  state: PipelineState,
  started: number,
  log: Logger, // ← 추가
): Promise<StageResult> {
  ...
  log.warn('processBatch returned null usage — CostMeter skip for this call', {
    stage: 'batch_structurize',
  });
}
```

옵션 B (Step 19 minimal): line 899 호출만 inline 컨텍스트 제거하고 warn 메시지에
포함:

```typescript
pipelineLog.warn(
  `processBatch returned null usage — CostMeter skip for batchRunId=${ctx.batchRunId} examId=${ctx.examId} stage=batch_structurize`,
);
```

옵션 A 가 옳은 방향 — 다른 stage helper 들도 동일하게 child log 전파해야 일관성.

**우선순위:** MAJOR (Step 19 완료 게이트 차단은 아니나, MINOR-A1 흡수 보고 정확성 위해 정정 권고).

### 🟠 MAJOR-S2 — engine_telemetry **write 측 미통합** (Phase 1 wire-up gap)

**파일/라인:** `apps/batch/src/cost-meter.ts` 전체, `apps/batch/src/pipeline.ts`
전체

**증거:**

`master-dashboard.md:32-44` 에서 Phase 1 활성 7 게이지의 write 주체로
`apps/batch/pipeline.ts` (batch_progress), `apps/batch/cost-meter.ts` (cost),
`apps/batch/loader.ts` (d1_slo) 등을 명시. 그러나 실제 코드에서 `POST /api/telemetry`
또는 `engine_telemetry` 직접 INSERT 호출 0건:

```bash
$ grep -rn "engine_telemetry\|/api/telemetry" apps/batch/src/
# (출력: 0 건)
$ grep -rn "writeTelemetryEvent\|telemetryEventPayload" apps/batch/
# (출력: 0 건)
```

즉 현 시점 admin-web `/telemetry` 는 8 gauge 모두 `no_data` 만 표시. 진산님
ROADMAP §8 line 510 "Engine Observability 8 게이지 가동" 의 "가동" 정의가
무엇인지에 따라 판정 분기:

- (해석 1) "가동" = 인프라 가동 = 대시보드 + write API + 트리거 = Step 19
  완료 OK
- (해석 2) "가동" = 데이터 흐름 가동 = BATCH-1 첫 적재 시점에 batch_progress /
  cost / d1_slo 데이터 기록 = Phase 2 (BATCH-1 적재 시작) 의무

`master-dashboard.md:5` "BATCH-1 적재부터 본격 활성" 명시 → 해석 1 = Step 19
완료 OK. 단, **경계 명확화** 가 plan/ROADMAP/handoff 에 누락되면 진산님이
Step 19 종료 후 admin-web 대시보드가 비어있는 것을 보고 "왜 데이터 없냐"
오해 위험.

**영향:**

- 기능 결함 X (master-dashboard.md 설계 의도와 정합).
- 하지만 진산님의 "★★★ ENGINE HARDENING 완료 ★★★" 알림 후 즉시 admin-web
  접속 시 8 게이지 모두 no_data 표시 → 인지 혼란 가능.
- handoff-027 또는 진산님 알림 메시지에 명시 권고:
  "Step 19 = Observability v1 인프라 완료. 데이터 흐름은 BATCH-1 적재 시작
  시점 (Phase 2 진입) 부터 기록."

**권고 fix:**

옵션 A (코드 변경 0): handoff-027 + 진산님 알림 메시지에 명시 추가.
옵션 B (Step 19 추가 작업): cost-meter.ts 의 SOFT_WARN/HARD_THROTTLE/KILL 발화
시점에 `POST /api/telemetry` 비동기 fire-and-forget 호출 추가 (cost gauge 1개만
선행 활성). pipeline.ts 의 stage 완료 시점에 batch_progress 추가.
옵션 C (Phase 2 진입 첫 step 으로 이월): 19a/20 step 신설.

진산님 결정 권고. **Step 19 자체 완료 판정에는 영향 없음**, 단 보고 명확성
의무.

**우선순위:** MAJOR (사용자 인지 명확성 — Phase 1 종료 게이트 직전이라 critical).

### N/A 항목

- 산식 정밀도 (formula engine 무관)
- pdfplumber subprocess (telemetry 무관)
- Vectorize / Claude API (telemetry write side 미구현이라 무관)
- FSRS (학습 SLO Phase 2 활성, 본 step 무관)

---

## Pass 2 (Architect) — Top-Down 연계 검증

### 결과 요약

✅ 17건 PASS / 🔴 0건 / 🟠 2건 (MAJOR) / N/A 5건

### 확인 항목 (PASS 증거)

1. **D1 스키마 ↔ Drizzle 정합 (engineTelemetry)**
   `migrations/0017_engine_telemetry.sql:47-67` 컬럼 9개 ↔
   `apps/api/src/db/schema.ts:600-626` Drizzle 테이블 9개 1:1.
   인덱스 3종 (`idx_engine_telemetry_gauge_recorded` /
   `_exam_gauge` / `_batch_run`) 모두 Drizzle `(table) => ({...})` 절에 동기.
   CHECK 제약 + 트리거는 의도적으로 Drizzle 미선언 (NC-1 정책 정합 — schema.ts:5-17).
2. **Hard Rule 17 — exam-ids.ts 외 리터럴 0건**
   `verify-engine-contracts.ts:274-285` `checkHardRule17` grep 화이트리스트 통과.
   `apps/api/src/telemetry/`, `apps/admin-web/src/` 신규 코드 모두 `EXAM_IDS.SON_HAE_PYEONG_GA_SA`
   경유 (테스트 11곳 + 본문 0건). admin-web 측은 fetch 가 string 그대로 보내는데 — Zod
   refine 이 서버 측에서 차단.
3. **Hard Rule 16 — 시그니처 첫 인자 examId**
   `routes.ts:122-142` POST 핸들러는 examId 를 payload 안에 받아 `writeTelemetryEvent`
   에 그대로 전달. write-helper 가 `assertValidExamId` 통과 후 INSERT 의 `?` bind 에 주입.
   Year 2 Phase 4 시점에 `WHERE exam_id = ?` 절 추가하는 것만으로 zero-cost 전환.
4. **Workers 호환 — fs/path 미사용 (telemetry 측)**
   `grep -n "node:fs\|node:path\|fsync" apps/api/src/telemetry/*.ts` = 0건.
   `crypto.randomUUID` 만 사용 (Web Crypto). Hono + D1 prepared API 만 사용.
5. **Hexagonal 위반 0**
   `apps/api/src/telemetry/` 가 `apps/api/src/auth/`, `apps/api/src/progress/` 등
   다른 모듈 직접 참조 0. shared 패키지 (`@thepick/shared`) 의 `Logger`,
   `ExamId`, `EXAM_IDS` 만 import.
6. **engine_telemetry append-only 트리거 0014 패턴 정합**
   `migrations/0014:111-120` `prevent_X_delete` (knowledge_nodes/formulas/constants)
   ↔ `migrations/0017:101-113` `prevent_engine_telemetry_update/delete` 동일
   `BEFORE` + `RAISE(ABORT, '...')` 패턴. 메시지 톤도 일관 ("forbidden ... use ...").
   다만 0014 는 화이트리스트 컬럼 (UPDATE OF id, type, ...) 이고 0017 은 전체
   UPDATE 차단 — append-only 라 화이트리스트 불필요 (rationale 주석 0017:91-95 정합).
7. **i18n — admin-web 단일 사용자 (진산님)**
   `TelemetryDashboard.tsx` + `types/telemetry.ts` + `index.astro` 한국어
   하드코딩 의도적. 사용자 노출 페이지 아님 (admin only). 진산님 직접 사용
   메모리 `feedback_focus_reliability_not_schedule` + admin-web 정의 정합.
8. **api/index.ts 라우트 등록**
   `apps/api/src/index.ts:9` import + line 107 `app.route('/api/telemetry', createTelemetryRoutes())`.
   기존 auth/progress/webhooks 와 동일 패턴. ADMIN_API_TOKEN binding 도 line 41
   `Bindings` 타입에 추가.
9. **테스트 마이그레이션 자동 로드**
   `apps/api/src/__tests__/helpers/d1-from-sqlite.ts:86-91` `createD1FromAllMigrations`
   가 `readdirSync(MIGRATIONS_DIR).filter(.sql).sort()` — 0017 자동 포함.
   따라서 routes.test.ts 가 실 SQLite + 0017 트리거 검증.
10. **types 동기 — admin-web 측 mirror**
    `apps/admin-web/src/types/telemetry.ts:9-18` enum + 22-31 TelemetryEvent +
    33-39 GaugeSnapshot ↔ `apps/api/src/telemetry/types.ts` 1:1. metricValue
    (number | null), metricJson (Record<string, unknown> | null), recordedAt
    (string) 모두 동일.
11. **Drizzle NC-1 정책 정합**
    `schema.ts:584-585` "Drizzle 정책 (NC-1): 본 선언은 타입 파생 전용. CHECK
    제약 + 트리거는 migrations/0017 본문에서 강제. drizzle-kit 사용 금지."
    ENGINE_TELEMETRY_GAUGES 런타임 상수 + EngineTelemetryGauge 타입 + 본 모듈
    내 export 한정 — Drizzle enum 필드용 + 외부 import 제공.
12. **ADMIN_API_TOKEN binding 분리**
    `apps/api/src/index.ts:41` `ADMIN_API_TOKEN?: string` 추가. wrangler.toml /
    .dev.vars 에 사용자가 별도 설정. JWT_SECRET / WEBHOOK_HMAC_SECRET 등 다른
    secret 과 격리.
13. **Cloudflare 단일 벤더 정합**
    `master-dashboard.md:14-23` 운영 모델 = D1 + Pages + Workers (외부 SaaS 0).
    메모리 `feedback_single_vendor_cloudflare` 정합. 비용 $0/m 추가 (추가 binding 0).
14. **Ontology Lock 영향 0**
    `engine_telemetry` 신규 테이블이 `packages/parser/src/ontology-registry.json`
    영향 없음. Knowledge graph 노드/엣지 미생성 — 시계열 fact table 만.
15. **verify-engine-contracts.ts 마이그레이션 카운트 = 17**
    `scripts/verify-engine-contracts.ts:351` `const required = 17;` ↔ 실제 파일 수
    `ls migrations/*.sql | wc -l` = 17. PASS.
16. **console.\* 검증 4 파일 확장**
    `verify-engine-contracts.ts:289-305` `checkConsoleUsage` paths 에 `cost-meter.ts`
    추가 (3 → 4 파일). cost-meter.ts grep `console.(log|warn|error|info|debug)` = 0
    (모두 `costMeterLog.warn|error` 로 교체).
17. **Drizzle BatchRunsDb / batch_runs FK 누락 의도적**
    `engine_telemetry.batch_run_id` 컬럼은 FK 미선언. 의도된 design — 시계열 fact
    table 의 batch_run_id 가 batch_runs 의 lifecycle 보다 길게 (1년 retention)
    유지될 수 있어 referential integrity cascade 위험. master-dashboard.md §3 retention
    정책 (Phase 2 1년) + 0017:97-98 wrangler d1 execute 수동 GC 명시 — FK 부재가
    의도. (단, 본 의도가 0017 SQL 주석에 명시되지 않음 → 아래 MAJOR-A2 참조)

### 반론 (Devil's Advocate)

- **시나리오 A — `/api/telemetry` 가 cache-policy `PRIVATE_PATH_PREFIXES` 미포함**
  `apps/api/src/middleware/cache-policy.ts:20-28` PRIVATE 목록은 `/api/auth/`,
  `/api/user/`, `/api/progress/`, `/api/payment/`, `/api/webhooks/`. `/api/telemetry/`
  부재. routes.ts:16 docstring "GET 응답 캐시 비활성 (cache-policy 미들웨어가
  private/no-store 강제)" 는 약간 부정확. 실제로는 cache-policy.ts:65-68 default
  floor `no-store` 가 적용되어 결과는 동일하지만 (private 미명시), Cloudflare
  Edge 가 X-Admin-Token 헤더 없이 응답을 캐시할 가능성은 0 (no-store 단독으로
  shared-cache 차단). 단 docstring 수정 권고 (MINOR — 본 리뷰 차원에서 미흡수).
- **시나리오 B — Cloudflare Workers `setInterval` 미지원 vs admin-web 측 setInterval**
  admin-web 은 Cloudflare Pages (browser 측 React) 라 Workers runtime 비대상.
  `window.setInterval` 정상. Workers 에서는 `routes.ts` 가 `setInterval` 미사용
  — request/response 라이프사이클 안에서만 동작. 안전.
- **시나리오 C — Year 2 multi-tenant 시 dashboard 쿼리가 exam_id 필터 누락**
  `routes.ts:187-204` `WHERE gauge_name = ?` 만, exam_id 필터 없음. Year 1 단일
  시험이라 OK 이나, Year 2 진입 시 admin-web 이 모든 시험의 게이지를 하나로
  뭉쳐 표시. Hard Rule 16 본문이 read 측에도 examId 시그니처 의무인데, GET
  /dashboard 는 query param 미수신. Phase 4 마이그레이션 시점에 dashboard 라우트도
  `?examId=...` query 추가 + WHERE 절 동시 변경 의무. 본 step 시점은 OK 이나,
  Hard Rule 16 본문 "Year 2 zero-cost 전환" 약속 위반 가능 — Year 2 진입 시
  dashboard 라우트 변경 의무. 아래 MAJOR-A1.

### 🟠 MAJOR-A1 — GET /telemetry/dashboard examId 시그니처 부재 (Year 2 zero-cost 위반 위험)

**파일/라인:** `apps/api/src/telemetry/routes.ts:180-227` (`GET /dashboard`),
`routes.ts:145-177` (`GET /gauges/:gaugeName`)

**증거:**

POST /api/telemetry 는 payload 에 `examId` 필수 (`types.ts:47`). 그러나 GET
read 라우트 두 개 (`/dashboard`, `/gauges/:gaugeName`) 는 examId query param /
header 모두 미수신. 내부 SQL `WHERE gauge_name = ?` 만 — Year 1 단일 시험이라
"모든 게이지 = 손해평가사" 가정 OK. Year 2 multi-tenant 진입 시:

- (해석 a) admin 이 모든 시험의 게이지를 한 화면에 합산 보고 싶다 → 현 구조 OK
- (해석 b) 시험별로 격리된 dashboard 가 필요 (Hard Rule 16 본문 정합) → 현
  구조는 query param + SQL WHERE 절 추가 필요 → 호출 측 (admin-web) 도 변경
  필요 = **zero-cost 전환 위반**

Hard Rule 16 의 "Year 2 (exam_id 컬럼 도입 후) 동일 래퍼 함수가 내부적으로
WHERE exam_id = ? 절 주입. 호출 측 코드는 변경 불필요" 약속과 충돌.

**영향:**

- 기능 결함 X (Year 1 단일 시험에서 정상).
- 약속 위반 — Year 2 진입 시 admin-web 코드도 수정 필요해짐 = zero-cost 약속 깨짐.
- backend-architect 류 페르소나가 P-3 5-페르소나 리뷰에서 동일 지적할 가능성 높음.

**권고 fix:**

옵션 A (Step 19 추가, 최소 변경): GET 라우트에 `examId` query param 옵션
추가. Year 1 시점은 default = `EXAM_IDS.SON_HAE_PYEONG_GA_SA` (단일 시험), 내부
SQL 은 그대로 `WHERE gauge_name = ?`. Year 2 진입 시 default 제거 + WHERE 절에
exam_id 추가. 호출 측 (admin-web) 은 Year 1 시점에 examId 미명시 (default 적용)
또는 명시 (정확).

옵션 B (Phase 2 진입 시 처리): handoff-027 + Year 2 마이그레이션 0005 plan 에
명시 이월. Step 19 자체에는 영향 없음.

진산님 결정 권고. 옵션 B 가 minimal, 옵션 A 가 Hard Rule 16 본문 의도 충실.

**우선순위:** MAJOR (Year 2 zero-cost 약속 정합성). 단 Step 19 게이트 차단은 아님 — Phase 4 이월 가능.

### 🟠 MAJOR-A2 — engine_telemetry FK 부재 + 의도 명문화 누락

**파일/라인:** `migrations/0017_engine_telemetry.sql:47-67`

**증거:**

`engine_telemetry.batch_run_id` (`source_id` 도) 컬럼이 FK 미설정. 0017 SQL 주석에서
이 의도를 명시하지 않음. master-dashboard.md 도 "append-only 보증" / "1년 보존
정책" 만 언급. FK 부재 의도가 retention 차이 (engine_telemetry 1년 vs batch_runs
laifecycle 짧음) 를 보호하기 위함이라면 SQL 주석에 명시 의무.

같은 0017:42 `PRAGMA foreign_keys = ON` 선언 + 마이그레이션 자체에 FK 0건 = "이
테이블은 referential integrity 가 cascade 영향을 받지 않도록 의도적으로 분리"
정확한 디자인. 단 명시 0.

**영향:**

- 운영 결함 X (Year 1 + Year 2 양쪽 안전).
- 운영자 (또는 차세션 Claude) 가 SQL 검토 시 "FK 누락 = 실수 아닌가?" 오해
  가능. 4-Pass Pass 4 (Contract) 가 동일 지적할 가능성 있음.

**권고 fix:**

`migrations/0017_engine_telemetry.sql` 주석에 추가 (디자인 의도 명문화):

```sql
--   5. FK 의도적 부재 — batch_run_id / source_id 는 referential 비-강제.
--      engine_telemetry retention (Phase 2: 1년) 가 batch_runs lifecycle 보다
--      길어 cascade 위험. 시계열 fact table 패턴.
```

**우선순위:** MAJOR (디자인 의도 명문화). Step 19 완료 게이트 차단 아님.

### N/A 항목

- truth_weight 정렬 (telemetry 무관)
- IndexedDB ↔ D1 동기화 (admin-web 만, IndexedDB 미사용)
- ARCHITECTURE.md 다이어그램 (별도 review 의무)
- pdfplumber subprocess (telemetry 무관)
- 다이어그램 정합성 (master-dashboard.md 가 별도 venue 라 본 리뷰 범위 외)

---

## 종합 판정

### 발견 사항 분류

- 🔴 CRITICAL: **0건**
- 🟠 MAJOR: **4건**
  - MAJOR-S1: pipeline.ts:899 `pipelineLog` 잔존 (MINOR-A1 부분 흡수)
  - MAJOR-S2: engine_telemetry write 측 미통합 (handoff/알림 명시 의무)
  - MAJOR-A1: GET /dashboard examId 시그니처 부재 (Year 2 zero-cost 위반 위험)
  - MAJOR-A2: engine_telemetry FK 부재 의도 명문화 누락
- ✅ PASS 35건 (Pass 1: 18, Pass 2: 17)

### 판정: **완료 가능 (조건부) — CRITICAL 0건**

본 step 진산님 명시 게이트 = "CRITICAL 0건이면 본 step 완료 가능" 충족.

MAJOR 4건은 모두 Step 19 자체 차단이 아닌 정합성/명문화 권고:

- **S1**: 차세션 즉시 정정 권고 (옵션 B minimal — 1줄 수정).
- **S2**: handoff-027 + 진산님 알림 메시지에 명시 추가 (코드 변경 0).
- **A1**: Phase 2 진입 시점 Year 2 plan 에 명시 이월 (Step 19 미흡수).
- **A2**: 0017 SQL 주석 1단락 추가 (1 commit minimal — 권고 즉시 정정).

### 진산님 결정 권고

다음 3 옵션 제시:

- **옵션 A (즉시 흡수)**: MAJOR-S1 + MAJOR-A2 즉시 정정 (코드 2-line + 주석
  1단락) → 5분 작업. 본 step 끝나기 전 완료.
- **옵션 B (handoff 이월)**: 4건 모두 handoff-027 명시 + 차세션 첫 step 으로
  흡수. Step 19 종료 + Phase 1 종료 알림은 그대로 발송.
- **옵션 C (선택)**: MAJOR-S1 만 즉시 정정 (코드 정합성 우선) + S2/A1/A2 는
  handoff. (권고 — 코드 정합성 + 보고 명확성 균형)

본 리뷰 자체로는 Step 19 완료 + Phase 1 종료 게이트 통과 판정.

---

## 리뷰 산출 파일

- `/home/soo/ClaudePro/ThePick/.claude/reviews/step19-pass12-20260501-154308.md` (본 파일)

## 다음 단계

- Pass 3 (Advocate) + Pass 4 (Contract) 별도 독립 에이전트 병렬 진행 의무
- 5-페르소나 기술부채 심층 리뷰 (Phase 1 종료 = Step 19 → 의무 트리거)
