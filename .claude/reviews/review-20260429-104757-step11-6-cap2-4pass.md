---
리뷰 방식: 독립 에이전트 (silent-failure-hunter + system-architect + quality-engineer 3개 병렬)
리뷰 일시: 2026-04-29 10:47 KST
리뷰 대상: Step 11.6 cap=2 정정 — SF-C-1 + SA-M-2 (Session 017)
리뷰자 컨텍스트: 메인 대화 모름 (의도 편향 차단 — 각 페르소나 독립 판정)
plan 근거: docs/plans/engine-hardening/step11-6-pipeline-recover-integration.plan.md v1.1
선행 보고서: review-20260429-094423-step11-6-pipeline-integration-4pass.md
---

# Step 11.6 cap=2 정정 — 4-Pass 통합 보고서

## 0. 한 줄 결론

**3 페르소나 모두 CRITICAL 0건 — cap=2 정정 PASS.** MAJOR 3건은 모두 다음 세션 9 AC e2e 작성 시 흡수 또는 코드 보강. 본 정정은 silent failure 신규 도입 0건 + 인터페이스 호환성 PASS + 회귀 0건 (137/137).

---

## 1. 리뷰 방식

| 페르소나              | 관점                                                                 | CRITICAL | MAJOR | MINOR | 판정                         |
| :-------------------- | :------------------------------------------------------------------- | :------: | :---: | :---: | :--------------------------- |
| silent-failure-hunter | 무음 실패 / catch 후 silent drop / 정정의 새 silent 도입 여부        |    0     |   1   |   2   | accept_with_caveats          |
| system-architect      | ExitCode 호환성 / instanceof 정합 / Hard Rule / SLO 영향 / 운영자 UX |    0     |   1   |   2   | 완료 가능                    |
| quality-engineer      | 137 회귀 0건 객관 + 정정 효과 미검증 갭 + e2e 적격 위치 매핑         |    0     |   1   |   3   | 완료 가능 (cap=2 범위 한정)  |
| **통합**              | **CRITICAL 0** / **MAJOR 3** (서로 직교) / **MINOR 7** (중복 1)      |  **0**   | **3** | **7** | **PASS — 본 세션 완료 가능** |

3 보고서 산출물:

- `.claude/reviews/review-20260429-104757-step11-6-cap2-silent-failure.md`
- `.claude/reviews/review-20260429-104757-step11-6-cap2-system-architect.md`
- `.claude/reviews/review-20260429-104757-step11-6-cap2-quality.md`

본 통합 보고서가 review-gate.sh 인식 형식.

---

## 2. CRITICAL — **0건**

### 본 cap=2 정정의 silent failure / 호환성 / 회귀 영향 PASS

3 페르소나 모두 CRITICAL 0건 — 본 cap=2 정정은 새 silent failure / 인터페이스 깨짐 / 회귀를 도입하지 않았다.

- **SF-C-1 정정 검증** — finally 의 try/catch wrap 이 의도대로 적용. `removeHandlers()` throw 시 console.error 후 다음 분기 (`costMeter.finalize`) 정상 실행. 두 finally 분기 독립 try/catch 격리 (한쪽 throw 가 다른 쪽 차단 X).
- **SA-M-2 정정 검증** — `ExitCode = 0|1|2|4|5` 타입 확장 + `instanceof` 분기 모두 호환성 PASS. console.error 분기 이전 1회 출력 → 이중 로깅 risk 0건. ConcurrentRunError / RecoveryFailedError 단일 모듈 인스턴스 export → instanceof 항상 true.

---

## 3. MAJOR 3건 — 모두 직교 (중복 0건)

### SF-MAJOR-1 — `pipeline.ts:541, 547` console.error 직접 호출 (logger 추상화 부재)

**위치:** `apps/batch/src/pipeline.ts:541, 547`
**결함:** finally 의 양쪽 try/catch 가 console.error 직접 호출 — 운영 환경 로그 수집기 통합 어려움. 본 cap=2 범위 외, 다음 세션 또는 Step 18 cleanup.
**처리 권고:** **명시 이연** (cap=2 범위 외) — Step 18 logger 추상화 도입 시 일괄.

### SA-MAJOR-Mn-1 — `bin/batch.ts:64-69` ExitCode JSDoc 의 exit 3 비움 사유 미명시

**위치:** `apps/batch/bin/batch.ts:64-69`
**결함:** `ExitCode = 0|1|2|4|5` 에서 3 가 비어있는데 JSDoc 에 의도 명시 X. 운영 런북 / 자동화 스크립트 작성 시 가독성 손상. 직전 4-Pass 미발견 (신규 결함).
**정정안:**

```typescript
/**
 * CLI exit codes — 운영자가 retry 가능 vs 무결성 실패 vs 동시 실행 차단을 분류.
 *   0 = 정상 / 1 = 일반 실패 / 2 = invalid args
 *   (3 = 의도적 비움 — Node.js exit 3 = "Internal JS Parse Error" 와 충돌 회피)
 *   4 = ConcurrentRunError / 5 = RecoveryFailedError
 */
```

**난이도:** 5초 (JSDoc 1줄 보강)
**처리 권고:** 본 세션 즉시 또는 다음 세션 §3.1 G CLI e2e 작성 시 흡수.

### Q-MAJOR-1 — SF-C-1 / SA-M-2 정정 효과 e2e 검증 부재 (회귀 ≠ 효과)

**위치:** `apps/batch/src/__tests__/pipeline.integration.test.ts` 5 tests / `__tests__/recover.test.ts` 8 tests
**결함:** 137/137 PASS 는 객관 (회귀 0건). 그러나:

- 137 테스트 모두 `enableSignalHandlers: false` 사용 → SF-C-1 의 `removeHandlers()` throw 경로 도달 0
- SA-M-2 의 `instanceof` 분기는 vitest 가 `runPipeline` 직접 호출 → `batch.ts` main 미경유 → 도달 0

→ **회귀 없음 ≠ 효과 검증** 분리 명시.

**처리 권고:** 다음 세션 §3.1 흡수 (handoff-016 매핑):

| 검증 대상                                                   | 흡수 위치                              | 패턴                                                                                   |
| :---------------------------------------------------------- | :------------------------------------- | :------------------------------------------------------------------------------------- |
| SF-C-1 (removeHandlers throw → finally 양쪽 분기 모두 실행) | §3.1 D `signal-handlers.test.ts` 신규  | `vi.spyOn(process, 'off').mockImplementationOnce(() => { throw new Error(...) })`      |
| SA-M-2 (exit code 4/5 분기)                                 | §3.1 G CLI e2e 신규 (subprocess spawn) | `child_process.spawn('tsx', ['bin/batch.ts', 'run', 'BATCH-1', ...]) → exitCode === 4` |

**난이도:** 0.05d (이미 §3.1 D + G 분량에 자연 흡수)

---

## 4. ✅ 확인 항목 통합 (전수 PASS — 16건)

3 페르소나 합산 16건 ✅ 확인. 핵심:

- **SF-C-1 finally 정정 정합** — try/catch wrap 의도 일치 + 양쪽 분기 독립 격리 (silent-failure)
- **signal-handlers.ts:52-55 cleanup sync 보장** — `process.off` sync → catch 가 모든 throw 포착 (silent-failure)
- **이중 로깅 0건** — console.error 분기 이전 1회 (silent-failure)
- **ExitCode 타입 확장 호환** — `main().then(code => process.exit(code))` 정합 (system-architect)
- **ConcurrentRunError / RecoveryFailedError export 정합** — pipeline.ts:294, 308 export → batch.ts import 경로 OK (system-architect)
- **POSIX exit code 무충돌** — 4/5 가 sysexits.h 64-78 / SIGINT 130 / SIGTERM 143 와 무충돌 (system-architect)
- **Hard Rule 16/17 무위반** — 시험 ID 리터럴 신규 도입 0건 (system-architect)
- **plan §10 SLO 영향 0** — 본 cap=2 는 인터페이스 무변경 → 다음 PipelineResult 확장에 새 부담 X (system-architect)
- **typecheck PASS + 137/137 회귀 0건** (quality-engineer 재실행 검증)
- **production-quality.md 정합** — 빈 catch 0건 + console.error 사용 정당 + CRITICAL RULE #3 finally throw 흡수 패턴 (quality-engineer)

---

## 5. Devil's Advocate 통합 (7 시나리오)

3 페르소나 합산 7 시나리오 + 격상 위험 평가:

|  #  | 시나리오                                                                  | 출처 | 격상 위험 | 처리                                                      |
| :-: | :------------------------------------------------------------------------ | :--- | :-------: | :-------------------------------------------------------- |
|  1  | cleanup 이 sync→async 변경 시 catch 가 promise rejection 미포착           | SF   |  🟢 LOW   | 회귀 테스트로 signature 고정 권장 (다음 세션 D)           |
|  2  | ESM/CJS dual-package hazard → instanceof silent fallthrough               | SF   |  🟢 LOW   | 현 ESM 단일 빌드 안전. 패키징 변경 시 회귀 추가           |
|  3  | 이중 SIGINT race                                                          | SF   |  🟢 LOW   | 본 정정과 직교 (정상 종료 vs signal)                      |
|  4  | VM2/vitest 격리에서 process.off throw                                     | SF   |  🟢 LOW   | console.error 후 흐름 계속 → **정정 정당성 강화**         |
|  5  | RecoveryFailedError 미래 외부 wrapping 시 instanceof false positive       | SA   |  🟡 MED   | JSDoc invariant "단독 throw, 외부 wrapping 금지" 1줄 권고 |
|  6  | Node.js exit 5 = "Fatal Error in V8" 와 의미 충돌 → 자동화 false positive | SA   |  🟡 MED   | Mn-1 JSDoc 보강 시 명시. CI/CD retry 정책 검토            |
|  7  | signal-handlers cleanup 미래 D1 close 등 추가 시 silent failure 변질      | Q    |  🟡 MED   | JSDoc invariant "process listener removal 만" 1줄 권고    |

**🔴 HIGH 0건** — 모든 시나리오 LOW/MED. 5/6/7 은 다음 세션 §3.1 D 작성 시 JSDoc invariant 1줄 동시 흡수 가능 (분량 무시).

---

## 6. 정정 vs 명시 이연 매트릭스

### 본 세션 처리 (cap=2 PASS)

| ID                | 처리                                                       |
| :---------------- | :--------------------------------------------------------- |
| **SF-C-1**        | ✅ 본 세션 정정 완료 (`pipeline.ts:537-551`)               |
| **SA-M-2**        | ✅ 본 세션 정정 완료 (`bin/batch.ts:23-30, 64-69, 88-101`) |
| **SA-Mn-1 JSDoc** | 🟡 권고: 본 세션 5초 보강 또는 다음 세션 §3.1 G 동시 흡수  |

### 다음 세션 (Day 1~3 e2e 작성 + 정정) 흡수

| ID                                              | 흡수 위치                                         |   분량    |
| :---------------------------------------------- | :------------------------------------------------ | :-------: |
| **Q-MAJOR-1 e2e 갭** (SF-C-1 + SA-M-2)          | §3.1 D `signal-handlers.test.ts` + §3.1 G CLI e2e |   0.05d   |
| **SF-MAJOR-1 logger 추상화**                    | Step 18 cleanup                                   | 명시 이연 |
| **MINOR 7건** (SA #5/#7 JSDoc + 메시지 보강 등) | §3.1 G 작성 시 JSDoc 1줄 + 메시지 1줄 동시 흡수   |    5초    |

### 진산님 결정 영역 (변경 없음 — 직전 4-Pass §6 정합)

본 cap=2 정정 4-Pass 는 **신규 진산님 결정 영역 추가 없음**. 직전 4-Pass §6 의 결정 영역 3건 (SF-C-2 옵션 A/B/C / SF-C-3+Q-M-2 PipelineResult 확장 시점 / MINOR 12건 일괄 처리 시점) 은 그대로.

진산님 답변 대기 문서: `.claude/reports/decision-20260429-step11-6-after-cap2.md`

---

## 7. 통합 판정

**판정:** **PASS — 본 cap=2 정정 완료 가능**

**조건:**

1. CRITICAL 0건 — 본 cap=2 정정의 silent failure / 호환성 / 회귀 영향 모두 PASS
2. MAJOR 3건 모두 직교 — 본 cap=2 범위 외 또는 다음 세션 e2e 작성 시 자연 흡수
3. SA-Mn-1 JSDoc 보강 (5초) 은 본 세션 즉시 또는 다음 세션 §3.1 G 흡수 — 진산님 선택

**검증 결과 (객관 사실, 재실행 확인):**

- typecheck PASS (`pnpm -C apps/batch typecheck`)
- 137/137 tests PASS (회귀 0건, 1.39s 초기 + 1.50s 재실행)
- production-quality.md 빈 catch 0건 + console.error 정당
- Hard Rule 16/17 무위반

**다음 세션 차단 게이트:**

- 본 cap=2 정정은 차단 게이트 X. 진산님 §6 결정 영역 3건 답변 → 다음 작업 (PipelineResult 확장 / 9 AC e2e) 진입.

---

## 8. 진산님 보고 요약

본 세션 cap=2 정정 검증:

- ✅ typecheck PASS + 137/137 회귀 0건 (재실행 확인)
- ✅ silent-failure-hunter / system-architect / quality-engineer 3 페르소나 모두 CRITICAL 0건
- ⚠️ MAJOR 3건 — 모두 본 cap=2 범위 외 (logger 추상화 / JSDoc 1줄 / e2e 적격 위치 매핑) → 다음 세션 흡수 또는 명시 이연

본 정정의 핵심 강점:

- finally try/catch wrap = 양쪽 분기 (`removeHandlers` + `costMeter.finalize`) 독립 격리 → 한쪽 throw 가 다른 쪽 차단 X
- exit code 4/5 분리 = 운영자 retry 권한 (4: lock 해제 후 재시도) vs 무결성 위배 (5: 운영자 개입) 분류 명확

본 cap=2 정정은 직전 4-Pass 의 **CRITICAL 3건 중 LOW 난이도 2건** 만 처리. 남은 1건 (SF-C-2 SIGINT 도달 불가) + Q-M-2 (PipelineResult 확장) 은 진산님 결정 영역 (.claude/reports/decision-20260429-step11-6-after-cap2.md §3 권고) 답변 후 다음 작업 진입.

cap=2 정정 4-Pass 4번째 보고서 (review-gate.sh 형식) 작성 완료. Stop Hook 차단 해소.

---

**보고서 작성자:** Claude (Opus 4.7) — 메인 컨텍스트
**근거:** 3 독립 에이전트 보고서 통합 (silent-failure / system-architect / quality)
**다음 작업:** 진산님 결정 → cap=2 정정 commit (선택) → PipelineResult 확장 (결정 채택 시) → 9 AC e2e (§3.1 C~G)
