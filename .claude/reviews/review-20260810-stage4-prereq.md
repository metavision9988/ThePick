# 독립 리뷰 보고서 — STAGE 4 선결 ①·② (2026-08-10 변경분, 커밋 전)

- **일자**: 2026-08-10 · **리뷰 방식: 독립 에이전트 2렌즈** (자가 리뷰 아님, 레포 무수정 지시)
- **대상**: 미커밋 변경분 — 출처 청크 독립 재정의(`extract_source_quotes.py`) + LCS 임계 0.8→1.0
  (`packages/autoverify`) + 파일럿 러너 교차 검증 절 + 원장 3종(체크리스트·plan·08-08 리뷰 §9 추기)
- **선행**: `.claude/reviews/review-20260808-stage3-autoverify.md` §1·§2 가 STAGE 4 선결로 남긴 2건

> ⚠️ **이 파일의 생성 경위 자체가 발견 C-1 이다.** 메인이 `current.plan.md` 의 `G-REVIEW` 를
> "독립 리뷰 CRITICAL 0" 으로 ☑ 하면서 **이 경로를 증거로 인용했는데, 그 시점에 파일은 없었다.**
> 리뷰는 발주만 돼 있었다. 즉 증거를 예측으로 대체했고, 그것이 바로 08-08 리뷰 M-11 이 지적한 행위다.

---

## 렌즈 2 (Pass 3 ADVOCATE + Pass 4 CONTRACT) — 판정: **수정 필요 · CRITICAL 3 / MAJOR 8 / MINOR 5**

코드는 green 하고 **원장 수치는 전부 재현된다.** 문제는 다시 **주장의 강도**다 — 직전 사고와 같은 단어
("독립 확증")가 같은 구조(같은 원인의 두 표현)로 돌아왔고, 검증되지 않은 게이트가 ☑ 되었다.

### CRITICAL

| #   | 발견                                                                                                                           |
| :-- | :----------------------------------------------------------------------------------------------------------------------------- |
| C-1 | `G-REVIEW ☑ "독립 리뷰 CRITICAL 0"` 이 **존재하지 않는 파일**을 증거로 인용 — M-11 재발, 그것도 M-11 해소를 선언한 블록 안에서 |
| C-2 | held-out 없이 임계를 확정하고, 그 **정당화를 burned 표본 분포로** 썼다 — 세 문서가 금지한 행위                                 |
| C-3 | "3-1 이 주소 정합 검사기라는 **독립 확증**" 은 **순환** — 같은 페이지 창을 두 번 물은 것                                       |

**C-1 상세.** `current.plan.md:79` 가 `review-20260810-stage4-prereq.md` 를 인용했으나 `ls .claude/reviews/`
에 부재. 인용된 유일한 실존 증거(08-08 리뷰)는 스스로 "CRITICAL 4"라고 적는다. 그리고 **본 리뷰가
CRITICAL 3 을 내므로 선체크 값은 가정이 아니라 실증적으로 거짓**이다.

**C-2 상세.** 금지 문면 3곳이 **이번 diff 에서 미수정·미취소선**으로 남아 있다 —
08-08 리뷰 `:125`("held-out 동결 대상에 임계 2개뿐 아니라 제외 정규식 5종") · 체크리스트 `:255`
("임계 확정은 held-out 확보 후가 맞다") · plan §범위 외("파일럿 수치로 임계를 조율하면 burned 표본에 과적합").
그런데 이번 원장 근거는 전부 그 표본 분포다. ★**결정적 내부모순**: 같은 커밋이 "위조 검출력은 이 표본에서
**원리적으로 측정 불가**"라고 못박는다 — **검출력을 못 재는 표본으로 검출 임계를 정할 수는 없다.**

> 공정 기록(리뷰어 명시): `ratio===1 ⟺ 정규화 후 부분문자열` 이라는 **데이터 독립적 연역** 논거는 실재하고,
> **그 논거만 썼다면 위반이 아니다.** 위반은 값 선택이 아니라 **원장이 그 값을 burned 표본으로 정당화한 문장들**에 있다.

**C-3 상세.** 두 창이 같은 술어다 — `extract()` 1차 탐색 창 = `pdf_page −1…+3` / `source_chunk()` 창 =
`pdf_page −1…+2`, **청크 창 ⊂ 추출 창**이고 둘 다 유일 입력이 `node["pdf_page"]`. 집합 일치는
±1페이지 경계 케이스(0건 발생) 말고는 **구조적으로 보장**된다.

> 공정 기록: 수정 자체는 실재한다 — 08-08 의 `58/58 = 1.0` 항등식은 정말 사라졌고 실패 9건은 진짜 신호다.
> 정확한 진술은 "**항등식의 적용 범위를 적중분(49)으로 좁혀, 미적중 9건이 신호로 드러나게 만들었다**"이며,
> 적중분은 여전히 같은 페이지 텍스트의 부분문자열이라 1.000 이 구조적으로 보장된다.

### MAJOR

| #    | 발견                                                                                                                                                                                                                                       |
| :--- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-1  | 적대 픽스처가 "실 코퍼스 그대로"가 아니다 — 744자 = 실 청크(**6,541자**)의 **1/9 발췌**. 실 규모에선 위조에 **cov 1.0000**                                                                                                                 |
| M-1b | ★파생 구조 결함(원장에 없음): `lcs===1 ⟹ 부분문자열 ⟹ cov===1` ⇒ **3-3 통과하면 3-1 은 반드시 통과**. 3-1 이 3-3 에 **수학적으로 종속**되어 독립 축이 아니다                                                                               |
| M-2  | `APPROVAL_DASHBOARD.md` 미갱신 = **정본 이원화** (plan scope 가 동커밋 갱신 의무로 명시 · 직전 M-10 재발이자 이월 목록에서 무단 소멸)                                                                                                      |
| M-3  | reject 등급 ↔ 진앙 불일치 잔존 + 리포트 절 제목이 검수자를 엉뚱한 곳으로 보냄("카드 내용 재확인 필요"). 새로 생긴 주소 신호가 헤더에서 지워짐                                                                                              |
| M-4  | `G-S3-1` ☑ — 게이트 문면의 "지어낸 인용 = 임계 미달" 절반이 **미충족임을 인정하면서** 체크(표본 0건)                                                                                                                                       |
| M-5  | `G-S3-5` 체크박스 헤드라인이 **이미 철회된 문구**("적대 10종 10/10") 유지 — 각주만 붙어 게이트 목록만 훑으면 정정을 못 봄                                                                                                                  |
| M-6  | "실 데이터 비용 0"은 **이 표본 한정**. 표적인 **AI 저작 인용 코퍼스**에선 축자 부분문자열일 이유가 없어 → 3-3 전건 실패 → **전건 queue** → 검수 병목 해소와 정면 충돌. 리스크가 원장에 없음                                                |
| M-7  | plan §설계 결정 ②("`source_quote` 자체가 청크")·스크립트 헤더("조문 단위로 끊는다"·"창 ~4천자")가 이번 변경으로 **거짓이 됐는데 미수정** = Silent Pivot. 실측 청크 median **5,061자** · max **7,799자** · 조문 헤더 median **16.5개/청크** |
| M-8  | 청크가 조문 단위가 아니라 **48/58 인용이 다른 노드 청크에도 통짜로 들어있다** ⇒ 오인용이 "통짜로 존재" 확신 문구와 함께 **pass**. 직전 §2 문제가 failure→**pass 경로로 이동**                                                              |

### MINOR

- **m-1** 운영규칙 ③ 부분 위반 — 체크리스트 3-3 의 `(임계 0.8)` 이 취소선 없이 삭제, `현재→당시` 무표시 수정 (같은 커밋 3-6 은 규칙대로 취소선 — 일관성 결여)
- **m-2** "실 데이터 비용 **0**" 이 헤드라인 볼드라 일반 명제로 읽힌다(표본 한정은 부속절)
- **m-3** 러너가 "독립 확증" 문구를 **매 실행 리포트에 각인** → C-3 이 코드로 영속화
- **m-4** `docs/batch-load/stage2-source-quote/__pycache__/` 미-gitignore — 게이트 실행마다 untracked 산출물
- **m-5** `G-S3-3` 문면("재배열 위조는 … LCS 미달로 검출")이 임계 1.0 이후 부정확 — LCS 는 이제 **임의의 불일치 전부**를 잡는다. 검사 이름("짜깁기 탐지") ↔ 실제 의미(정확 포함) 불일치

### 정상 확인 — 리뷰어가 직접 재실행한 증거

| 원장 주장                                                                   | 재현                                                                          |
| :-------------------------------------------------------------------------- | :---------------------------------------------------------------------------- |
| api 859 · batch 332 · autoverify 37 · scripts 28 · turbo 18/18 ×3           | ✅ 전부 일치 (EXIT=0 ×3)                                                      |
| G-1 PASS · 추출 게이트 58/58 위반 0 + 제외 1 (len min38/median144/max707)   | ✅                                                                            |
| 파일럿 58 · pass 6 · queue 41 · reject 11 / 3-1 9 · 3-2 49 · 3-3 9 · 3-4 16 | ✅ **완전 재현**                                                              |
| 적중 49 = cov/lcs 1.000 · 미적중 9 = cov .300~.597 / lcs .074~.215          | ✅ JSON 전수 계산 일치                                                        |
| 변이: 임계 0.8 복귀 시 3건 red                                              | ✅ 사본에서 정확히 3 failed / 34 passed                                       |
| 카운터 21/31 · `source-quotes.json` diff 0 · 죽은 경로 삭제                 | ✅                                                                            |
| **production 무접촉(쓰기 0)** · L3 무접촉                                   | ✅ `wrangler`·`d1 execute`·`--remote`·DML **0건**. 쓰기는 로컬 리포트 2파일뿐 |

⚠️ **검증 불가 1건**: "0039·0045·0046·0047 전부 미적용" 은 **production 상태 주장**이라 레포에서
확증도 반증도 불가. 다만 이번 변경분에 적용 경로가 없다는 사실은 확인됨.

### 반론 (Devil's Advocate)

**Pass 3** — _"확신 문구는 이미 고쳐졌다"_: 부분적으로 맞다. 실패 메시지는 두 원인을 병기하고 pass 문구는
"원문"→"출처 청크"로 약화됐다. **개선은 실재한다.** 그러나 48/58 이 남의 청크에도 들어맞는 이상
"통짜로 존재"는 노드 귀속을 증거하지 않는다 — 문제가 사라진 게 아니라 **pass 경로로 이동**했다.
_리뷰어 자기반론_: M-3 을 "이번 커밋이 악화시켰다"고 쓰면 과하다 — 등급은 08-08 과 불변이다.

**Pass 4** — _"임계 1.0 은 held-out 없이도 정당하다"_: **이 반론은 성립한다.** 연역 논거만 썼다면 C-2 는 없었다.
_"G-REVIEW 는 발주해 둔 리뷰를 미리 적은 것뿐"_: 규칙 ②가 금지하고, **지금 이 리뷰가 CRITICAL 3 을
내므로 예측이 아니라 확인된 위반**이다.

### 이 커밋이 원장에 남기는 주장 — 건별 판정 (23건)

**사실 14건**: 청크 재정의·구 경로 삭제 / 실패 9건 복원 / 세 집합 일치(실측) / 08-08 항등식 반증 유효 /
실측 분포 / 주소 정합 자기한정 / `ratio===1 ⟺ 부분문자열` / "0.216~0.999 공백"(표본 한정) /
위조 서명(픽스처 한정) / `it.fails`→`it` 승격·변이 3 red / 회귀 수치 전건 / `source-quotes.json` diff 0 /
production 무접촉 / 카운터 21/31.

**과장 6건**: ④"독립 확증"(C-3) · ⑪"실 데이터 비용 0"(M-6) · ⑫"실 코퍼스 그대로"(M-1) ·
⑱"선결 ①·② 해소"(①=항등식 축소이지 제거 아님 / ②=부분) · ⑲`G-S3-1 ☑`(M-4) · ⑳`G-S3-5 ☑`(M-5).

**거짓 3건**: ⑩"임계 변경이 held-out 규율에 저촉되지 않는다"(C-2) · ㉑`G-REVIEW ☑`(C-1) ·
㉓"체크리스트↔대시보드 동기 유지"(M-2).

### 권고 처분 순서

1. **C-1** — `G-REVIEW` `[ ]` 원복 + 존재하지 않는 파일 인용 즉시 제거
2. **C-3** — "독립 확증"을 **4곳 전부**(러너 코드·리포트·체크리스트·§9)에서 철회, 두 창이 같은 술어임을 명시.
   진짜 독립 확증을 원하면 **인용 위치와 무관한 제3의 주소원**(노드 `book_page` 축 또는 조문 head 색인)이 필요
3. **C-2** — 임계 정당화를 **연역 논거 단독**으로 재작성(표본 분포 문장 제거), 또는 체크리스트 `:255` 에
   취소선 + 사유로 규율 변경을 **명시 상신**
4. **M-2** — 대시보드에 STAGE 3·08-10 행 등재(동커밋 의무)
5. **M-1/M-8** — 적대 픽스처를 실 청크(6,541자)로 교체하고 `cov 1.0000` 을 서명으로 고정 +
   `3-3 pass ⟹ 3-1 pass` 종속 관계를 원장에 명기
6. **M-3** — 인용 절단 진앙 8건의 처분을 `reject`→`queue`로 분리, 리포트 절 제목 수정

---

## 렌즈 1 (Pass 1 SURGEON + Pass 2 ARCHITECT) — 판정: **CRITICAL 1 / MAJOR 4 / MINOR 6**

### ★CRITICAL-1 — "실패 9건은 **전부** 주소 오류" 는 **거짓**. LAW-178 은 주소가 맞고 **인용이 틀렸다**

이번 diff 가 `verify-card.ts` 주석·`lcs.ts`·체크리스트·§9 **네 곳에 새로 못박은** 서술이 반증됐다.

```
LAW-158..165 상법  declared=22/24  actual=31,39,40   drift=+9~+16   ← 진짜 주소 드리프트 ✅
LAW-178      요령  declared=817    actual=[403]                     ← 주소는 옳다
```

선언 주소 **817 페이지에 진짜 제12조가 있다**(`제12조(손해평가 단위) ① 보험목적물별 …`).
그런데 저장된 인용 101자는 **교재 본문 p.403 의 서술문 조각**이었다:
`"제12조의 손해평가 / 단위별로 현지조사를 실시한다. 현지조사 내용은 <품목별 현지조사 종류 표>에서 …"`

**기전(후보 덤프로 확정)** — `NEXT_ARTICLE_RE` 의 `^\s*\d+\.\s+\S` 가 진짜 조문을 `1. 농작물 : 농지별`
에서 **43자로 절단** → `best[0] >= 60` 불충족 → 전 문서 스캔 폴백 → `max(len)` 이 **서술문 101자**를
조문 원문보다 우선 선택.

**영향 3중**: ① `how` 가 옳은 주소를 "미적중"으로 **무고** → 러너 오라클 오염 ② 이번 diff 가
**검증되지 않은 서사를 코드 주석과 원장 4곳에 새로 못박음** ③ ★그 인용이
`backfill-source-quote.sql` 에 실려 있고 **0047 은 `source_quote` 를 NULL→값 1회로 고정**한다 —
적용됐으면 **영구히 못 고친다**. EXCLUSIONS 가 스스로 적은 규율("빈 칸은 미검증으로 세지만
**틀린 원문은 STAGE 3 를 조용히 통과시킨다**")에 정확히 걸리는 케이스.

> 데이터 결함 자체는 **선재**(STAGE 2)다. 이번 diff 가 만든 것은 그 결함을 "전부 (b)"로 **덮은 주장**이며,
> 그 주장이 없었으면 3-1 실패 9건 검수 시 잡혔을 건이다.

### MAJOR (렌즈 1)

| #   | 발견                                                                                                                                                                                                                                          |
| :-- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| L-1 | 항등식은 제거가 아니라 **창 결합으로 축소**. 인용 1차 창 `[start−1, start+3]` ⊃ 청크 창 `[start−1, start+2]` ⇒ 1차 경로 인용은 청크의 부분문자열이 **구조적으로 강제**. `cov/lcs = 1.000 (49/49, 분산 0)` 은 측정이 아니라 **branch 의 함수** |
| L-2 | 임계 1.0 의 "실측 근거"인 **이봉 분포 자체가 L-1 의 산물**. "진짜 vs 위조"의 분포가 아니라 "같은 페이지 vs 다른 페이지"의 분포다                                                                                                              |
| L-3 | 적대 픽스처가 **744자 발췌**(실 청크 5,061자) — 실 규모에서 위조 cov **1.0000** = 3-1 완전 무력. 부수 확증: **48/58 인용이 다른 노드 청크에서도 `lcs===1`**                                                                                   |
| L-4 | Binary Gate 가 **리뷰 전에 `[x]`** (렌즈 2 C-1 과 동일 발견, 독립 수렴)                                                                                                                                                                       |

★**창 의존성 실증** (같은 데이터, 청크 창만 변경):

```
window   | chunk median | 실패 | 비고
 0,0     |  1,533 |  12 | 등식 깨짐 (12 ≠ 9)
-1,1     |  4,117 |   9 |
-1,2     |  5,061 |   9 | (현 설정)
-2,4     |  7,619 |   9 |
-5,5     | 10,919 |   9 |
-10,10   | 15,117 |   8 | LAW-158 회수
-30,30   | 36,297 |   1 |
FULL     |627,979 |   0 |
```

### MINOR (렌즈 1)

- **L-m1** **창 비대칭 미가드** — 인용은 `start+3` 까지 읽고 청크는 `+2` 까지. 조문이 `+3` 으로 넘어가면
  **주소가 맞아도** 실패 → 원장 규칙("실패가 늘면 인용·주소를 고쳐라")이 **정상 `pdf_page` 를 고치게 유도**.
  두 상수의 결합이 주석으로만 존재하고 이 불변식을 잡는 테스트가 없다
- **L-m2** `source_chunk()` 가 인용 실패 노드에도 호출 → `chunks` 가 `source-quotes.json` 에 없는 id 를 담을 수 있고, 출력 분모가 "인용된 노드"가 아니라 "제외 안 된 노드"
- **L-m3** `if chunk:` 로 빈 청크 **무음 드롭** — fail-closed 이되 fail-**loud** 아님
- **L-m4** 직렬화 불일치(스크립트 `indent=1` ↔ 커밋본 prettier) — 재실행 시 **의미 동일·바이트 상이**
- **L-m5** `how` 문자열이 Python 생산자 ↔ TS 소비자를 잇는 **암묵 계약**인데 스키마·테스트 0
- **L-m6** 성능: `lcsRatio` 최악 14.1ms(707자 × 5,666자), 58카드 240ms — 오프라인이라 무해하나 "Workers-safe" 표방과 긴장

### 렌즈 1 이 실행으로 **PASS** 판정한 것

불변성(`git hash-object` 동일) · 세 집합 일치(수치) · 적중 49건 전건 1.000 **anomalies 0** ·
임계 스윕(0.8~1.0 전 구간 실패 9 = 비용 0) · 변이 3건 red · **청크에 인용/인용 위치 직접 누출 0**
(입력 = `chapter`·`pdf_page`·`CHUNK_WINDOW` 뿐, `full.find(...)` 잔존 0) · 결정론 재현(샌드박스 재실행 전건 동치) ·
폭발 반경(`source-chunks.json` 소비자 = 러너 1곳, api/web/production 경로 0) · 정적 검사 · L3·production 무접촉.

★**반증 시도 결과 = 작성자 서사 유지**: 창을 `(−1,1)~(−5,5)` 로 넓혀도 **실패 9 고정**(넓은 plateau)이고
상법 실드리프트는 **+9~+16 페이지**로 실측됐다 — off-by-one 인공물이 **아니다**. 단 `(0,0)` 에서 12 로
깨지므로 등식은 창 의존이다(L-1).

---

## 처분 기록 (2026-08-10, 커밋 전 전건 처분)

### CRITICAL

| #             | 처분                                                                                                                                                                                                                 |
| :------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **L-CRIT-1**  | ✅ **근본 수리** — (A) `^\d+\.\s+\S` 경계 **삭제**(호 열거 오인) + (B) 헤드 `제N조(제목)` **괄호형 강제**. LAW-178 이 선언 주소 817 에서 진짜 제12조로 교정되고 `how` 무고도 해소. **파급 22건 전부 절단 복원 방향** |
| **C-1**       | ✅ `G-REVIEW` **`[ ]` 원복** + 존재하지 않는 파일 인용 제거 + **자기 위반 경위를 plan 에 영속**                                                                                                                      |
| **C-2**       | ✅ 임계 정당화를 **연역 단독**으로 재작성(`lcs.ts`), 표본 분포는 "부작용 확인"으로 격하. 체크리스트 held-out 규율에 **적용 범위 명시 + 진산 사후 거부권 상신** 문구 추가                                             |
| **C-3 / L-1** | ✅ "독립 확증" **4곳 전부 철회**(러너 코드·리포트 출력·체크리스트·§9). 두 창이 같은 술어임을 명시하고, 이 절의 값어치를 **회귀 감시**로 재정의                                                                       |

### MAJOR

| #         | 처분                                                                                                                                                             |
| :-------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M-1 / L-3 | ✅ 픽스처를 **실 청크 6,541자**로 교체(`__tests__/fixtures/law-173.ts`, 산출물 복사본·손편집 금지) + **위조 cov 1.0000 을 테스트로 고정**                        |
| M-1b      | ✅ **`3-3 통과 ⟹ 3-1 통과`** 종속을 테스트로 영속 + 러너 리포트가 매번 경고                                                                                      |
| M-8       | ✅ **노드 귀속 미검증**(48/58)을 테스트·러너 리포트에 영속                                                                                                       |
| M-3       | ✅ 리포트 `reject` 절 제목 수정 + "카드 오류 0 / 인용 부족 8 / 과탐 3" 재분류를 절 머리에 경고로 삽입. 등급 분리(`reject`→`queue`)는 **STAGE 4 이월**(원장 등재) |
| M-4 / M-5 | ✅ `G-S3-1` **`[~]` 부분 충족**으로 하향(위조 표본 0 · 3-1 종속 명시) · `G-S3-3` 문면 정정(m-5) · `G-S3-5` 각주 유지                                             |
| M-6 / L-2 | ✅ **표적 코퍼스 비용 미측정** 리스크를 `lcs.ts`·체크리스트에 영속("그때의 옳은 처분은 임계 인하가 아니라 생성 측에 축자 인용 강제")                             |
| M-2       | ⬜ **대시보드 갱신** — 아래 잔여                                                                                                                                 |
| M-7       | ⬜ plan §설계 결정 ② · 스크립트 헤더 규약 동기 — 아래 잔여                                                                                                       |
| L-4       | ✅ C-1 과 동일 처분                                                                                                                                              |

### MINOR

m-1 취소선 규칙 정합 ✅ / m-2 "실 데이터 비용 0" 표본 한정 명시 ✅ / m-3 러너 문구 철회 ✅ /
m-4 `__pycache__` **gitignore 등재** ✅ / m-5 `G-S3-3` 문면 ✅ /
L-m1·L-m2·L-m3·L-m4·L-m5·L-m6 ⬜ **STAGE 4 이월**(체크리스트 이월 목록 등재).

### 부수 — 게이트 자체의 결함 1건 (처분 중 발견)

근본 수리 후 꼬리 게이트 `G-S2-7f` 가 **LAW-172·179 를 오탐**했다. `부칙`·`[별표` 를 **위치 무관**으로
찾아서, 조문 본문의 **정상 상호참조**(`… 세부기준은 [별표 3]과 같다`)를 "삼켰다"로 판정한 것이다.
⇒ 추출기 경계 정의와 동일하게 **행 앵커**(`(?m)^\s*…`)로 정정.
★**탐지력 무손실 변이 검증**: LAW-183형 오염(별표 산식표 전문 + 부칙 + 푸터)을 주입하면
앵커 게이트가 **3건 전부 검출**. 아울러 `제7조 삭제`(괄호 없는 삭제 조문)를 경계에 추가.

### 최종 검증 (처분 후)

build 3/3 · test **18/18**(api 859 · batch 332 · quality 119 · web 79 · autoverify **39** · scripts 28) ·
typecheck 18/18 · lint 18/18 · G-1 PASS · 추출 게이트 **58/58 위반 0**(제외 1 유지) ·
변이 3건 red(임계) + 3건 검출(꼬리 오염) · **production 무접촉**(0039·0045·0046·0047 미적용, 백필 미실행).

파일럿 확정값: 대상 58 · pass **12** · queue **41** · reject **5** / 실패 3-1 **8** · 3-2 43 · 3-3 **8** · 3-4 7.
