# Step 16c — Pass 3 (Advocate) + Pass 4 (Contract) 독립 4-Pass 리뷰

**작성일:** 2026-05-01 ~10:22 KST
**리뷰 방식:** 독립 에이전트 (자가 리뷰 금지) — Pass 3+4 단일 에이전트, 코드 작성 컨텍스트 미보유. auto-review-protocol.md §"규칙 0" 최소 구성.
**리뷰자 컨텍스트:** 변경 파일 6건 + 연관 문서 전량 직접 Read 후 매핑.
**대상 step:** Engine Hardening Step 16c — 0016 마이그레이션 e2e + EXAM_IDS allowlist (assertValidExamId) + MINOR-PA1/PA2 흡수.

**리뷰 범위**

변경 파일 6건:

1. `apps/batch/__tests__/d1-trigger-verify.test.ts`
2. `apps/batch/__tests__/reproducibility-idempotency.test.ts`
3. `apps/batch/src/pipeline.ts`
4. `packages/shared/src/constants/exam-ids.ts`
5. `docs/plans/engine-hardening/step5-reproducibility-idempotency.plan.md`
6. `docs/plans/engine-hardening/ROADMAP.md`

연관 문서 4건:

- `.jjokjipge/handoff-session-024.md`
- `.claude/reviews/step16b-pass34-20260430-200133.md`
- `packages/shared/src/exam-adapter.ts`
- `docs/plans/engine-hardening/ROADMAP.md` §8

---

## Pass 3 (Advocate) — UX + 보안

✅ 14건 / 🔴 0건 / 🟠 0건 / 🟡 2건 / N/A 4건

### 확인 (PASS 증거 — 파일:라인 + 확인 내용)

**에러 UX**

1. `packages/shared/src/constants/exam-ids.ts:49-53` — `assertValidExamId` 에러 메시지 형태: `Invalid examId: '${candidate}'. Allowed values: ${ALL_EXAM_ID_VALUES.join(', ')}.` candidate 값과 허용 값 목록을 동시에 노출 — caller 가 즉시 수정 경로를 알 수 있다.

2. `apps/batch/src/pipeline.ts:418-423` — `assertValidExamId(ctx.examId)` 호출이 recover/INSERT 등 어떤 side-effect 도 일어나기 전 진입 첫 라인. invalid examId 진입 시 batch_runs 행 INSERT 0건 보장 — silent state 누출 0건. 5줄 주석에 throw 이유, 우회 경로, 근거 리뷰 번호까지 명시.

3. `apps/batch/__tests__/reproducibility-idempotency.test.ts:642-666` — brand 우회 임의 string examId throw 테스트. `'invalid-exam-id' as unknown as ExamId` 캐스팅으로 컴파일 타임 우회 명시적 재현. 에러 메시지 regex 3-way 검증.

4. `reproducibility-idempotency.test.ts:668-688` — 빈 문자열 케이스 별도 테스트. complementary coverage.

5. `reproducibility-idempotency.test.ts:690-710` — 정상 EXAM_IDS.SON_HAE_PYEONG_GA_SA 진입 시 통과 + qg2Passed=true 회귀 검증. false positive 방어.

**보안 — SQL injection 방어선**

6. `exam-ids.ts:23 + 36-38` — `ALL_EXAM_ID_VALUES` 컴파일 타임 고정 배열. `isValidExamId` `.includes(candidate)` 비교만 수행. 어떠한 SQL 도 실행하지 않으므로 `assertValidExamId` 자체가 SQL injection 을 만들 수 없다.

7. `pipeline.ts:456-464` — `assertValidExamId` 통과 후 `insertNewRun(ctx.examId, ...)` 진입. SQL injection 방어선이 `assertValidExamId` 에 올바르게 위치.

8. `exam-ids.ts:47-54` — 에러 메시지 candidate interpolation. examId 는 시험 식별 문자열(공개 값) — 사용자 PII 무관. 운영 로그 — 수험생 노출 없음. PII 노출 0건.

**상태 표현**

9. `pipeline.ts:417-423` — throw 시 recover 이전. batch_runs 고아 행 0건. 취소/롤백 로직 불필요.

10. `d1-trigger-verify.test.ts:420-430` — 게이트 ② PRAGMA 컬럼 검증.

11. `d1-trigger-verify.test.ts:432-441` — 게이트 ②: idx_knowledge_nodes_batch_source unique=1 + partial=1 확인.

12. `d1-trigger-verify.test.ts:490-506` — 게이트 ⑤: `<no_page>` fallback 다른 nodeId 비충돌.

13. `d1-trigger-verify.test.ts:508-524` — 게이트 ⑤: 동일 nodeId UNIQUE constraint failed ABORT 확인.

14. `d1-trigger-verify.test.ts:526-550` — idempotency: PART 2/3 부분 재실행 안전.

**N/A 항목**

- 오프라인 (Service Worker / IndexedDB): 본 변경은 server-side BATCH — 무관.
- 접근성: backend 로직 + 테스트 — UI 무관.
- 정답 안전: BATCH 파이프라인 인프라 — 수험생 문제 정답 데이터 무관.
- i18n: 운영자/개발자 대상 영문 — 수험생 노출 없음.

### MINOR — 반론 (Devil's Advocate)

🟡 **MINOR-P3-1: `assertValidExamId` 메시지에 candidate 그대로 노출 — 장기적 XSS 잠재**

내부 배치 파이프라인 Node.js 환경이라 현재 위험 없음. 미래 Admin Web API 에러 직렬화 시 raw HTML 렌더링 코드 등장 시 XSS 경로 가능성. sanitize 의무 동시 발생 권고.

- 신뢰도: 50 (현재 단계 위험 없음 — 미래 경로)
- 처리 시점: Admin Web API 에러 직렬화 구현 시 동시 처리

🟡 **MINOR-P3-2: `ALL_EXAM_ID_VALUES` 의 `ReadonlyArray<string>` widening — 타입 안전성 부분**

`Object.values(EXAM_IDS)` 반환 타입 `ExamId[]` 가 `ReadonlyArray<string>` 으로 widening. 런타임 동작 무관.

- 신뢰도: 40 (런타임 무관 — 타입 수준 minor)
- 처리 시점: Year 2 확장 또는 타입 강화 PR

---

## Pass 4 (Contract) — 기획 대조 Silent Pivot 탐지

✅ 22건 / 🔴 0건 / 🟠 0건 / 🟡 1건 / N/A 2건

### 확인 (PASS 증거 — 파일:라인 + 확인 내용)

#### 4.1 plan v1.4 §"Step 16c 진입 게이트" 5항목 정합

1. **게이트 ① (AC-RP-6 e2e — better-sqlite3 환경)** — `plan.md:167` ✅. `d1-trigger-verify.test.ts` 전 파일 better-sqlite3 사용. wrangler 미진입은 NG-1 명시 SKIP — 정당한 이연.

2. **게이트 ② (PRAGMA 컬럼/인덱스 존재 검증)** — `plan.md:168` "게이트 ② 3 tests" ✅. `d1-trigger-verify.test.ts:420-448` 3개 it 블록과 1:1 정합.

3. **게이트 ③ (partial UNIQUE 동작 3 tests)** — `plan.md:169` ✅. 라인 450-488 3 tests 정확 일치.

4. **게이트 ④ (0014 트리거 갱신 본문 검증)** — `plan.md:170` ✅. 기존 4 tests 검증됨 (라인 304-350).

5. **게이트 ⑤ (`<no_page>` fallback 2 tests)** — `plan.md:171` ✅. 라인 490-524 2개 it 1:1 정합.

#### 4.2 plan v1.4 §"v1.4 흡수 추가 항목" 5건 정합

6. **항목 6 — MINOR-PA2-m1** — ✅. `exam-ids.ts:36-53` 두 함수 신설, `pipeline.ts:417-423` 진입 첫줄 호출, 회귀 3 tests 정확.

7. **항목 7 — MINOR-PA1-m1** — ✅. `reproducibility-idempotency.test.ts:107-126` JSDoc tolerable_fields 4종 사유 명시.

8. **항목 8 — MINOR-PA1-m2** — ✅. `reproducibility-idempotency.test.ts:259-282` Map.set vs production D1 PK 차이 + 위임 검증 경로 크로스 레퍼런스.

9. **항목 9 — MAJOR-A1 NG-5 SKIP 유지** — `plan.md:178` ⏳ 명시 ✅. signal-handlers.test.ts + cost-meter-pipeline-kill.test.ts 분리 검증 위임 근거 명시.

10. **항목 10 — MINOR-A3 Step 18 위임** — `plan.md:179` ⏳ 명시 ✅.

#### 4.3 ROADMAP §8 Step 16c ✅ 표기 정합

11. **ROADMAP.md line 495** — 본 세션 6건 변경 파일 + 236/236 PASS 정합.

#### 4.4 handoff-024 §2.3 차세션 흡수 의무 7건 매핑 정합

12. MAJOR-A1 (NG-5 SKIP) — Silent Pivot 검증 §"시나리오 2" 참조 ✅
13. MAJOR-C1 (직전 세션 흡수) — ✅
14. MINOR-A3 (Step 18 위임) — Silent Pivot 검증 §"시나리오 3" 참조 ✅
15. MINOR-C2 (M-2 작성 시) — 명시 이연 정합 ✅
16. MINOR-PA1-m1 — ✅ 흡수 확인
17. MINOR-PA1-m2 — ✅ 흡수 확인
18. MINOR-PA2-m1 — ✅ 흡수 확인

#### 4.5 Hard Rules 위반 여부

19. Hard Rule 1 (`.env*` 미커밋) — N/A
20. Hard Rule 16 (시험 경계 강제) ✅ 0 위반 + Year 2 zero-cost
21. Hard Rule 17 (시험 ID 리터럴 단일 선언) ✅ 0 위반 (grep 검증)
22. ALL_EXAM_ID_VALUES Year 2 확장 zero-cost — `Object.values(EXAM_IDS)` 자동 반영 ✅

**N/A 항목**

- 수치/임계값 변경: 본 변경에 constants 수치 변경 없음
- 배치 순서: BATCH-1 진입 전 인프라 단계 — 무관

### 반론 (Silent Pivot 의심 시나리오 검증)

**시나리오 1: 게이트 ① wrangler d1 미진입 — Silent Pivot 인가 vs 정당한 NG-1 위임?**

판정: **Silent Pivot 아님.** plan v1.3 §NG-1 에 wrangler d1 SKIP 사유 명시 (`plan.md:374-394`). plan v1.4 게이트 ①에도 SKIP 사유 명시. handoff-024 §2.3 에 wrangler d1 관련 독립 이월 항목 없음 → 진산님도 NG-1 위임 인지 상태. wrangler d1 검증은 Phase 2 ADR-018 진입 시 별도 plan 의무로 정당하게 이연.

**시나리오 2: MAJOR-A1 NG-5 SKIP 유지 — handoff-024 §2.3 "Step 16c 진입 시" 명시와 정합인가?**

판정: **Silent Pivot 아님.** Step 16c 진입 시 "흡수 or 별도 plan 위임 결정"이 이루어졌고, plan v1.4 + ROADMAP 이 그 결정을 문서화. "Step 16c 진입 시" 처리가 "별도 plan 작성으로 결정"으로 해소. 단, 다음 handoff 에서 "NG-5 별도 plan 위임 확정 (Step 16c 에서 결정 완료)"으로 명확화 권고.

**시나리오 3: MINOR-A3 Step 18 위임 — handoff-024 §2.3 "Step 16c 또는 Step 18" 정합?**

판정: **Silent Pivot 아님.** "또는(or)" 조건의 한 선택지 채택. 사유 "logger 모듈 도입 동시 처리" — Step 18 cohesion 측면 합리적. 기획 이탈 0건.

### MINOR — 계약 미세 불일치

🟡 **MINOR-P4-1: ROADMAP §8 236/236 PASS 수치 독립 검증 불가**

신규 11건 (8 + 3) vs ROADMAP +12 차이 1건. 기존 파일 보강이 1건 추가 가능성. 본 리뷰에서 직접 카운트 cross-check 불가.

- 신뢰도: 45 (1건 차이 — 기능 결함 아님)
- 처리 시점: Step 18 CI 통합 자동 집계로 해소

---

## 판정 요약

```
── 4-PASS REVIEW (Step 16c Pass 3+4) ──────────────────
리뷰 방식: 독립 에이전트 1개 (Pass 3+4 통합) / 자가 리뷰 금지
리뷰 범위: 변경 파일 6개 + 연관 파일 4개

Pass 3 (Advocate): ✅ 14건 / 🔴 0건 / 🟠 0건 / 🟡 2건 / N/A 4건
반론:
  - MINOR-P3-1: assertValidExamId 메시지 candidate 노출 — Admin Web 직렬화 시 XSS 잠재
  - MINOR-P3-2: ALL_EXAM_ID_VALUES ReadonlyArray<string> widening (런타임 무관)

Pass 4 (Contract): ✅ 22건 / 🔴 0건 / 🟠 0건 / 🟡 1건 / N/A 2건
확인:
  - plan v1.4 §"Step 16c 진입 게이트" 5항목 × 코드 1:1 정합
  - handoff-024 §2.3 7건 매핑 전량 정합 (3건 ✅ + 4건 ⏳)
  - Hard Rule 16/17 0 위반 + Year 2 zero-cost
반론:
  - 3개 Silent Pivot 시나리오 모두 정당한 이연 (Silent Pivot 아님)
  - MINOR-P4-1: 236/236 수치 독립 검증 불가 (기능 결함 아님)

판정: 완료 가능
────────────────────────────────────────────────────────
```

**CRITICAL 0건 → "완료" 선언 가능** (auto-review-protocol.md §"규칙 4" 충족)

### 차세션 이월 트래킹 항목

| ID                | 내용                                                              | 처리 시점                         |
| ----------------- | ----------------------------------------------------------------- | --------------------------------- |
| MAJOR-A1          | runBatchWithKill 실제 kill e2e                                    | NG-5 별도 plan                    |
| MINOR-A3          | recover.ts logger.info 강화                                       | Step 18 logger 모듈 도입 시       |
| MINOR-C2          | formula_AST / edge_dependency_graph e2e 미커버                    | master-test-checklist v1          |
| MINOR-P3-1 (신규) | assertValidExamId 메시지 candidate 노출 — 미래 Admin Web XSS 잠재 | Admin Web API 에러 직렬화 구현 시 |
| MINOR-P4-1 (신규) | ROADMAP 수치 독립 검증 불가                                       | Step 18 CI 자동 집계로 해소       |

본 리뷰에서 발견된 신규 CRITICAL/MAJOR 0건.

---

**리뷰자:** Claude Sonnet 4.6 — Pass 3+4 독립 위임 (코드 작성 컨텍스트 미보유)
**리뷰 깊이:** 변경 파일 6건 전량 직접 Read + 연관 문서 4건 Read + 3개 Silent Pivot 시나리오 개별 검증
**반론 의무:** Pass 3 2건 + Pass 4 3건 Silent Pivot 검증 + 1건 MINOR — auto-review-protocol.md §"규칙 3" 충족
**증거 기반 의무:** Pass 3 14건 + Pass 4 22건 — "규칙 2" 충족
