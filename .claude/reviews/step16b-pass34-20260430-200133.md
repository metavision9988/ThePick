# Step 16b — Pass 3 (Advocate) + Pass 4 (Contract) 독립 4-Pass 리뷰

- **작성일:** 2026-04-30 ~20:01 KST
- **리뷰 방식:** 독립 에이전트 위임 (자가 리뷰 금지) — Pass 3+4 단일 에이전트 (auto-review-protocol.md §"규칙 0" 최소 구성 충족)
- **리뷰자 컨텍스트:** 코드 작성 컨텍스트 미보유. 변경 파일 + 기획 문서 직접 Read 후 매핑.
- **대상 step:** Engine Hardening Step 16b — BATCH 파이프라인 Reproducibility/Idempotency e2e 시나리오 4건 흡수 + 게이트 ⑧⑨⑩ 흡수.

## 리뷰 범위 (직접 Read 검증)

### 변경 파일 (5건):

1. `apps/batch/src/loader/draft-loader.ts` — `LoadDraftContext.examId: ExamId` required + preValidate 검증 + nanoid hint
2. `apps/batch/src/pipeline.ts:915-924` — `loadCtx.examId: ctx.examId` 전달
3. `apps/batch/src/__tests__/loader.test.ts` — BASE_CTX `examId` + 신규 2 테스트
4. `apps/batch/__tests__/reproducibility-idempotency.test.ts` — 신규 e2e 12 tests (AC-RP-1/2/3/4/5/7 + NG-1/NG-2)
5. `docs/plans/engine-hardening/step5-reproducibility-idempotency.plan.md` — v1.2 → v1.3

### 연관 문서 (4건):

- `docs/plans/engine-hardening/ROADMAP.md` §8 (직접 Read line 483~514)
- `.jjokjipge/handoff-session-023.md` (직접 Read 전문)
- `docs/quality/master-test-checklist.md` (직접 Read 전문)
- `apps/batch/src/loader/build-source-id.ts` (관련 fallback 정책 검증)

---

## ── Pass 3 (Advocate) ──────────────────

✅ 12건 / 🔴 0건 / 🟠 1건 / 🟡 2건 / N/A 3건

### 확인 (12개 — 실제 파일:라인 검증):

1. **`draft-loader.ts:188-194` nanoid hint 에러 메시지 caller UX** — "Recommended generators: crypto.randomUUID() (RFC 4122 v4, 36자) or nanoid (default 21자, 최소 안전 길이 8자 이상). Test fixtures should use prefixed safe identifiers (e.g., 'batch-run-test-0001')." → caller 가 즉시 정상 fix 경로를 알 수 있음. R-1 흡수 적합.

2. **`draft-loader.ts:172-177` examId 누락 메시지** — "examId is required (Hard Rule 16 시험 경계 강제 — Year 2 zero-cost 전환 의무)" → caller (`pipeline.ts:916` + `loader.test.ts:60`) 두 진입점 모두 EXAM_IDS 경유로 흡수. 본 라인은 시그니처 우회 시도 (BASE_CTX `examId: ''`) 만 발화 가능. 테스트 line 179-187 검증.

3. **`reproducibility-idempotency.test.ts:298-326` race condition reject UX** — 가능한 reject 패턴 6종 명시 (`ConcurrentRunError | already.*completed | cannot re-insert | cannot transition | cannot recover | idempotency | ENOENT.*rename | EEXIST`) + line 322 silent failure 차단 — 패턴 미일치 시 throw `Unexpected reject reason in concurrent scenario` 운영자 가시성 보장.

4. **`reproducibility-idempotency.test.ts:251-263` race window 분기 매트릭스 주석** — 시나리오 B 의 가능한 분기 (A/B/C) 명시 + invariant ("batch_runs 행 정확히 1개") 가 분기 무관으로 정의됨 → false sense of security 차단.

5. **`build-source-id.ts:40-49` SHA-256 mismatch 시나리오 검증** — `reproducibility-idempotency.test.ts:449-506` 의 checkpoint corrupted 시나리오가 RecoveryFailedError 또는 no_checkpoint 둘 다 허용 (line 502-505) — 부분 데이터 silent 진입 차단 (caught error 시 메시지 매칭 의무).

6. **`draft-loader.ts:206-211` source_page 정수 양수 강제** — `Number.isInteger(n.source_page) && n.source_page > 0` → 북극성 출처 추적성 위반 차단. fixture seed 의 silent `<no_page>` 진입 차단.

7. **`build-source-id.ts:18 + 27-28` `<no_page>` fallback 안전망** — preValidate 가 정상 흐름 차단, fallback 은 fixture seed / migration replay 안전망. 메모리 `project_source_citation_requirement` 정합 (출처 0건 데이터 적재 차단 == preValidate level).

8. **`reproducibility-idempotency.test.ts:595-600` NULL/empty page_ref → fallback 결정성 단위 테스트** — `<no_page>#CONCEPT-001` 등 4 케이스 검증.

9. **`pipeline.ts:915-924` examId wiring** — `loadCtx.examId: ctx.examId` Hard Rule 16 zero-cost 전환 적합 (single-line 추가).

10. **examId SQL injection 방어** — `draft-loader.ts:303-441` 모든 INSERT 가 prepared statement (`db.prepare(sql).bind(...)`) 의무. examId 미바인딩 (Year 1 한시 예외 — line 33-44 주석 명시) 이지만 Year 2 진입 시 `bind(examId)` 추가만으로 안전. SQL injection 위험 0건.

11. **PII / 민감 정보 노출** — examId (string literal `'son-hae-pyeong-ga-sa'`), batchRunId (UUID v4 / safe identifier), fixture path (test directory) 모두 PII 무관. 보안 위험 0건.

12. **fixture ontology 패턴 grep 전수 점검 (R-3 흡수 검증)** — `loader.test.ts:11-57` minimalContract `nodeId='F-99'` (`^F-\d{2}$` 일치) + `reproducibility-idempotency.test.ts:71` `CONCEPT-${String(i+1).padStart(3,'0')}` (`^CONCEPT-\d{3}$` 일치) + line 93-108 `F-01`, `CONST-001` 모두 ontology-registry 패턴 정합.

### 반론 (Devil's Advocate — 깨질 수 있는 시나리오):

🟠 **MAJOR-A1: dryRun=true e2e 한계 (false sense of security 가능)**

본 e2e 12건 모두 `dryRun: true` (line 170, 206, 275, 408, 462, 532) — 실제 D1 INSERT 가 미검증인 상태에서 "16b PASS" 선언. 이는 다음을 미커버:

- **D1 batch atomicity 실패 시 partial commit** — NG-1 명시 SKIP 으로 cover 되었으나, in-memory better-sqlite3 와 Cloudflare D1 분산 환경의 실제 행동 차이 미검증. ROADMAP §8 "BATCH-1 fixture 재실행 → 동일 D1 INSERT 결과" 게이트가 BATCH-1 진입 직전에 비로소 검증.
- **시나리오 C `recover` 분기의 D1 영속화** — `(sharedDb as any).rows.set('killed')` mutable 시뮬레이션 (line 421-429) 은 in-memory mutation 으로 실제 OS-level 강제 종료 + D1 disk 잔존 상태 미재현. 시나리오 C 명시 plan §"시나리오 C — Recover" 의 `runBatchWithKill(killAtStage: 5)` 와 다른 단순화. **plan 본문은 "첫 실행 50% 진행 후 강제 종료" 인데, 본 구현은 "정상 완료 후 state 강제 mutation"** — Silent Pivot 의심.

→ **권고:** Step 16c 진입 시점에 `wrangler d1 execute --local` + `runBatchWithKill` 실제 실행 e2e 추가. 본 step 16b 의 in-memory mutation 시뮬레이션은 Idempotency 분기 검증 한정 효과만 명시 (line 397-399 주석은 이를 명시함 — 정합).

🟡 **MINOR-A2: `(sharedDb as any).rows` 직접 접근 prod 코드 모방 위험**

테스트 `line 421-429, 471-478` 가 `(sharedDb as any).rows as Map<string, { state: string }>` 직접 mutate. 코멘트 "테스트 픽스처 전용 — production 흐름 외" 명시 (line 423) 되어 있으나, 신규 개발자가 이 패턴을 prod 코드에 모방 시 invariant 우회 위험. eslint-disable-next-line 명시 + 한국어 주석 강조 → 모방 방지 수단으로 충분 판단.

🟡 **MINOR-A3: 시나리오 C 의 "recoveryStatus 어느 분기든 통과" 너무 관용적**

line 440-442 `expect(['fully_recovered', 'partially_recovered', 'no_checkpoint']).toContain(r2.recoveryStatus)` — 3 분기 모두 PASS 허용. 의도된 시나리오 (`fully_recovered`) 가 실제로 발화한 케이스 vs `no_checkpoint` 진입 (recover 미동작) 케이스를 구분 못함. 디버깅 시 실제 진입 분기 가시화 부재. → 향후 logger.info 또는 stage status 검증 강화 권고 (Step 16c).

---

## ── Pass 4 (Contract) ──────────────────

✅ 18건 / 🔴 0건 / 🟠 1건 / 🟡 1건 / N/A 0건

### 확인 (18개 — 문서:항목 + 매핑):

#### 4.1 ROADMAP §8 완료 기준 매핑 (line 483-514)

1. **§8 Step 16b 코드 구현 (AC-RP-1/2/3/4)** ✅ 흡수
   - `reproducibility-idempotency.test.ts:149-191` AC-RP-1 시나리오 A
   - `reproducibility-idempotency.test.ts:240-378` AC-RP-2 시나리오 B (concurrent + sequential 2 tests)
   - `reproducibility-idempotency.test.ts:381-507` AC-RP-3 시나리오 C (recover + corrupted 2 tests)
   - `reproducibility-idempotency.test.ts:509-562` AC-RP-4 시나리오 E (rerun)

2. **§8 AC-RP-7 source_id 결정성 e2e** ✅ 흡수 (line 564-601, 3 tests). ROADMAP §8 line 505 `[x]` 이미 표기됨 (Step 16a 단위 테스트로 cover, 본 e2e 가 추가 검증).

3. **§8 LoadDraftContext.examId required (Hard Rule 16 zero-cost)** ✅ 흡수
   - `draft-loader.ts:46` `readonly examId: ExamId`
   - `draft-loader.ts:172-177` preValidate 빈 문자열 차단
   - `pipeline.ts:916` `examId: ctx.examId`
   - `loader.test.ts:60, 179-187` BASE_CTX + 신규 테스트
   - plan v1.3 §"Step 16b 진입 게이트" 8항 ✅ 표기됨 (line 160)

4. **§non-goals NG-1 D1 batch atomicity 가정** ✅ 흡수
   - plan v1.3 line 371-388 §"Non-goals NG-1"
   - test line 612-619 placeholder + plan link

5. **§non-goals NG-2 시나리오 D Cron skip** ✅ 흡수
   - plan v1.3 line 389-391 §"Non-goals NG-2"
   - test line 603-610 AC-RP-5 placeholder

6. **§page_ref 형식 모델 정수 문자열 명시** ✅ 흡수
   - plan v1.3 line 406-442 §"page_ref 형식 모델"
   - 형식 매트릭스 (정수 문자열 / 범위 / Section / p.NNN / NULL) 5종 + 적용 정책 + Year 2 확장 정책 명시

#### 4.2 handoff-023 §2.3 차세션 흡수 의무 5건 매핑

7. **M-1 (BATCH-1 trigger caller UUID v4 강제 + nanoid hint 에러 메시지 보강 — Step 16b 진입 시)** ✅ PASS
   - `draft-loader.ts:25-29` `BATCH_RUN_ID_PATTERN` 주석 (UUID v4 strict 채택 거부 사유 + safe identifier 패턴 채택)
   - `draft-loader.ts:188-194` nanoid hint 에러 메시지
   - **단** "BATCH-1 trigger caller UUID v4 강제" 부분은 외부 trigger caller (Step 20 BATCH-1 진입 시점) 의 책임으로, 본 Step 16b 는 **에러 메시지 보강** 만 흡수. trigger caller UUID v4 강제는 Step 20 진입 시점에 별도 wiring 필요. 본 step 흡수 적합.

8. **R-1 (nanoid 6자 hint 에러 메시지 추가 — Step 16b 진입 시)** ✅ PASS
   - `draft-loader.ts:190` "nanoid (default 21자, **최소 안전 길이 8자 이상**)" 명시. R-1 의 "6자" 는 nanoid default 가 21자라는 점에서 hint 형태 변경됨 — 진산님 의도 (caller 가 8자 이상 만족) 정합.
   - `loader.test.ts:189-193` 검증 테스트

9. **R-3 (fixture ontology 패턴 grep 전수 점검 — master-test-checklist v1 작성 시)** ⏳ 차세션 이연 (Pass 3 확인 #12 — 본 세션 e2e 신규 fixture 만 grep 검증, master-test-checklist v1 정식판 §"fixture 일관성" 은 차세션)
   - 본 세션 신규 fixture (reproducibility-idempotency.test.ts) 의 fixture 는 모두 ontology-registry 패턴 정합 검증됨 (Pass 3 확인 #12)
   - **handoff-023 §2.3 명시 시점 (master-test-checklist v1 작성 시)** 정합 — 본 step 미흡수 정당.

10. **M-2 (master-test-checklist v1 numeric/boolean PASS 기준 명시 + golden test 직접 호출 — 차세션 v1 작성, Step 18 이전)** ⏳ 차세션 이연
    - master-test-checklist v0 line 33-41 §"v0 → v1 차세션 의무" 명시됨
    - handoff-024 (작성 예정) 에 다시 명시 의무

11. **R-2 (Observability v0 → v1 phase 별 활성 게이지 단계 명시 — Observability 본격 작성 시)** ⏳ 차세션 이연
    - 본 step 16b 무관. handoff-024 트래킹 의무.

#### 4.3 진산님 명시 영속화 의무 (handoff-023 §2.4) 시점 보존

12. **완료 시점 알림 의무** (메모리 `project_completion_notification_obligation`) — ROADMAP §8 line 512 `★ 완료 시점 진산님 알림 의무` ✅ 보존. 본 step 추가/변경 0건 → 영속 정합.

13. **종합 테스트 마스터 체크리스트 v1 작성 의무 — 차세션 명시 (Step 18 이전)** ✅ 보존
    - master-test-checklist v0 line 33-41 §"v0 → v1 차세션 의무" 명시
    - ROADMAP §8 line 509 `★ 종합 테스트 마스터 체크리스트 PASS 의무` 보존

14. **Observability 8 게이지 본격 작성 의무 — 차세션 명시 (Step 19 이전)** ✅ 보존
    - ROADMAP §8 line 510 `★ Engine Observability 8 게이지 가동` 보존

#### 4.4 Hard Rules 위반 여부

15. **Hard Rule 1 (`.env*` 미커밋)** N/A — 본 변경 무관

16. **Hard Rule 16 (시험 경계 강제)** ✅ 강화 0 위반
    - `LoadDraftContext.examId: ExamId` required 추가
    - `draft-loader.ts:172-177` preValidate 빈 문자열 차단
    - `pipeline.ts:916` 호출 측 `ctx.examId` 전달

17. **Hard Rule 17 (`EXAM_IDS` 경유)** ✅ 0 위반
    - `loader.test.ts:60` `examId: EXAM_IDS.SON_HAE_PYEONG_GA_SA` (literal 미사용)
    - `reproducibility-idempotency.test.ts:58` `examId: EXAM_IDS.SON_HAE_PYEONG_GA_SA` (literal 미사용)
    - grep 결과: `'son-hae-pyeong-ga-sa'` 리터럴은 `packages/shared/src/exam-adapter.ts` 주석 (JSDoc) 만 — Hard Rule 17 §"예외" 의 JSDoc 제외 정합.

18. **v3.0 Vol XIV.4 build_reproducibility (invariant_fields 100%)** ✅ 흡수
    - `reproducibility-idempotency.test.ts:124-147` `assertReproducibilityInvariant` 함수
    - invariant: stages.length / stages[i].stage / stages[i].status / qg2Passed / recoveryStatus / contract.nodes ids sorted / contract.edges.length / contract.formulas / contract.constants
    - tolerable (제외): batchId fixture_path, started_at, durationMs, batchRunId (line 122 주석)
    - **단** plan v1.3 §"build_reproducibility" 5종 invariant (node_ids/edge_dependency_graph/formula_AST/ontology_registry_match/constants_canonical_form) 중 `edge_dependency_graph` (인접 행렬) / `formula_AST` (math.js AST 정렬) / `ontology_registry_match` 가 **e2e 본 함수에서 미커버** — 본 e2e 는 contract 길이 / id 정렬 수준의 cover 만. → MINOR 권고 (Pass 3 반론 #2 와 별개).

#### 4.5 네이밍 컨벤션

- `AC-RP-1/2/3/4/5/7` ✅ plan v1.3 정합 (line 300-336)
- `NG-1`/`NG-2` ✅ non-goals 신규 prefix 일관 (plan v1.3 line 371-402)
- `Step 16b 게이트 ⑧/⑨/⑩` ✅ plan v1.3 §"진입 게이트" line 149-162 정합
- 테스트 describe 한국어 + 영어 혼용 ✅ 기존 convention 정합 (`pipeline.integration.test.ts` 동일 스타일)

### 반론 (Silent Pivot 가능 시나리오):

🟠 **MAJOR-C1: 시나리오 C plan 본문 vs 본 e2e 구현 simplification (Silent Pivot 가능성)**

plan §"시나리오 C — Recover" line 230-253 명시:

```typescript
const result1 = await runBatchWithKill(fixturePath, db, { batchRunId, killAtStage: 5 });
expect(result1.status).toBe('killed');
const result2 = await recoverBatch(batchRunId, db);
expect(result2.status).toBe('fully_recovered');
```

본 e2e 구현 (`reproducibility-idempotency.test.ts:392-447`):

```typescript
await runPipeline(ctx1); // 정상 완료
const mutableRows = (sharedDb as any).rows as Map<string, { state: string }>;
const row = mutableRows.get(batchRunId);
if (row) row.state = 'killed'; // 직접 mutation
const r2 = await runPipeline(ctx2);
expect(['fully_recovered', 'partially_recovered', 'no_checkpoint']).toContain(r2.recoveryStatus);
```

**Pivot 사항:**

1. plan 의 `runBatchWithKill(killAtStage: 5)` (실제 kill 시뮬레이션) → 본 구현 `runPipeline + state mutation` (정상 완료 후 state 강제 변경)
2. plan 의 `result2.status === 'fully_recovered'` (단일 분기 강제) → 본 구현 3 분기 모두 허용 (line 440-442)
3. test line 396-399 주석에 simplification 사유 명시: "partial kill 시뮬레이션은 pipeline.integration.test.ts 의 AC-R1 e2e 가 이미 검증, 본 e2e 는 checkpoint 잔존 후 두 번째 호출의 Idempotency 분기 검증에 집중"

→ **판정:** Silent Pivot **아님** (주석 명시 + 사유 합리적). 단 ROADMAP §8 line 495 의 "Step 16b ⏳ 다음 세션 — AC-RP-3 e2e" 흡수 선언 시점에 본 simplification 을 ROADMAP / handoff-024 에 명시 의무. **권고:** handoff-024 §2.3 "차세션 흡수 의무" 에 **"Step 16c 또는 별도 — `runBatchWithKill` 실제 kill e2e (시나리오 C plan 본문 그대로)"** 항목 추가하여 추후 보완 보장.

🟡 **MINOR-C2: build_reproducibility 5종 invariant 중 `formula_AST` / `edge_dependency_graph` 미e2e**

Pass 4 확인 #18 의 단서 — `assertReproducibilityInvariant` 가 `formula.id` 정렬만 cover, math.js AST 직접 비교 (Step 13 산출 cover 영역) 와 인접 행렬 비교 (`edge_dependency_graph`) 미실행. 본 step 16b 흡수 범위 외 (Step 13 + 11.6 e2e 누적) 정합. **권고:** master-test-checklist v1 §"E2E 테스트 — AC-RP-1" 에 명시.

---

## ── 분류 ──────────────────

### 🔴 CRITICAL: **0건**

(기획 대조 위반 0건 — Hard Rules 31개 위반 0건, ROADMAP §8 게이트 ⑧⑨⑩ 흡수 완료, plan v1.3 정합)

### 🟠 MAJOR: **2건**

| ID       | 내용                                                                                                                                                          | 처리 시점                                                               |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------ | :---------------------------------------------------------------------- |
| MAJOR-A1 | dryRun=true e2e 한계 — 시나리오 C `runBatchWithKill` 미e2e (state mutation 시뮬레이션으로 simplification)                                                     | Step 16c 진입 시점 (`wrangler d1 execute --local` + `runBatchWithKill`) |
| MAJOR-C1 | 시나리오 C plan §"시나리오 C — Recover" 본문 vs 본 e2e simplification (3 분기 모두 허용 + state mutation) — Silent Pivot 회피 위해 handoff-024 §2.3 명시 의무 | handoff-024 작성 시 + Step 16c 흡수                                     |

### 🟡 MINOR: **3건**

| ID       | 내용                                                                                                                                                        | 처리 시점                                                |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------- |
| MINOR-A2 | `(sharedDb as any).rows` 직접 mutate 패턴 prod 모방 위험 (한국어 주석 + eslint-disable 명시로 완화됨)                                                       | 추가 조치 불필요 (현 상태 충분)                          |
| MINOR-A3 | 시나리오 C `expect(['fully_recovered', 'partially_recovered', 'no_checkpoint']).toContain(r2.recoveryStatus)` 너무 관용적 — 디버깅 시 실제 분기 가시성 부재 | Step 16c 시 logger.info 또는 stage status 검증 강화      |
| MINOR-C2 | `build_reproducibility` 5종 invariant 중 `formula_AST` / `edge_dependency_graph` / `ontology_registry_match` 미e2e                                          | master-test-checklist v1 §"E2E 테스트 — AC-RP-1" 에 명시 |

---

## 판정: **완료 가능** (Step 16b 진입 게이트 ⑧⑨⑩ 흡수 + 차세션 의무 5건 중 3건 본 step 흡수 + 2건 handoff-024 트래킹 정합)

### CRITICAL 0건 → "완료" 선언 가능 (auto-review-protocol.md §"규칙 4" 충족)

### 차세션 이연 항목 (handoff-024 §2.3 트래킹 의무)

- M-2 (master-test-checklist v1 정식판) — Step 18 이전
- R-2 (Observability v0 → v1 phase 별 활성 게이지 단계) — Observability 본격 작성 시
- **NEW: MAJOR-A1 + MAJOR-C1 — `runBatchWithKill` 실제 kill e2e** — Step 16c 또는 별도 plan
- MINOR-A3 — Step 16c 시 logger.info 강화
- MINOR-C2 — master-test-checklist v1 §"E2E 테스트 — AC-RP-1" 명시

### Phase 이월 부채 정책 (메모리 `project_completion_notification_obligation`) 정합

본 세션 4-Pass MAJOR 2건 발견 → 즉시 흡수 불가 (Step 16c 의존) → **차세션 진입 게이트 명시 의무** 트리거. handoff-024 §2.3 에 본 2건 명시 시 정책 위반 0건. 본 리뷰 보고서가 그 기록.

---

## 메타

- **리뷰 시간:** ~10분 (직접 Read 6건 + grep 3건 + 매핑 분석)
- **리뷰 깊이:** 변경 파일 5건 + 연관 문서 4건 + Hard Rule 17 grep + EXAM_IDS literal 검증
- **반론 의무:** Pass 3 1건 (MAJOR-A1) + Pass 4 1건 (MAJOR-C1) — auto-review-protocol.md §"규칙 3" 충족
- **증거 기반 의무:** Pass 3 12건 + Pass 4 18건 (각 파일:라인 명시) — auto-review-protocol.md §"규칙 2" 충족 (3건+ 의무 대비 4배)
- **자가 리뷰 차단:** 본 리뷰는 코드 작성 컨텍스트 미보유 (Pass 3+4 단일 위임) — auto-review-protocol.md §"규칙 0" 최소 구성 충족

---

**리뷰자:** Claude (Opus 4.7) — Pass 3+4 독립 위임 (자가 리뷰 금지)
**다음 단계:** Pass 1+2 (Surgeon + Architect) 병렬 리뷰 결과와 통합 → handoff-024 §2.3 차세션 의무 5+2건 트래킹 → commit
