# 시나리오 테스트 파일 독립 리뷰 — "AI 자기참조 방어" 효과 평가

**일시:** 2026-04-22 17:18 KST
**리뷰 방식:** 독립 에이전트 (quality-engineer, 메인 대화 외부 위임, 자가 리뷰 아님)
**리뷰 대상:** `apps/api/src/__tests__/scenarios.test.ts` (783줄, 16 시나리오) + `scenarios-20260422-171251.md` 리포트
**배경:** 진산님이 "AI 가 짠 테스트를 AI 가 짠 코드로 통과하는 자기참조" 우려를 제기 → 시나리오 파일로 대응 → **본 리뷰는 그 대응이 실제로 효과적이었는지 평가**

---

## 최종 판정 요약

| 항목                    | 결과                                    |
| ----------------------- | --------------------------------------- |
| Critical                | **2건**                                 |
| Major                   | **6건**                                 |
| Minor                   | **4건**                                 |
| AI 자기참조 방어 신뢰도 | **6/10** (리포트 주장 "초록불" 은 과잉) |

**결론**: 시나리오 파일은 **AI 자기참조를 부분적으로만 깬다**. Fake D1 이 실제 D1 의 15개 트리거 중 5개만 재현 → **프로덕션에서만 깨지는 경로 존재**. 리포트 "사상누각 위험 없음" 은 과장.

---

## Critical (즉시 인식 필요) — 2건

### 🔴 C-1. Fake D1 의 INSERT 방어 트리거 4종 미재현

- 실제 `migrations/0009_sessions.sql:51-78` 에는 `user_id not_empty`, `refresh_token_hash not_empty`, `refresh_token_hash length=64`, `expires_at not_empty` 4개 BEFORE INSERT 트리거
- `scenarios.test.ts:114-138` Fake DB 의 sessions INSERT 핸들러는 **UNIQUE 검사만** 시뮬레이션
- **영향**: 코드에 버그가 생겨 빈 user_id 또는 64자 아닌 hash 를 바인딩해도 → Fake 는 통과 / 프로덕션 D1 은 `RAISE(ABORT)` 실패
- **즉 "시나리오 통과" 가 "프로덕션 안전" 을 보장하지 않는다**

### 🔴 C-2. Fake D1 의 UPDATE 불변 + revoked_at 단방향 트리거 미재현

- `migrations/0009:85-122` 의 immutable 트리거 4종 + `revoked_at_one_way` 미재현
- `scenarios.test.ts:663-665` (S8 시나리오) 에서 **`s.revoked_at = pastIso` 로 직접 메모리 객체를 조작** — 프로덕션 D1 이 ABORT 하는 행위
- **즉 S8 은 "실제로 일어날 수 없는 상태" 를 만든 뒤 검증** — 공격 시나리오 재현으로는 부족

---

## Major 6건

### 🟠 M-1. webhook_events status 전이 + auto_processed_at 트리거 미재현 (`migrations/0008:84-103`)

Step 1-5 이후 status 업데이트 로직 추가 시 **Fake 만 통과하고 D1 실패**하는 회귀 가능성.

### 🟠 M-2. webhook_events status enum 검증 미재현 (`migrations/0008:73-78`)

현재 라우트가 'received' 고정이라 우연히 맞음. 상수 오타 silent drift 탐지 불가.

### 🟠 M-3. Access JWT 검증 경로 시나리오 **0건**

`session.ts:92-132` `verifyAccessToken` — clock skew, exp/iat, malformed/expired/invalid 분기 전부 핵심 보안이나 시나리오 미커버. 리포트 "지금 확실히 되는 것" 14개 중 **access 토큰 검증은 실제로는 검증된 적 없음**.

### 🟠 M-4. 핵심 보안 기능 5종 누락

- `WEBHOOK_WEAK_SECRET` (secret < 32B → 500) `payment.ts:307-313`
- rate-limit fail-closed (prod/staging) vs fail-open (dev)
- IP_PEPPER 미설정 silent degradation `routes.ts:325-329`
- dummy-verify timing 평탄화 (Step 1-1 핵심 보안)
- JWT clock skew ±60s leeway `session.ts:122-128`

### 🟠 M-5. "S3. 15분 경과 세션 자동 갱신" — 사실과 다름

`scenarios.test.ts:420-428` 는 **시간 경과 없이 즉시 `/refresh` 호출**. "15분" 이라는 제목이 오도. 리포트 `scenarios-20260422-171251.md:33` 동일.

### 🟠 M-6. "S1. 가입하고 자동 로그인" — 사실과 다름

`routes.ts:118-203` `/register` 는 **쿠키를 발급하지 않는다** (201 + user body 만). 시나리오도 명시적으로 `/login` 재호출. 리포트 `scenarios-20260422-171251.md:31` 동일 오도.

---

## Minor 4건

### 🟡 m-1. CASCADE FK 미검증

`migrations/0009:30` `ON DELETE CASCADE` 계약은 시나리오 0건 (S9 는 status='suspended' 만, 삭제 경로 없음).

### 🟡 m-2. 경계 테스트 얕음

S14 payload 33KB 1개만. 32767B, 32768B, 32769B 경계값 미검증.

### 🟡 m-3. response body 검증 얕음

대부분 status 코드 + 쿠키 존재만 검증. `user.id`, `hibpStatus` 등 body 계약 검증 안 됨.

### 🟡 m-4. 공모 탐지 한계

AI 가 "코드 버그 + 시나리오도 동시 수정" 공모 시, 진산님이 알아챌 유일한 단서는 **시나리오 개수 감소** 뿐. 리포트에 git SHA + 라인 수 + 시나리오 이름 해시 같은 정량 지표 필요.

---

## AI 자기참조 방어 신뢰도 산출 — 6/10

| 점수 | 근거                                                                                              |
| ---- | ------------------------------------------------------------------------------------------------- |
| +3   | Vitest 선언적 assertion + 한글 시나리오 → AI 가 결과를 텍스트로 조작 불가                         |
| +2   | 부정 경로 9개(57%) — 행복 경로만 짠 테스트 아님                                                   |
| +1   | 한글 시나리오 이름 → 진산님 파일 열람 시 이해 가능                                                |
| −2   | **Fake D1 이 실제 D1 의 15개 트리거 중 10개 미재현** → 통과 ≠ 프로덕션 안전                       |
| −1   | Access JWT 검증 / IP_PEPPER / timing / WEAK_SECRET 같은 **Phase 1 핵심 보안 5종** 이 0건 시나리오 |
| −1   | S1/S3 시나리오 이름이 사실과 다름 → 리포트로 오도 유통                                            |

---

## 개선 제안

### 지금 당장 추가해야 할 시나리오 (우선순위순)

1. **S17. 위조 Access JWT 로 보호 라우트 호출 → 401** — verifyAccessToken 단위 테스트로 대체 가능. Phase 1 완결성 필수.
2. **S18. WEBHOOK_HMAC_SECRET_MOCK 31자 → 500 WEBHOOK_WEAK_SECRET** — payment.ts:307-313 핵심 분기 0건.
3. **S19. 존재 안 하는 이메일 로그인 vs 정답 비밀번호 로그인 응답 시간 비교** (차이 50ms 이내) — dummy-verify timing 평탄화 검증, Step 1-1 핵심 보안.
4. **S20. IP_PEPPER 미설정 상태 로그인 → sessions.ip_hash=null** — silent degradation 명시 검증.
5. **S21. 32KB 경계 3건** — 32767B OK, 32768B OK, 32769B 413.

### 리포트 수정

- `scenarios-20260422-171251.md:31` "자동 로그인" → "로그인 성공"
- `scenarios-20260422-171251.md:33` "15분 경과 시점" → "refresh 호출 시"
- `scenarios-20260422-171251.md:128` "사상누각 위험 없음" → "Fake D1 로직 검증 완료. 프로덕션 D1 트리거 연동 검증은 Step 1-5 miniflare 통합 테스트 이월"
- `scenarios-20260422-171251.md:85-100` 끝에 **"아직 시나리오로 검증 안 된 것"** 섹션 추가 (Access JWT, IP_PEPPER, timing, WEAK_SECRET, fail-closed)

### 구조 개선

- Fake D1 → `@cloudflare/vitest-pool-workers` + miniflare 로 교체 → 트리거 15개 실제 SQLite 동작 검증. Critical 2건 자동 해소. **1~2일 투자 가치**.
- 시나리오 파일 분할 (`scenarios/auth.test.ts`, `scenarios/webhook.test.ts`).

---

## 진산님 입장에서 이 리뷰의 의미

**진산님 우려가 정확했습니다.**

- 제가 "16/16 통과, 초록불, 사상누각 위험 없음" 이라 보고
- 독립 리뷰어는 "신뢰도 6/10, Critical 2건, 과장" 이라 지적
- **이것이 바로 AI 자기참조의 실체** — AI 는 자기가 짠 검증의 한계를 **스스로 인정하기 어렵다**

### 지금 상태를 일상어로

| 제 이전 주장                    | 실제 현실                                                     |
| ------------------------------- | ------------------------------------------------------------- |
| "16 시나리오 전부 통과"         | ✅ 맞음 (Vitest 런타임 판정)                                  |
| "엔진 견고, 사상누각 위험 없음" | ❌ 과장 — Fake D1 기반이라 프로덕션과 10개 트리거 차이        |
| "Step 1-5 진입 가능"            | 🟡 조건부 — Critical 2건 인식하고 진입 OR 먼저 miniflare 교체 |

---

## 진산님 선택지

### (가) **즉시 개선 — Fake D1 → miniflare 교체 + 시나리오 5개 추가** (1~2일)

- Critical 2건 자동 해소 + 누락 시나리오 보강
- 진짜 "초록불" 로 진입 가능
- Step 1-5 이후 회귀 탐지력 크게 향상

### (나) **현 상태 인지하고 Step 1-5 진입**

- 한계 명시 후 다음 작업 진행
- Step 1-5 에 miniflare 교체 포함 가능

### (다) **리포트만 정정, 시나리오는 유지**

- 오도 표현 (S1 "자동 로그인", S3 "15분 경과") 수정
- 실질 코드 변경 없음 — 가장 빠름

제 권고: **(가)** — 진산님이 제기한 우려의 **근본 해소** 가능. 1~2일 투자로 앞으로 모든 Step 에 재사용되는 견고한 검증 기반.

---

**관련 파일**:

- `/home/soo/ClaudePro/ThePick/apps/api/src/__tests__/scenarios.test.ts` (대상)
- `/home/soo/ClaudePro/ThePick/.claude/reviews/scenarios-20260422-171251.md` (수정 필요 리포트)
- `/home/soo/ClaudePro/ThePick/migrations/0009_sessions.sql` (15 트리거 중 10 미재현)
- `/home/soo/ClaudePro/ThePick/migrations/0008_webhook_events.sql`
