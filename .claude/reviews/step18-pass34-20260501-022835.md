# Step 18 — Pass 3 (Advocate) + Pass 4 (Contract) 독립 4-Pass 리뷰

**리뷰 시각**: 2026-05-01 02:28:35 UTC (KST 11:28)
**리뷰 에이전트**: `pr-review-toolkit:code-reviewer` (독립 서브에이전트)
**리뷰 범위**: 변경 6 파일 + 연관 4 파일 (logger.ts / cost-meter.ts / handoff-025 / ROADMAP §8)
**판정**: ✅ **완료 가능** (CRITICAL 0 / MAJOR 0 / MINOR 4 — 보고만)

---

## Pass 3 — ADVOCATE (Cross-Cutting, UX + 보안)

### ✅ 8건 확인

1. **운영자/사용자 이중 채널 분리 정합** — `recover.ts:166-179` already_completed 분기: Result.message 한국어 사용자 친화 + logger.info JSON 운영자 채널.
2. **recover 7개 status + throw 1개 = 8 분기 빠짐 0건** — 라인 166/186/215/241/266/288/301/324/350 전수 logger 호출 확인.
3. **signal-handlers.ts:31-44 SignalHandlerOptions.logger? 옵션** — 테스트 격리 + 호출자 컨텍스트 누적 가능. defaultLog fallback 으로 누락 시에도 JSON 출력 보장.
4. **LoggerEnvironment narrow 안전성** — pipeline.ts:75-81 / recover.ts:46-52 / signal-handlers.ts:25-29 모두 `VALID_LOGGER_ENVS` Set 검증 → `as LoggerEnvironment` cast 안전.
5. **PII 안전성** — context 필드는 batchRunId(UUID) / examId(enum) / stage / operation / decision 만. 사용자 입력 / 토큰 / API key 0건 주입.
6. **execFileSync + shell 미사용** — `verify-engine-contracts.ts:103-122` args 배열 hardcoded, user input 미수용. command injection 차단.
7. **CI artifact 노출 안전성** — engine-contracts-report.json 은 numeric/boolean/PASS/FAIL/test count 만. batchRunId 등 동적 식별자 0건.
8. **exam_id mismatch CRITICAL 가시화** — `recover.ts:301-307` cross-tenant 차단 흐름이 운영자 채널에 명시 출력. silent failure 0%.

### 🔴 CRITICAL: 0건

### 🟠 MAJOR: 0건

### 🟡 MINOR: 2건 (보고만)

#### MINOR-3A. cost-meter.ts 3건 console.\* 잔존 — Step 18 logger 도입 부분 흡수

- 위치: `apps/batch/src/cost-meter.ts:415, 426, 437`
- 사실: `console.warn` ×2 + `console.error` ×1 production 잔존. verify-engine-contracts checkConsoleUsage 검사 대상은 pipeline/recover/signal-handlers 3 파일로 한정.
- **handoff-025 §2.3 MINOR-A3 명시 범위**: "recover.ts/pipeline.ts logger.info 강화" — cost-meter 미언급. 본 세션 흡수 범위와 정합.
- 권고: Step 19 진입 시 흡수 범위 확장 (cost-meter + checkpoint + d1-batch-runs-db).

#### MINOR-3B. 마이그레이션 카운트 16 hardcode — Pass 2 MAJOR-A1 와 동일 이슈

- 위치: `scripts/verify-engine-contracts.ts:341` + `master-test-checklist.md` §6.2
- 권고: Step 19 plan 진입 게이트에 갱신 의무 명시.
- **본 세션 흡수**: verify-engine-contracts.ts:341 주석 강화 (Pass 2 MAJOR-A1 즉시 흡수와 통합).

### 반론 (Devil's Advocate)

**시나리오**: SIGINT/SIGTERM kill 직후 markBatchRunKilled 비동기 호출 + `process.exit(130)` 동시 → `pipelineLog.error` fire-and-forget. JSON 출력 stdout flush 가 process.exit 직전 손실 가능. **Year 1 default sink (sync console) 안전, Year 2 Workers async sink 진입 시 추가 보강 필요. Step 18 범위에서 현실적 손실 위험 0%.**

---

## Pass 4 — CONTRACT (기획 대조, Silent Pivot 탐지)

### ✅ 10건 확인

1. **handoff-025 §2.3 차세션 흡수 의무 매핑**:
   - **M-2 (master-test-checklist v1 정식판)** → `docs/quality/master-test-checklist.md:1-315` v1 작성 완료 ✅
   - **MINOR-A3 (recover/pipeline logger 강화)** → 9 + 7 = 16건 logger 호출 도입 ✅
   - **MINOR-C2 (AC-RP-1 invariant 매핑)** → master-test-checklist §4.3 5건 매핑 ✅
   - **R-2 (Observability v1)** → Step 19 P-3 명시 이연 ✅
2. **ROADMAP §8 Step 18 충족** — `Step 18 자동 검증 스크립트 PASS (의무화)` → verify-engine-contracts.ts PASS=4 FAIL=0 SKIP=2.
3. **Hard Rule 17 검증 자동화** — exam-ids.ts 외 production 'son-hae-pyeong-ga-sa' 리터럴 0건. CI gate 강제.
4. **Hard Rule 16 examId 시그니처 유지** — recover.ts BatchRunsDb 인터페이스 selectByRunId/updateState/insertNewRun 모두 첫 인자 examId. logger 도입 영향 0건.
5. **auto-review-protocol 규칙 0 (독립 에이전트)** — 본 리뷰 메인 컨텍스트 분리 위임.
6. **Silent Pivot 0건** — handoff §2.3 명시 4건 정확 일치. cost-meter.ts 3건 console 잔존은 §2.3 명시 범위 외라 정합.
7. **네이밍 일관성** — pipelineLog/recoverLog/defaultLog `${service}-Log` 패턴. master-test-checklist Cat 1~8 / AC ID 일관.
8. **production-quality.md 7 금지 패턴 회귀** — any 0건 / hardcoded 0건 / console.log 0건 (Cat 7 PASS) / 빈 catch 0건 / TODO 0건 / `import *` 0건.
9. **CI 강제 보완 관계** — quality-gate.sh hook (PreToolUse) + ESLint (commit) + verify-engine-contracts.ts (CI) 3 단계 시점 구분. 충돌 0건.
10. **Year 2 zero-cost 전환 의무** — recover.ts examId 시그니처 보존 + BatchRunsDb 인터페이스 변경 0건.

### 🔴 CRITICAL: 0건

### 🟠 MAJOR: 0건

### 🟡 MINOR: 2건 (보고만)

#### MINOR-4A. logger.ts 자체 fallback console.\* — 검사 범위 외 (의도된 예외)

- 위치: `packages/shared/src/logger.ts:338, 364, 368`
- 사실: `// eslint-disable-next-line no-console` 명시 + "최종 방어선" / "default sink" 주석. Hard Rule #3 예외 수용 명시.
- 정합성: production-quality.md "빈 catch 금지 + 에러 로깅 + 폴백" 정합.
- 권고: 향후 verify 가 packages/shared 까지 확장될 때 excludeExactPaths 에 logger.ts 추가 필요 (현재 무관).

#### MINOR-4B. ROADMAP §8 미체크 → commit 시 갱신 의무

- 위치: `docs/plans/engine-hardening/ROADMAP.md:496` `[ ]` → `[x]` 갱신.
- **본 세션 즉시 흡수**: commit 전 `[x] Step 18 ✅ ...` 갱신 (4-Pass 영속화와 동시).

### 반론 (Devil's Advocate)

**시나리오 1 (verify-engine-contracts 자기 검증 누락)**: 본 스크립트가 자기 자신 검증 X. `KW_EVAL` 등 토큰 분할 결합 패턴이 production code 다른 곳에서 모방되면 Cat 7 우회 가능. **운영 위험 0% (scripts/ 는 production runtime 미진입), 모방 위험 보고만**.

**시나리오 2 (invariant_fields 매핑 false-negative)**: master-test-checklist §4.3 의 3 invariant 매핑 (formula 251 / quality 41 / parser 136) 동시 실패해도 AC-RP-1 e2e 만 PASS면 false negative 가능. **방어**: verify-engine-contracts Cat 1+2+3 합계 909 hardcoded → 단위/모듈 회귀가 Cat 1+2+3 FAIL 로 검출 → invariant 불변. 충분.

---

## 종합 (Pass 3+4)

| Pass              | CRITICAL | MAJOR | MINOR |
| ----------------- | -------- | ----- | ----- |
| Pass 3 (Advocate) | 0        | 0     | 2     |
| Pass 4 (Contract) | 0        | 0     | 2     |

### 본 세션 즉시 흡수

- **MINOR-3B = MAJOR-A1 (Pass 2)**: verify-engine-contracts.ts:341 주석 강화 (Pass 2 MAJOR-A1 와 통합).
- **MINOR-4B**: ROADMAP §8 line 496 `[x]` 갱신.

### MINOR 4건 차세션 트래킹

| ID       | 항목                                      | 처리 시점                                  |
| :------- | :---------------------------------------- | :----------------------------------------- |
| MINOR-3A | cost-meter.ts 3건 console.\* 잔존         | Step 19 진입 시 흡수 범위 확장             |
| MINOR-3B | 마이그레이션 카운트 hardcode              | Pass 2 MAJOR-A1 통합 흡수 (본 세션)        |
| MINOR-4A | logger.ts 자체 fallback console.\* (예외) | verify 범위 확장 시 excludeExactPaths 추가 |
| MINOR-4B | ROADMAP §8 [x] 갱신                       | 본 세션 commit 시 흡수                     |

### 판정

**완료 가능** — handoff-025 §2.3 P-2 흡수 의무 4건 모두 정합 + Silent Pivot 0건 + production-quality.md Hard Rule 15/16/17 + 7 금지 패턴 0건 회귀 + auto-review-protocol 규칙 0~4 정합.
