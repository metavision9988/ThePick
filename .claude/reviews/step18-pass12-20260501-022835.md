# Step 18 — Pass 1 (Surgeon) + Pass 2 (Architect) 독립 4-Pass 리뷰

**리뷰 시각**: 2026-05-01 02:28:35 UTC (KST 11:28)
**리뷰 에이전트**: `pr-review-toolkit:silent-failure-hunter` (독립 서브에이전트, 자기 확인 편향 0)
**리뷰 범위**: 변경 6 파일 + 연관 4 파일
**판정**: 🟠 **수정 필요** → MAJOR 2건 즉시 흡수 후 완료

---

## 변경 파일

1. `apps/batch/src/pipeline.ts` — 9건 console.error/warn → pipelineLog (createLogger 모듈 스코프) 교체
2. `apps/batch/src/recover.ts` — recover 7개 status 분기 logger.info/warn/error 신규 추가
3. `apps/batch/src/signal-handlers.ts` — SignalHandlerOptions.logger? 옵션 추가 + console.error 2건 교체
4. `scripts/verify-engine-contracts.ts` — 신규 자동 검증 스크립트 (Cat 1+4+6+7 boolean/numeric)
5. `docs/quality/master-test-checklist.md` — v0 골격 → v1 정식판 (8 카테고리 매트릭스)
6. `.github/workflows/ci.yml` — `Verify engine contracts` step + artifact upload

## 연관 파일

- `packages/shared/src/logger.ts` (default sink, JSON serialization)
- `packages/shared/src/index.ts` (export \*)
- `apps/batch/__tests__/signal-handlers.test.ts` (consoleErrorSpy 호환성)
- `migrations/0014_phase05_critical_hardening.sql`, `migrations/0016_knowledge_nodes_batch_idempotency.sql`

---

## Pass 1 — SURGEON (Bottom-Up, 코드 정합성)

### ✅ 7건 확인

1. **logger emit fail-safe 3단계 fallback** — `packages/shared/src/logger.ts:284-342` 정상 직렬화 → 최소 필드 fallback → 원시 console 최후 폴백 → 완전 swallow. emit 절대 throw 안함 (Hard Rule #3 예외 수용 명시).
2. **NODE_ENV 매핑 안전성** — pipeline.ts:68-81 / recover.ts:39-52 / signal-handlers.ts:23-29 모두 `VALID_LOGGER_ENVS` Set 화이트리스트 검증.
3. **vitest --reporter=json 출력 파싱** — 직접 실행 stdout 첫 줄에 단일 JSON object. `indexOf('{')` ~ `lastIndexOf('}')` 슬라이스 정상 동작.
4. **execFileSync shell 미사용 보안** — `verify-engine-contracts.ts:103-122` shell escape 우회 0건 + stdin 차단.
5. **위험 키워드 토큰 분할 결합** — `verify-engine-contracts.ts:84-94` `KW_EVAL`/`KW_FUNCTION`/`KW_INNER_HTML` 분할 후 결합 — 정규식 의미 보존 + security hook false-positive 회피.
6. **signal-handlers.test.ts 6/6 PASS** — logger JSON 출력의 `message` 필드 + `error.message` 필드가 toContain assertion 통과.
7. **logger.serializeError(undefined) 안전성** — `recover.ts:301` 의 `recoverLog.error('...', undefined, {...})` 호출 시 logger.ts:310-312 `if (err !== undefined)` 가드로 직렬화 skip → 정상.

### 🔴 CRITICAL: 0건

### 🟠 MAJOR: 1건

#### MAJOR-S1 — CI artifact 경로 mismatch (즉시 흡수)

- **위치**: `.github/workflows/ci.yml`
- **현상**: `Verify engine contracts` step 의 `pnpm --filter @thepick/batch exec` cwd = `apps/batch/` → `> engine-contracts-report.json` 출력은 `apps/batch/engine-contracts-report.json` 에 저장. 직후 `Upload engine contracts report` step 의 `path: engine-contracts-report.json` (default cwd = repo root) → 파일 not found.
- **영향**: actions/upload-artifact@v4 가 파일 없어도 warning + PASS — silent 누락. master-test-checklist v1 §0.2 "자동화 가능 항목은 verify-engine-contracts.ts JSON 결과 (CI artifact) 로 집계" 의무 위반.
- **흡수 (commit 전)**: upload-artifact path 를 `apps/batch/engine-contracts-report.json` 로 변경 + 주석에 cwd 차이 명시.

### 🟡 MINOR: 4건

- **MINOR-S1**: `filterGrepLines` 인라인 주석 false-positive 위험 — Phase 2 보강.
- **MINOR-S2**: vitest stdout 다중 JSON object 파싱 충돌 가능성 — `--outputFile` 전환 권고 (Phase 2).
- **MINOR-S3**: execFileSync maxBuffer 64MB — 향후 5000+ 테스트 + 인라인 testResults 시 한도 위험. silent failure 차단 자체는 OK.
- **MINOR-S4**: vitest 실패 시 `failedTests=1` 폴백의 사유 불명 — Phase 2 cause 필드 부가.

### 반론 (Devil's Advocate)

- **시나리오 1 (Workers 환경 NODE_ENV 매핑 실패)**: Cloudflare Workers 가 'workers-cron' 같은 custom 값 주입 시 'development' 폴백 → production stack trace 경로 redact 미적용. apps/batch 는 Node.js 전용이라 현 시점 영향 0%, Workers 통합 시 주의.
- **시나리오 2 (SIGINT race)**: `markBatchRunKilled().catch(...)` 내 `pipelineLog.error` 가 `process.exit(130)` 직전 unresolved Promise 라 emit 전 종료 가능. fire-and-forget 본질적 한계, Step 11.6 SF-DA-1 의식적 결정 (`MetaPersistenceFailure` caller alarm 의무 명시) — 의도된 trade-off.

---

## Pass 2 — ARCHITECT (Top-Down, 연계 검증)

### ✅ 6건 확인

1. **import 방향 단방향** — apps/batch/src/\* → @thepick/shared 단방향. 역방향 grep 0건.
2. **logger.ts export 정합** — `packages/shared/src/index.ts:3` `export * from './logger.js'` → Logger / LoggerEnvironment / createLogger 모두 단일 진입점에서 import.
3. **Hard Rule 16 examId 컨텍스트 일관 주입** — pipeline.ts 9건 + recover.ts 7건 모두 `{batchRunId, examId, stage|operation|decision}` 패턴 일관.
4. **마이그레이션 카운트 16 정합** — `migrations/` 폴더 0001~0016 = 16개. `verify-engine-contracts.ts` required=16 일치.
5. **모노레포 합계 909 정합** — master-test-checklist v1 §1.1 합계 = `33+251+136+41+236+199+13 = 909`. `verify-engine-contracts.ts` VITEST_PACKAGES required 합계 동일.
6. **verify-engine-contracts.ts 자기 검증** — 본 스크립트는 `process.stderr.write` 만 사용 (logger 미경유). console.\* 회귀 검사 대상 3 파일 외이므로 자기 grep 영향 없음.

### 🔴 CRITICAL: 0건

### 🟠 MAJOR: 1건

#### MAJOR-A1 — 마이그레이션 카운트 hardcoded `required=16` (즉시 주석 보강)

- **위치**: `verify-engine-contracts.ts:341` (변경 후 line 341-345)
- **현상**: Step 19 `0017_engine_telemetry.sql` 추가 시 observed=17 / required=16 → `count >= required` 통과. 단방향 게이트 (감소만 차단, 증가 허용) → 갱신 망각 시 신규 마이그레이션 미적용 상태로 PASS 가능.
- **영향**: master-test-checklist v1 §0.2 PASS 기준 자체 정의 충족 but Step 19 BATCH-1 직전 게이트 실효성 약화.
- **흡수 (commit 전)**: 본 line 의 주석에 ⚠️ 강조 + Step 19 갱신 의무 + master-test-checklist §6.2 동시 갱신 의무 명시.

### 🟡 MINOR: 4건

- **MINOR-A1**: logger.child() 미활용 — 매 호출마다 inline context 중복. Step 19 리팩토링 권고.
- **MINOR-A2**: cross-tenant 차단 로그 cause 라우팅 — Phase 2 alarm rule 작성 시 cause 필드 인덱싱 의무.
- **MINOR-A3**: Workers 호환성 — `createLogger.fromEnv()` factory 도입 검토 (Phase 2).
- **MINOR-A4**: checkpoint exam_id undefined legacy path — ADR-007 Year 2 plan 명시.

### 반론 (Devil's Advocate)

- **시나리오 1 (artifact 누락)**: MAJOR-S1 fix 누락 시 PR 머지 → CI PASS but artifact 부재 → 진척도 추적 불가능.
- **시나리오 2 (마이그레이션 카운트 false-positive)**: Step 19 plan 작성자가 갱신 망각 + 0017 추가만 → 17 ≥ 16 PASS but telemetry 테이블 trigger 동작 검증 0건 → BATCH-1 실행 시 telemetry 적재 silent fail.

---

## 종합 (Pass 1+2)

| Pass               | CRITICAL | MAJOR | MINOR |
| ------------------ | -------- | ----- | ----- |
| Pass 1 (Surgeon)   | 0        | 1     | 4     |
| Pass 2 (Architect) | 0        | 1     | 4     |

### 본 세션 즉시 흡수 (commit 전)

1. **MAJOR-S1** — `.github/workflows/ci.yml` upload-artifact path 를 `apps/batch/engine-contracts-report.json` 로 명시 + cwd 차이 주석.
2. **MAJOR-A1** — `scripts/verify-engine-contracts.ts:341` 마이그레이션 카운트 주석 강화 + Step 19 갱신 의무 + master-test-checklist 동시 갱신 의무 명시.

### MINOR 8건 차세션 트래킹

| ID       | 항목                                                          | 처리 시점                                            |
| :------- | :------------------------------------------------------------ | :--------------------------------------------------- |
| MINOR-S1 | filterGrepLines 인라인 주석 false-positive                    | Phase 2                                              |
| MINOR-S2 | vitest stdout 다중 JSON 파싱 (--outputFile 전환)              | Phase 2                                              |
| MINOR-S3 | execFileSync maxBuffer 64MB 한도 (silent failure 차단 검증됨) | Phase 2                                              |
| MINOR-S4 | NumericMetric cause 필드 부가                                 | Phase 2                                              |
| MINOR-A1 | logger.child() 활용 리팩토링                                  | Step 19 (CostMeter 도입 시점)                        |
| MINOR-A2 | cross-tenant cause 라우팅 alarm rule                          | docs/observability/master-dashboard.md (Step 19 R-2) |
| MINOR-A3 | createLogger.fromEnv() factory                                | Phase 2                                              |
| MINOR-A4 | checkpoint exam_id legacy path                                | ADR-007 Year 2 Phase 4                               |

### 판정

**MAJOR 2건 즉시 흡수 후 완료 가능** — CRITICAL 0건 / Phase 이월 부채 0건 정책 정합 (MAJOR 2건 본 세션 흡수 + MINOR 8건 명시 트래킹).
