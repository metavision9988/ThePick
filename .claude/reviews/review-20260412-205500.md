리뷰 방식: 독립 에이전트 3개 병렬
리뷰 범위: 변경 파일 4개 + 수정 파일 2개 + 연관 파일 2개

# 4-PASS REVIEW — Step 0-8 M08 Ontology Registry + Schema Validator

## 에이전트 구성

1. silent-failure-hunter → Pass 1 (Surgeon) + Pass 2 (Architect)
2. code-reviewer → Pass 3 (Advocate) + Pass 4 (Contract)
3. pr-test-analyzer → 테스트 커버리지 분석

## Pass 1 (Surgeon): 🔴 3건 / 🟠 2건 — 수정 완료

확인:

- ontology-registry.ts:24 — `as unknown as` 캐스트 런타임 무검증 → assertRegistryShape() 추가
- ontology-registry.ts:29-33 — RegExp 컴파일 에러 핸들링 없음 → compilePattern() 래퍼 추가
- schema-validator.ts:145-156 — truth_weight null/NaN/비정수 통과 → 3단계 검증 추가
- schema-validator.ts:174,183,192,200 — 빈 source_id/target_id 검증 누락 → MISSING_REQUIRED_FIELD 추가
- schema-validator.ts:104-158 — title/content 필수 필드 미검증 → 검증 추가

반론: Claude API가 truth_weight 필드 자체를 생략하면 JSON 파싱 후 undefined가 됨.
현재 코드는 이를 MISSING_REQUIRED_FIELD로 잡지만, 배치 프로세서(M07)에서
default 값 주입 로직이 없으면 매번 거부됨 → M07 구현 시 확인 필요.

## Pass 2 (Architect): 🔴 0건 / 🟠 1건 — 수정 완료

확인:

- import 방향: parser → shared (OK), parser → api (없음, OK)
- Workers 제약: fs/path 사용 없음, JSON import는 esbuild 번들링
- Enum 동기화: registry JSON ↔ shared types ↔ DB schema 7/13/7 일치 확인
- Enum 동기화 테스트 없음 → 4개 동기화 테스트 추가

반론: registry, shared/types.ts, DB schema.ts 3곳에 같은 enum이 수동 유지됨.
빌드 타임 자동 생성은 없으므로, 향후 타입 추가 시 누락 가능 → 동기화 테스트로 방어.

## Pass 3 (Advocate): 🔴 0건 / 🟠 2건 — 1건 수정, 1건 Defer

확인:

- 에러 메시지에 허용 목록 포함 (registry.node_types.join 등) — 개발자 친화적
- ReDoS 위험 없음: 모든 패턴 `^PREFIX-\d{N}$` — 고정 반복, 앵커
- 테스트 커버리지: 13/13 ValidationErrorCode 커버 (DUPLICATE_NODE_ID, INVALID_CONTRACT_STRUCTURE 추가)
- Knowledge Contract 추가 필드(is_active, expected_inputs 등) 미포함 → Defer (Step 0-9에서 처리)
- 입력 구조 검증 누락 → INVALID_CONTRACT_STRUCTURE 에러코드 추가로 해결

반론: Knowledge Contract 타입에 DB schema 필드 전체를 매핑하지 않음(title→name 등).
매핑 실수가 Step 0-9에서 발생할 수 있음 → 매핑 주석을 schema-validator.ts에 추가.

## Pass 4 (Contract): 🔴 0건 / 🟠 0건 — False Positive 1건

확인:

- 설계서(line 697-709) 3개 패턴: CONCEPT-\d{3}, F-\d{2}, INS-\d{2} 정확 반영
- 나머지 4개 패턴: L3 plan에 명시하여 인간 승인 → Silent Pivot 아님 (FP)
- ontology-registry.json + schema-validator.ts 2개 산출물 설계서 일치
- Hard Rule "ontology-registry.json 외 ID 생성 금지" → validator가 미등록 ID 거부로 구현

반론: 설계서 원본에는 3개 패턴만 명시. plan에서 7개로 확장한 것은 승인받았으나,
향후 설계서 업데이트 시 불일치가 생길 수 있음 → 설계서 업데이트는 다음 세션에서.

## 수정 요약

| #   | 이슈                   | 등급     | 조치                                            |
| --- | ---------------------- | -------- | ----------------------------------------------- |
| 1   | as unknown as 무검증   | CRITICAL | assertRegistryShape() 런타임 검증 추가          |
| 2   | RegExp 에러 핸들링     | HIGH     | compilePattern() 래퍼 + 에러 컨텍스트           |
| 3   | truth_weight null/NaN  | CRITICAL | 3단계 검증 (null→필수, isFinite→isInteger→범위) |
| 4   | 빈 source_id/target_id | CRITICAL | MISSING_REQUIRED_FIELD 에러 추가                |
| 5   | 중복 노드 ID           | HIGH     | DUPLICATE_NODE_ID 에러코드 추가                 |
| 6   | 입력 구조 검증         | HIGH     | INVALID_CONTRACT_STRUCTURE 에러코드 추가        |
| 7   | title/content 미검증   | HIGH     | MISSING_REQUIRED_FIELD 추가                     |
| 8   | Enum 동기화 테스트     | HIGH     | 4개 동기화 테스트 추가                          |
| D1  | KC 추가 필드           | Major    | Defer → Step 0-9                                |
| D2  | 필드명 매핑            | Major    | 주석 추가, 매핑은 Step 0-9                      |

## 검증 결과

- pnpm typecheck: 13/13 통과
- vitest run: 54/54 통과 (37 → 54, +17 테스트 추가)

## 판정: 완료 가능 (Critical 0건)
