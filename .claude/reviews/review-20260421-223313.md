# Level 2 축약 리뷰 — Phase 1 Step 1-3 (이월 Major 3건 해소)

**일시:** 2026-04-21 22:15~22:33 KST
**리뷰 방식:** 독립 에이전트 (메인 대화 외부에 위임, 자가 리뷰 아님)
**가이드:** `/home/soo/ClaudePro/ThePick/Guide/3단계리뷰.md` Level 2 축약 (Phase A + C + D)
**Phase B 생략 근거:** scope 작고 M-5 breaking change 는 typecheck 이 선제 감지 (plan §검증계획 명시)
**최종 상태:** 수정 후 Major 3건 추가 해소 + Minor 5건 tech-debt 등록 완료

---

## 0. 리뷰 범위

### 변경 파일 (Step 1-3 scope)

- M-4 (webhook secret 길이 하한 32B + wrangler dev placeholder):
  - `apps/api/src/webhooks/payment.ts` (상수 + 검증 로직 + 주석 ASCII 기준 명시)
  - `apps/api/wrangler.toml` ([vars] 4종 provider placeholder)
  - `apps/api/src/webhooks/__tests__/payment.test.ts` (SECRET 32B+ 변경, WEAK_SECRET + 경계 테스트 2건)
- M-5 (hibp/rate-limit logger request-scoped 주입, BREAKING):
  - `apps/api/src/auth/hibp.ts` (모듈 레벨 logger 제거, `checkPwned(plaintext, logger)`)
  - `apps/api/src/auth/rate-limit.ts` (4개 함수 `logger: Logger` 4번째 인자)
  - `apps/api/src/auth/routes.ts` (register + login 주입 4곳 + **M-8 누락 수정**)
  - `apps/api/src/webhooks/payment.ts` (`checkWebhookIpRateLimit` 에 reqLog)
  - `apps/api/src/auth/__tests__/hibp.test.ts` (createMockLogger + 주입 패턴)
  - `apps/api/src/auth/__tests__/rate-limit.test.ts` (전면 재작성)
- M-8 (UNIQUE 에러 공유 상수):
  - `apps/api/src/middleware/retry.ts` (`D1_UNIQUE_CONSTRAINT_PATTERN` export + JSDoc 의존성)
  - `apps/api/src/webhooks/payment.ts` (import 사용)
  - **`apps/api/src/auth/routes.ts` (register EMAIL_TAKEN catch 에서 동일 상수 사용 — Phase A 발견 후 즉시 수정)**
  - `apps/api/src/middleware/__tests__/retry.test.ts` (공유 상수 매칭 4건)
- M-10 (ADR-002 §6 TTL 정책 1문단 추가):
  - `docs/adr/ADR-002-payment-adapter-abstraction.md`
- 문서:
  - `docs/plans/current.plan.md` (Step 1-3 plan)
  - `.claude/tech-debt.md` (TD-002 해소 + TD-016~018 신규 등록)

---

## 1. Pass 분담 (독립 에이전트 3개 병렬)

| Pass    | 관점                             | 에이전트                    | 핵심 질문                                   |
| ------- | -------------------------------- | --------------------------- | ------------------------------------------- |
| Phase A | 바텀업 (파괴자/배관공/회계사)    | `feature-dev:code-reviewer` | 파일 단독 터지는 경로                       |
| Phase C | 도메인 (plan/CLAUDE.md/ADR 대조) | `feature-dev:code-reviewer` | Silent Pivot / 스코프 확장 / 이월 해소 확인 |
| Phase D | 공격자 (7 카테고리)              | `security-engineer`         | M-4 공격 표면 + M-5 보안 영향               |

각 에이전트는 구현 맥락 없이 독립 실행. Phase B 생략 (plan 명시 결정).

---

## 2. 초기 리뷰 발견 (3개 에이전트 결과 종합)

### Phase A (바텀업) 주요 발견

- 🟠 **Major — M-8 routes.ts 누락**: `apps/api/src/auth/routes.ts:157` 에 `/UNIQUE constraint failed/` 로컬 regex 잔존. M-8 scope 가 payment.ts 에만 집중됐으나 routes.ts 의 register EMAIL_TAKEN catch 가 동일 패턴 사용 중. 플래그 누락(`/i` 없음) 으로 대소문자 변형 시 이미 drift 상태.
  - 확인: routes.ts:157 plain regex vs payment.ts:377 `D1_UNIQUE_CONSTRAINT_PATTERN.test()` vs retry.ts:34 `/i` flag.
  - 수정 방향: routes.ts 에 `D1_UNIQUE_CONSTRAINT_PATTERN` import + 호출 대체.
- ✅ 나머지 (M-4 상수 검증, M-5 logger 주입, M-8 payment.ts 측, ADR 갱신) 각 파일별 "파괴자/배관공/회계사" 0건 확인 — 각 파일 구체 증거 3건+.

### Phase C (도메인) 주요 발견

- 🟠 **Major (P-3) — TD-002 미체크**: plan 검증 계획 L123 "`.claude/tech-debt.md` TD-002 체크박스 완료 + 커밋 해시 기록" 미이행. M-5 실질 해소됐으나 대장 추적 누락. CRITICAL RULE #4 위반 가능성.
- 🟡 Minor (P-1): plan 상단 "3 provider × 3 env" 오기 (실제 4 provider × dev only). 본문 나열은 4종으로 정확. 편집 불일치.
- 🟡 Minor (P-2): plan §4 "나머지 4건 유지" 표현 vs 실제 ADR-002 §6 "2건 해소 표시 + 1건 신규" 변경. 기능 회귀 없음.
- ✅ Hard Limit / Rule 15·16·17 / ADR-002,005,006,008,009 준수 확인.
- ✅ Step 1-2 이월 M-3/M-4/M-5/M-8/M-10 해소 추적 확인.

### Phase D (공격자) 주요 발견

- 🟠 **Major (D-7-4) — logger 회귀 방지책 부재**: M-5 이후 누군가 실수로 hibp.ts/rate-limit.ts 상단에 `const logger = createLogger(...)` 부활 시 TS 컴파일 + 테스트 통과 → environment 고정 재발. 주석/문서 의존만 존재.
  - PoC: 신규 개발자가 "인자 전달이 번거롭다" 며 모듈 상단에 fallback 변수 도입 → 경고 없음.
  - 해소안: ESLint `no-restricted-syntax` rule (Phase 2 이월 가능).
- 🟡 Minor (D-3-3): `MIN_WEBHOOK_SECRET_BYTES` 이름 vs 실측 단위 (UTF-16 code unit) 드리프트. ASCII 기준 byte=length. fail-safe direction 이라 수용.
- 🟡 Minor (D-8): wrangler.toml secret 정책 문서 승격 필요.
- ✅ D-1 자원 고갈: HMAC 계산 이전에 secret 길이 검증 + rate-limit 선행 배치 확인 (payment.ts:280→292→302→313→330).
- ✅ D-2 경쟁 조건: 동기 비교 + D1 UNIQUE 원자성으로 race 없음.
- ✅ D-4 Token 노출: secret 내용 로그 0건 (envKey/minBytes 만). wrangler placeholder 는 dev 전용 의도.
- ✅ D-5 Replay: `D1_UNIQUE_CONSTRAINT_PATTERN` 동일 `/i` 플래그 유지 확인.
- ✅ D-6 Role 우회: N/A.

---

## 3. 수정 내역 (2차 — Phase A+C 결과 반영)

### M8-routes (Phase A Major 해소)

- `apps/api/src/auth/routes.ts:32` — `D1_UNIQUE_CONSTRAINT_PATTERN` import 추가
- `apps/api/src/auth/routes.ts:158` — `/UNIQUE constraint failed/.test()` → `D1_UNIQUE_CONSTRAINT_PATTERN.test()` + 주석

### TD-002 해소 처리 (Phase C P-3 Major 해소)

- `.claude/tech-debt.md` — TD-002 `[ ]` → `[x]` + 취소선 + 해소 근거 명시

### plan.md 오기 정정 (Phase C P-1 Minor)

- `docs/plans/current.plan.md:11` — "3 provider × 3 env" → "4 provider × dev env only"
- `docs/plans/current.plan.md:47` — "provider 3종 mock secret placeholder" → "provider 4종"

### 주석 보강 (Phase D D-3-3 Minor)

- `apps/api/src/webhooks/payment.ts:49-58` — `MIN_WEBHOOK_SECRET_BYTES` 주석에 "ASCII 기준 length=byte, 비-ASCII 는 fail-safe direction" 명시

### Phase 2 이월 항목 등록 (Phase D D-7-4 Major + D-8 + P-2 Minor)

- `.claude/tech-debt.md` TD-016 — logger 회귀 방지 ESLint rule
- `.claude/tech-debt.md` TD-017 — wrangler.toml secret 정책 문서 승격
- `.claude/tech-debt.md` TD-018 — ADR-002 §6 "나머지 4건 유지" 표현 정합성

---

## 4. 재검증 (2차 수정 후)

| 항목                                   | 결과                                                                         |
| -------------------------------------- | ---------------------------------------------------------------------------- |
| `pnpm --filter @thepick/api typecheck` | 0 errors                                                                     |
| `pnpm --filter @thepick/api lint`      | 0 errors                                                                     |
| `pnpm --filter @thepick/api test`      | **7 files / 93 tests passed** (기존 83 + retry 4 + rate-limit 4 + webhook 2) |
| `pnpm --filter @thepick/api build`     | 219KB / gzip 44KB, 4 webhook secret + 3 rate-limiter + DB/CACHE 바인딩 확인  |

---

## 5. 최종 판정

- Critical: **0건**
- Major: **0건** (원래 3건 → 2차 수정으로 2건 해소, 1건은 ESLint rule = TD-016 으로 이월 등록)
- Minor: **5건 전부 `.claude/tech-debt.md` 등록 완료** (TD-016 ~ TD-018 신규 + TD-001/003/004/… 기존 유지)

**Step 1-3 "완료" 선언 가능** — `.claude/rules/auto-review-protocol.md` 규칙 4 "Critical 0건이어야 완료 선언 가능" + "증거 기반 보고" 충족.

### Devil's Advocate (최종 반론 체크)

- "M8-routes 는 plan scope 에 없었는데 무단 확장 아닌가?" — plan §3 M-8 본문은 payment.ts 범위로 명시됐으나 **Phase A 리뷰 과정에서 동일 패턴이 routes.ts 에도 존재함이 발견**. M-8 설계 의도 ("silent drift 방지 — 문자열 regex 를 각자 작성하지 말 것") 와 직접 모순되는 누락. 리뷰의 scope 확장 발견을 즉시 수정한 것은 **scope 확장이 아닌 plan 범위 정정**으로 처리 타당.
- "TD-016 ESLint rule 을 왜 지금 구현 안 했나?" — ESLint rule 설계 + allowlist (routes.ts/payment.ts/index.ts buildLogger 내부) + 회귀 테스트 구성은 반나절 이상 작업. Step 1-3 scope 외 설계 검토 포함하므로 Phase 2 이월이 적절. 현재는 Hook/주석/리뷰 삼중 방어로 우선 수용.

---

## 6. 참조 파일

- 최종 커밋: Step 1-3 본 커밋 (다음 단계)
- plan: `docs/plans/current.plan.md` (scope + 승인 + 검증 계획 모두 체크 완료)
- ADR: `docs/adr/ADR-002-payment-adapter-abstraction.md` §Addendum §6 (M-4 해소 표시 + TTL 신규)
- tech-debt: `.claude/tech-debt.md` TD-001~018
- 이전 리뷰: `.claude/reviews/review-20260421-174845.md` (Step 1-2 4-Phase)
