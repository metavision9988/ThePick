# P0 CRITICAL 정정 (R-C1 / Q-C1 / B-C3) — 4-Pass 독립 에이전트 통합 리뷰

**리뷰 방식: 독립 에이전트** (silent-failure-hunter + system-architect + quality-engineer 3개 병렬, 메인 컨텍스트 자가 리뷰 X)
**작성일:** 2026-04-28 (KST)
**대상:** P0 CRITICAL 정정 작업 결과 — review-gate.sh Stop Hook 의무 4-Pass
**변경 파일 3건:**

- `apps/batch/src/cost-meter.ts` (R-C1: line 30 import + line 330-361 `toCheckpointCostState()` 메서드 신규)
- `apps/batch/src/checkpoint.ts` (Q-C1: line 188-282 `assertCanonicalSafe` 재작성 — visited WeakSet + 9종 거부)
- `migrations/0015_batch_runs.sql` (B-C3: line 60-80 트리거 정정)

**연관 자료:** `.claude/reports/engine-hardening-midpoint-20260428-synthesis.md` (5-페르소나 14 CRITICAL 출처) + `.claude/reports/midpoint-meta-audit-20260428.md` (메타 감사) + `.claude/reports/engine-hardening-vision-analysis-20260428.md` v1.1 §11

---

## 0. 한 줄 결론 — 통합 판정

> **`reject_and_revise` (quality engineer 가장 엄격 기준 채택)**.
> CRITICAL 4건 (모두 quality — 신규 분기 테스트 0건 + plan Silent Pivot) / MAJOR 11건 / MINOR 다수.
> **Step 11.6 코드 진입 BLOCK** — Quality 의무 작업 약 2.25d 추가 (5-페르소나 §3.2 의 0.5d 추정은 4.5배 underestimate).
> 단, **본 정정 자체가 silent failure / system architecture 차원에서는 PASS** (silent-failure + system-architect 모두 `accept_with_caveats`).

---

## 1. 3 에이전트 판정 일람

| 에이전트              |          판정          | CRITICAL | MAJOR  | 산출물                                                        |
| :-------------------- | :--------------------: | :------: | :----: | :------------------------------------------------------------ |
| silent-failure-hunter |  accept_with_caveats   |    0     |   2    | `.claude/reviews/midpoint-20260428-p0fix-silent-failure.md`   |
| system-architect      |  accept_with_caveats   |    0     |   4    | `.claude/reviews/midpoint-20260428-p0fix-system-architect.md` |
| **quality-engineer**  | **reject_and_revise**  |  **4**   | **5**  | `.claude/reviews/midpoint-20260428-p0fix-quality.md`          |
| **합계**              | **reject (가장 엄격)** |  **4**   | **11** | —                                                             |

CRITICAL 4건 모두 quality 영역 — 다른 두 영역은 정합. 즉 **코드 자체는 정합 / 테스트 + plan 문서가 미흡**.

---

## 2. CRITICAL 4건 통합 매트릭스 (모두 quality)

|   #    | 항목                                                                                                    | 영역 | 조치                                                                    |
| :----: | :------------------------------------------------------------------------------------------------------ | :--- | :---------------------------------------------------------------------- |
| **Q1** | `toCheckpointCostState()` 단위 테스트 0건                                                               | R-C1 | 4 시나리오 (empty / soft / soft+hard / soft+hard+kill) 신규 테스트 의무 |
| **Q2** | `assertCanonicalSafe` 신규 4종 (Symbol/WeakMap-WeakSet/Promise/TypedArray) + circular ref 거부 검증 0건 | Q-C1 | 5종 분기 신규 테스트 의무                                               |
| **Q3** | **AC-Snapshot plan 본문 미갱신 (5종 → 9종+circular)** = Silent Pivot = **CRITICAL RULE #1 위반**        | Q-C1 | **즉시 plan v1.1 정정** (0.1d)                                          |
| **Q4** | 0015 트리거 e2e 검증 0건 (mock `BatchRunsDb` 가 RAISE(ABORT) 시뮬레이션 X)                              | B-C3 | better-sqlite3 통합 테스트 의무 + AC-T3 정의 갱신                       |

**Q3 가 가장 시급:** Silent Pivot 은 본 프로젝트 CRITICAL RULE #1 ("기획과 다르게 구현하려면 코딩 멈추고 인간에게 먼저 보고") 직접 위반. plan §7 의 AC-Snapshot 이 5종 거부만 명시한 채로 코드는 9종 거부 = 기획 ≠ 구현.

---

## 3. MAJOR 11건 통합 매트릭스

### 3.1 silent-failure-hunter (2건)

|   #   | 항목                                                                                                                                                     | 조치                                       |
| :---: | :------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------- |
| SF-M1 | Q-C1 재귀 깊이 제한 부재 — circular ref 차단 OK 단 선형 깊이 10K+ 시 V8 stack overflow → CheckpointCorruptedError 매핑 부재                              | `depth` 파라미터 추가, MAX_DEPTH=1000 권고 |
| SF-M2 | R-C1 `start()` 전 `toCheckpointCostState()` 호출 가드 부재 — recordTokens/finalize 와 일관성 깨짐. 초기 spend 만 인계된 정상 스냅샷으로 silent 통과 위험 | `if (this.startedAt === null) throw` 추가  |

### 3.2 system-architect (4건)

|   #   | 항목                                                                                                                                        | 조치                                                                           |
| :---: | :------------------------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------- |
| SA-M1 | **Step 11.6 plan v1.0 §4.3.4 `extractCostState(meter)` free function 가정 vs 실제 R-C1 의 `meter.toCheckpointCostState()` 인스턴스 메서드** | **plan v1.1 정정 의무** — 5곳 호출 갱신                                        |
| SA-M2 | 0015 트리거 이름 변경 (`*_terminal` → `*_non_completed`) 의 마이그레이션 idempotency 가드 부재                                              | `DROP TRIGGER IF EXISTS trg_batch_runs_recover_only_from_terminal;` 한 줄 추가 |
| SA-M3 | `CheckpointCostState` 정의 위치 cost-meter docstring 명시 부재                                                                              | docstring 보강                                                                 |
| SA-M4 | `assertCanonicalSafe` 9종 거부 중 4건 defense in depth — 합리적 안전망이나 명시 의무                                                        | docstring 의 거부 사유 보강                                                    |

### 3.3 quality-engineer (5건)

|  #   | 항목                                                                                                          | 조치                                         |
| :--: | :------------------------------------------------------------------------------------------------------------ | :------------------------------------------- |
| Q-M1 | visited WeakSet 외부 주입 시나리오 미검증                                                                     | 외부 주입 ban 명시 또는 internal-only 변환   |
| Q-M2 | `assertCanonicalSafe` throw → `CheckpointCorruptedError` 통합 부재 (raw Error 누출, batchRunId 컨텍스트 손실) | wrapper 함수 도입                            |
| Q-M3 | `getCurrentSpend()` 부동소수점 → canonical JSON hash 비결정성 위험                                            | toFixed(6) 또는 정수 마이크로USD 직접 직렬화 |
| Q-M4 | 137/137 PASS 거짓 안심 5가지 패턴 명시 — 신규 분기 0% 커버리지                                                | Q1~Q4 정정으로 해소                          |
| Q-M5 | Build SLO `build_correctness 0.999` 회귀 차단 자동화 미구축                                                   | Step 7 contract verify 시점에                |

---

## 4. PASS 증거 통합 (3 에이전트 합계 13건)

본 정정의 강점 — 의도적 검증 사항:

1. cost-meter ↔ checkpoint **단방향 의존** (`import type` runtime 의존성 0)
2. `BatchCheckpoint.cost_state?` ↔ `CheckpointCostState` type-shape 정합
3. Q-C1 9종 거부의 BatchCheckpoint shape 적합 — 정상 흐름 false-positive 0
4. 0015 트리거 vs 0014 prevent_knowledge_nodes_update 테이블 영역 비교차
5. **시험 도메인 커플링 0** 도입 (UKE 비전 §11.4 universal engine 정합)
6. cost-meter.ts:351-361 ↔ checkpoint.ts:60-68 5필드 → 3필드 매핑 정합성
7. 0015 트리거 WHEN 조건이 OLD='in_progress' 정상 recover 경로 허용 (recover.ts:230-234 흐름 추적)
8. checkpoint.ts:227 Date 검사가 line 267 typeof 'object' 분기 이전 — JSON.stringify replacer 진입 전 차단
9. 137/137 PASS 회귀 0건 (단 신규 분기 0% 커버리지 = quality CRITICAL)
10. typecheck PASS
11. canonical JSON 결정성 보장 (visited WeakSet 동작 nondeterministic 0)
12. 트리거 변경의 SQL 레벨 정합 (defense in depth)
13. Application 레벨 24h 판정의 책임 분리 (recover.ts vs SQL 트리거)

---

## 5. Devil's Advocate 통합 (3 에이전트 합계 8 시나리오)

1. **B-C3 kill → recover origin guard 부재** — application logic gap (silent 아님이나 차기 통합 plan 보강)
2. **Q-C1 정상 데이터 거짓 양성** — Map 우발 도입 시 noisy explicit failure (의도된 차단)
3. **R-C1 stage 중간 호출 시 partial breach 누락** — temporal gap (Step 11.6 통합에서 finalize 직전 추가 호출 의무화)
4. **cost-meter 진화 시 `CheckpointCostState` backward compat** — 모든 새 필드 optional 의무
5. **`assertCanonicalSafe` visited WeakSet sharedRef false-positive** — `toSnapshot()` 컨벤션 명시 의무
6. **SQLite 트리거 race window** — Application 레벨 24h+ 분기로 race 진입 차단, AC-R3 fork(child_process) 검증 권고
7. **5-페르소나 §3.2 의 "0.5d 이연" 추정 = 8배 underestimate** (실제 의무 ≈ 3d). Step 11.6 통합 후 결함 발견 비용 5d+ 고려 시 선행이 net 절감.
8. **Q3 Silent Pivot** — plan ≠ 구현 자체가 본 프로젝트 CRITICAL RULE #1 직접 위반. 진산님 즉시 보고 의무 영역.

---

## 6. 진행 권고 — 결정 후보 3건

### 후보 A — Quality 권고 그대로 (Step 11.6 진입 BLOCK + 추가 2.25d)

```
[0.1d]  Q3+SA-M1: Step 11.6 plan v1.1 정정 — AC-Snapshot 9종 명시 + extractCostState 5곳 갱신
[0.1d]  SA-M2: 0015 마이그레이션 idempotency 가드 추가 (DROP TRIGGER IF EXISTS)
[0.1d]  SF-M1+M2: depth 파라미터 + start() 가드 추가
[1.0d]  Q1+Q2: toCheckpointCostState() 4 시나리오 + assertCanonicalSafe 5종 분기 신규 단위 테스트
[0.5d]  Q4: 0015 트리거 e2e 통합 테스트 (better-sqlite3 또는 wrangler --local --persist)
[0.25d] 138+/138+ PASS 확인 + typecheck PASS
[0.25d] 4-Pass 재검증 (3 에이전트 병렬, CRITICAL 0건 후 진행)
─────────
합계: 약 2.3d
```

장점: 메타 감사 결함 5 정합 ("구현 전 설계 완성"). Step 11.6 코드 진입 시 신규 테스트가 e2e 보강 역할.
단점: 5-페르소나 §3.2 시간 추정 1.8d 초과.

### 후보 B — 절충 (Q3+SA-M1+SA-M2 즉시, 신규 테스트는 Step 11.6 동시)

```
[0.3d]  Q3+SA-M1+SA-M2 즉시 정정 (plan v1.1 + 마이그레이션 idempotency)
[1.5d]  B-C1+B-C2 (Step 5 plan + 0016 + BatchRunsDb examId) — 5-페르소나 §3.2 그대로
[0.2d]  ROADMAP v1.2 패치
[2.6~3d] Step 11.6 코드 + 신규 테스트 동시 작성 (Q1+Q2+Q4 흡수)
─────────
합계: 4.6~5d (5-페르소나 §3.2 의 4.4d 대비 +0.2~0.6d)
```

장점: Silent Pivot (Q3) 즉시 차단. 신규 테스트가 Step 11.6 통합 시점에 e2e 로 동시 작성 = 효율.
단점: Step 11.6 코드 진입 시점에 미검증 정정 코드 잠재 = 후보 A 보다 위험.

### 후보 C — 변경 0 (5-페르소나 §3.2 그대로)

장점: 일정 영향 0.
단점: Q3 Silent Pivot 미정정 → CRITICAL RULE #1 위반 지속. quality CRITICAL 4건 누적.
**비추천** — Quality engineer 가 reject 명시.

---

## 7. 권고

**후보 B (절충)** 권고. 사유:

1. Q3 (Silent Pivot) 은 **0.1d 작업** 이라 즉시 처리 가능 — 미루면 CRITICAL RULE #1 위반 확대
2. SA-M1 (plan v1.1 `extractCostState` → `toCheckpointCostState`) + SA-M2 (마이그레이션 idempotency) 동시 처리 — 모두 0.1~0.2d
3. Q1+Q2+Q4 신규 테스트는 Step 11.6 통합 시 자연스럽게 e2e 형태로 흡수 가능 — 분리 작성보다 효율
4. 5-페르소나 §3.2 의 다음 단계 (B-C1+B-C2, 1.5d) 는 변경 없이 진행

단 **진산님이 후보 A (가장 엄격) 채택 시** 즉시 전환 — quality engineer 의 권위 + 메타 감사 결함 5 정합.

---

## 8. 진산님 즉시 결정 요청

|        후보         | 추가 시간 | 위험                                      |
| :-----------------: | :-------: | :---------------------------------------- |
|      A (엄격)       |   +2.3d   | 0                                         |
| **B (절충 — 권고)** | **+0.3d** | **신규 테스트 Step 11.6 흡수 의존**       |
|     C (그대로)      |     0     | Silent Pivot 지속 (CRITICAL RULE #1 위반) |

진산님 응답 후:

- A 채택 → 즉시 plan v1.1 + 마이그레이션 idempotency + 신규 테스트 + 4-Pass 재검증
- B 채택 → 즉시 plan v1.1 + 마이그레이션 idempotency, Step 11.6 통합 시 신규 테스트 동시
- C 채택 → 진산님 명시 사유 요청 (CRITICAL RULE #1 위반 인지 후에도 채택?)

---

## 9. review-gate.sh Stop Hook 통과 조건

본 4-Pass 통합 보고서가 다음 만족:

- ✅ 리뷰 방식 = 독립 에이전트 (3개 병렬, 메인 컨텍스트 자가 리뷰 X)
- ✅ 산출물 위치 = `.claude/reviews/review-YYYYMMDD-HHMMSS-*.md`
- ✅ 영속 git 추적 가능 위치
- ✅ CRITICAL/MAJOR/MINOR 분류
- ✅ Devil's Advocate 8 시나리오
- ✅ PASS 증거 13건

단 **CRITICAL 4건 미해소** = "완료" 선언 X. 본 보고서가 4-Pass 의무 통과 증거이나, 진산님 결정 후 추가 정정 + 재검증 필요.

---

**보고서 작성:** Claude (Opus 4.7) — 메인 컨텍스트, 3 독립 에이전트 산출물 통합
**다음 갱신:** 진산님 결정 후 정정 작업 진입 + v1.1 (정정 결과 반영)
**아카이브:** Engine Hardening 완료 시 `docs/reports/archive/`
