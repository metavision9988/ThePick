# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.astro/
.wrangler/
.turbo/

# Test coverage
coverage/

# Playwright (ADR-040 §5 #7)
playwright-report/
test-results/
.playwright/

# Environment files
.env
.env.local
.env.*.local
.dev.vars

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
*:Zone.Identifier

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Cache
.cache/
*.tsbuildinfo

# Python virtual environments
.venv/

# Cloudflare
.mf/

# Cloudflare Workers generated types (regenerated via `pnpm --filter @thepick/api cf-typegen`)
apps/api/worker-configuration.d.ts

# Manual smoke harness — secrets and raw response payloads
apps/batch/src/__manual__/.env*
apps/batch/src/__manual__/secrets/
apps/batch/src/__manual__/output/
docs/measurements/raw/

# BATCH pipeline checkpoints (recover/snapshot 산출물 — Step 11.5)
.checkpoint/

# Claude Code scheduled task mutex — 세션-로컬 PID/procStart 잠금 파일.
# 머신 간 PID 의미가 다르므로 (linux jiffies 기반) 커밋 시 stale lock으로 cron 영구 차단 위험.
# 5-페르소나 리뷰 devops C-D1 흡수 (Session 076).
.claude/scheduled_tasks.lock

# 트리거형 티어 리뷰 훅(mark-review-pending/enforce-review)의 세션-로컬 마커.
# 코드 변경 시 생성, 리뷰 완료 후 rm 으로 정리되는 휘발성 런타임 상태 — 커밋 금지.
.claude/.review-pending
.claude/.review-files
